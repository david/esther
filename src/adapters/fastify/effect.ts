// Fastify effect adapter — placeholder for future implementation.
// This will handle outgoing side effects that need to go through Fastify
// (e.g., calling external HTTP services via the Fastify instance).

export {};
