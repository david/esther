# 04 — Use event definitions in processor binding and finish gates

## Source
- plan/01-implementation-plan.md

## Goal
Prove generated event schemas work end-to-end with `processorEvent({ schema: Event.schema })`, preserve effect behavior, and finish full project gates.

## Depends on
- 01.md

## Scope
- `src/core/processor.test.ts` or `src/__tests__/pipeline-wiring.test.ts`
- `src/__tests__/type-check.ts` for processor handler inference if not already covered
- `src/core/event.ts` only if generated schema typing needs refinement
- `src/core/processor.ts` only if event-type extraction compatibility or imports need correction
- `src/core/app.ts` only if processor wiring needs correction

## Contract surfaces
- processor event binding schema input
- generated event schema extraction for `onAfterCommit` registration
- processor handler event inference
- effect descriptor execution path
- full project verification gates

## Plan-check watch items
- Keep direct `EventDefinition` acceptance in reducer/read-model/processor APIs out of this slice; users must pass `.schema`.
- Ensure generated `.schema` remains a real `z.ZodObject` with literal `type`, not wrapped by transform/refine that breaks `extractEventType`.
- Preserve `extractEventType` direct module compatibility from `src/core/processor.ts` if tests/imports rely on it.
- Add root-import type tests proving public API, not only relative module imports.

## Work
- Add a focused failing runtime test using `defineEvent(...)` and `processorEvent({ schema: Event.schema, handler })`.
- Use existing app/event-store/effect-adapter test style to emit or append a matching event and verify processor effect behavior.
- Verify unrelated event types do not trigger this processor binding.
- Verify handler receives typed event payload from generated schema; add compile-only assertion in `src/__tests__/type-check.ts` if runtime test cannot prove inference.
- Keep processor API unchanged: the binding must pass `schema: Event.schema`, not the whole event definition.
- Fix only event helper/schema typing or event-type extraction wiring needed to make existing processor path work.
- Run final full gates after focused tests pass.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `processorEvent({ schema: Event.schema })` registers and handles generated-schema events.
- Matching event payload values reach processor handler and produce expected effect descriptor.
- Unrelated event types do not trigger this binding.
- Handler event type inference exposes the event payload from the event definition schema.
- No processor DSL signature changes to accept `EventDefinition` directly.
- Existing raw Zod event schemas and `DomainEvent` aliases still work.
- Full gates pass: `bun run test`, `bun run typecheck`, and `bun run lint`.

## Suggested tests
- Add or extend one focused processor test before production changes.
- Add compile-only handler inference assertion in `src/__tests__/type-check.ts` if needed.
- Keep raw-schema regression coverage intact.

## Manual verification
- not applicable; framework library processor/effect behavior is covered by automated tests and has no UI/manual workflow.

## Verification
- `bun test src/core/processor.test.ts`
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun run test`
- `bun run typecheck`
- `bun run lint`
