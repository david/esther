# 03 — Update public guidance to prefer operations

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Make docs and LLM guidance teach `operations` as the canonical `createApp(...)` key, mention `slices` only as a deprecated compatibility alias, and avoid implying `defineSlice(...)` or adapter rename work exists.

## Depends on
- 01.md
- 02.md

## Scope
- `README.md`
- `llms.txt`
- `doc/architecture.md`
- `doc/domain-language.md`
- other docs/examples found by targeted search that teach `AppConfig.slices`
- no API/code changes unless docs reveal missed task 01/02 scope that must be reported first

## Contract surfaces
- public docs / LLM guidance
- canonical app wiring examples
- deprecated alias documentation
- follow-up surfaces for dispatch/adapters stay explicitly unchanged when mentioned

## Intent / decision vocabulary
- `sliceMeansEventModelingBoundaryOutsideAppConfig`
- `configureDispatchableOperations`
- `acceptDeprecatedSlicesAlias`
- `dynamicDispatchUnchanged`

## Plan-check watch items
- Update `doc/architecture.md`, `doc/domain-language.md`, `llms.txt`, and any README/public guidance that teaches `AppConfig.slices`; leave dispatch/adapters compatibility names explicitly unchanged.
- Search all `createApp({` and `slices:` docs/tests; convert canonical app wiring to `operations` while preserving alias tests.
- Do not rename exported `DispatchFn`, adapter request fields, Fastify route config, `OperationName`, `RegisterableOperation`, or unknown-slice error text.
- Record `llms.txt` update or reason in implementation checkpoint.

## Work
- Search docs and guidance for AppConfig vocabulary drift:
  - `rg "AppConfig|createApp\\(|slices:|defineSlice|sliceName|route\\.slice" README.md llms.txt doc src`
- Update public examples that pass dispatchable commands/queries into `createApp(...)` to use `operations`.
- Add or update wording that `slices` is a deprecated compatibility alias for `AppConfig` only.
- Ensure docs do not introduce or imply `defineSlice(...)`.
- Keep adapter and dispatch compatibility names unchanged; if mentioned, state they are unchanged follow-up surfaces rather than part of this rename.
- Update `llms.txt` in the same slice, or write the exact no-change reason in `impl/checkpoints/03.md`.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` canonical app wiring uses `operations`.
- README and docs prefer `operations` for `createApp(...)` AppConfig examples.
- `slices` appears only as deprecated `AppConfig` compatibility alias or unrelated unchanged adapter/dispatch terminology.
- No doc implies `defineSlice(...)` exists.
- Docs clearly avoid scope creep into dispatch/adapters when those names appear.

## Suggested tests
- `rg "createApp\\(|slices:|defineSlice|sliceName|route\\.slice" README.md llms.txt doc src`
- `bun run typecheck`

## Manual verification
- Open `README.md`, `llms.txt`, `doc/architecture.md`, and `doc/domain-language.md`.
- Confirm canonical `createApp(...)` examples use `operations`.
- Confirm `slices` is described only as deprecated `AppConfig` alias where relevant.
- Confirm no `defineSlice(...)` API is documented.
- Confirm dispatch/adapters names remain documented as unchanged if mentioned.

## Verification
- `rg "createApp\\(|slices:|defineSlice|sliceName|route\\.slice" README.md llms.txt doc src`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
