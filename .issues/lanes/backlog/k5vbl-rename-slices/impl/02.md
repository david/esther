# 02 — Convert canonical app wiring examples in tests

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Update existing tests and examples that teach canonical `createApp(...)` wiring to use `operations`, while preserving explicit deprecated-alias coverage from task 01.

## Depends on
- 01.md

## Scope
- `src/**/*.test.ts`
- `src/**/*.test.tsx`
- `src/__tests__/type-check.ts`
- examples or fixture files under `src/` that show `createApp({ ... slices: ... })`
- no adapter public contract files unless tests expose canonical AppConfig examples there

## Contract surfaces
- test/example usage of `AppConfig`
- deprecated `slices` coverage remains intentionally narrow
- adapter dispatch and route field names remain unchanged

## Intent / decision vocabulary
- `sliceMeansEventModelingBoundaryOutsideAppConfig`
- `dynamicDispatchUnchanged`
- `acceptDeprecatedSlicesAlias`

## Plan-check watch items
- Search all `createApp({` and `slices:` docs/tests; convert canonical app wiring to `operations` while preserving alias tests.
- Keep at least one runtime test and one type test for deprecated `slices` alias.
- Do not rename exported `DispatchFn`, adapter request fields, Fastify route config, `OperationName`, `RegisterableOperation`, or unknown-slice error text.

## Work
- Search test and example code for canonical app config uses:
  - `rg "createApp\\(|slices:" src README.md llms.txt doc`
- In tests/examples where `slices` means `AppConfig` dispatchable commands/queries, change to `operations`.
- Leave explicit deprecated-alias tests from task 01 using `slices`.
- Leave adapter-specific fields alone, including CLI `sliceName`, Fastify `route.slice`, URL fallback path naming, and direct `app.dispatch(sliceName, input)` wording in code.
- Do not change behavior assertions except where expected input config key changed.
- If a test intentionally checks legacy `slices` compatibility outside task 01, either keep it and document why in the checkpoint or consolidate coverage into task 01 tests.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Most canonical `createApp(...)` examples in tests use `operations`.
- Deprecated `slices` runtime and type tests still exist.
- No adapter route/request names or dispatch parameter names are renamed.
- No event-store, read-model, processor, projection, or effect behavior changes.

## Suggested tests
- `bun test src/core/app.test.ts`
- `bun run typecheck`
- `bun run test`

## Manual verification
- not applicable — this is test/example source cleanup with no standalone user workflow; grep review and automated tests verify scope

## Verification
- `rg "createApp\\(|slices:" src README.md llms.txt doc`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
