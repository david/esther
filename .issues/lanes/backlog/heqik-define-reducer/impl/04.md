# 04 — Prove castTagQuery and read descriptors preserve behavior

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal
Add/update runtime coverage proving reducer-backed `castTagQuery` and `eventsByTagsDescriptor` preserve subject lookup, absent/error behavior, subject binding, read interpreter resolution, and boundary observations.

## Depends on
- 03.md

## Scope
- `src/core/slice.test.ts`
- `src/core/read-interpreter.test.ts`
- `src/core/read-model.test.ts` if descriptor helper coverage belongs there
- `src/__tests__/pipeline-wiring.test.ts`
- small fixes in `src/core/slice.ts`, `src/core/read-model.ts`, or `src/core/read-interpreter.ts` only if these tests expose cutover regressions

## Contract surfaces
- `castTagQuery({ reducer })`
- computed `${key}Subject` binding
- read-model cast lookup absent/error paths
- read descriptor `eventsByTagsDescriptor(tags, reducer)`
- read interpreter forwarding to `eventStore.queryByTags(tags, reducer)`

## Plan-check watch items
- Preserve `castTagQuery` absent handling, schema-error behavior, `${key}Subject`, and boundary observation tests while removing subject-aware fold.
- Watch existing `schemas: []` tests; replace with reducer definitions without widening event/state types or introducing unsafe casts.
- Do not add `object`, explicit `any`, or `Record<string, unknown>` while repairing mapped-key/type accumulation issues.

## Work
- Update `castTagQuery` unit tests to create reducer definitions, including empty-schema reducers where previous tests used `schemas: []`.
- Preserve assertions for cast lookup success, absent forwarding, read-model schema errors, and deferred `toStep(deps)` behavior.
- Replace subject-aware fold expectations with reducer-only state assertions plus explicit `${key}Subject` assertions.
- Update pipeline wiring tests proving reducer-backed `castTagQuery` records boundary observations and stale-boundary preconditions still reject conflicting writes.
- Update read descriptor/interpreter tests proving `eventsByTagsDescriptor(tags, reducer)` resolves through `eventStore.queryByTags(tags, reducer)` and returns reducer state.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `castTagQuery` tests no longer rely on subject-aware folds.
- `${key}Subject` remains available and typed/observable where tests exercise cast lookup.
- Absent subject behavior and schema-error behavior match the pre-cutover behavior.
- Read interpreter tests prove descriptor reducers are forwarded to the event store.
- Boundary observation and stale-boundary tests for cast queries still pass.

## Suggested tests
- Use existing slice/read-interpreter/pipeline tests as regression anchors.
- Add focused assertions before code fixes when behavior diverges.

## Manual verification
- not applicable — library DSL/read-interpreter behavior is verified through runtime tests; no UI, server route, or CLI workflow exists.

## Verification
- `bun test src/core/slice.test.ts src/core/read-interpreter.test.ts src/core/read-model.test.ts src/__tests__/pipeline-wiring.test.ts`
