# 03 — Clean obsolete noop-adapter scaffolding

## Source
- plan/01-implementation-plan.md

## Goal
Remove test-only noop input adapter scaffolding that is no longer needed, and update user-facing app wiring docs that imply an input adapter is mandatory.

## Depends on
- 02.md

## Scope
- `src/core/processor.test.ts`
- `src/core/read-model.test.ts`
- `doc/architecture.md`
- optional nearby comments in `src/core/app.ts` or tests if they still imply mandatory input adapter

## Contract surfaces
- Public docs for `createApp()` app wiring.
- Processor and read-model tests that currently provide local noop input adapters only to satisfy app construction.
- No event, persistence, read-model, processor, effect, adapter, or dispatch contract changes.

## Plan-check watch items
- Update any docs/comments that still imply `createApp()` requires one input adapter binding, especially if `doc/architecture.md` remains user-facing for app wiring.
- Remove only local noop helpers that become clearly obsolete; do not mass-remove `createInMemoryAdapter()` from integration tests.
- Avoid broad integration-test rewrites.
- Keep core/adapters dependency boundary unchanged.

## Work
- Remove `createNoopInputAdapter()` from `src/core/processor.test.ts` if all call sites can omit `inputAdapter` cleanly.
- Update those `createApp()` calls to omit `inputAdapter` and keep the original processor assertions unchanged.
- Remove `createNoopInputAdapter()` from `src/core/read-model.test.ts` if all call sites can omit `inputAdapter` cleanly.
- Update those `createApp()` calls to omit `inputAdapter` and keep the original read-model assertions unchanged.
- Do not remove `createInMemoryAdapter()` from broader integration tests unless a local compiler error directly requires it.
- Update `doc/architecture.md` app wiring language from required one input adapter binding to optional input adapter binding / direct dispatch without transport.
- Check nearby comments touched by tasks 01–02 for stale mandatory-adapter wording and update only if needed.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Local noop input adapter helper is gone from `src/core/processor.test.ts` when safe.
- Local noop input adapter helper is gone from `src/core/read-model.test.ts` when safe.
- Processor and read-model tests still exercise the same processor/read-model behavior through `createApp()`.
- `doc/architecture.md` no longer says `createApp()` wires one mandatory input adapter binding.
- No broad integration-test churn or concrete adapter behavior changes are introduced.
- Full project gates pass.

## Suggested tests
- Existing processor tests should cover processor behavior after removing noop scaffolding.
- Existing read-model tests should cover read-model behavior after removing noop scaffolding.
- Full typecheck should catch stale `AppConfig` assumptions.

## Manual verification
- not applicable — cleanup/docs slice has no external runtime workflow; automated tests and doc review cover it.

## Verification
- `bun test src/core/processor.test.ts src/core/read-model.test.ts`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
