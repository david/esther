# 03 — Cover raw and cast observations with no fanout

## Source
- plan/01-implementation-plan.md

## Goal

Prove all command-side observation paths use same tag-visibility guard and mismatch causes no downstream fanout.

## Depends on
- 02.md

## Scope
- `src/core/pipeline.ts`
- `src/__tests__/pipeline-wiring.test.ts`
- app wiring helpers in existing tests, only if needed

## Contract surfaces
- raw command event path
- command-side `castTagQuery(...)`
- read-model/projector fanout via `onAfterInsert`
- processor/effect fanout via `onAfterCommit`
- event-store append boundary

## Intent / decision vocabulary
- `noMismatchFanout`
- `castTagQuery(...)` mismatch uses same guard as `tagQuery(...)`
- raw event path still guarded despite no event-definition schema
- side effects run only after successful append

## Plan-check watch items
- Verify no append by querying store or append counter, not only by result error.
- Verify no fanout across read-model binding/projector and processor/effect paths; current hook names are `onAfterInsert` and `onAfterCommit`.
- Keep `EventTagMismatchError` as framework error bypassing `outputErr`.
- Keep core free of adapter imports; do not duplicate guard in event-store adapters.

## Work
- Add focused failing tests in `src/__tests__/pipeline-wiring.test.ts` for raw command event mismatch returning `EventTagMismatchError`.
- Add focused failing test for `castTagQuery(...)` observation mismatch returning `EventTagMismatchError`.
- Add no-fanout test that registers read-model binding/projector and processor/effect paths, dispatches mismatch, and asserts counters stay zero.
- If helper from 02 already covers raw/cast paths, keep implementation change minimal and only fix uncovered edge.
- Do not add Fastify-specific mapping or event-store adapter policy.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Raw command event path cannot append event missing observed tags.
- `castTagQuery(...)` observation cannot append event missing observed tags.
- Mismatch result is `EventTagMismatchError`, not app/user `outputErr`.
- Mismatch appends no event.
- Mismatch runs no read-model binding/projector, processor, or effect fanout.

## Suggested tests
- Add raw-path, cast-path, and no-fanout pipeline tests before any implementation adjustment.

## Manual verification
- not applicable — side-effect behavior is internal library fanout; counters in automated tests are more reliable than manual observation.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun run typecheck`
