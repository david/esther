# 04 — Update public notes and run full gates

## Source
- plan/01-implementation-plan.md

## Goal

Finish public-facing notes for the stricter descriptor read behavior, run full repo verification, and record final implementation alignment.

## Depends on
- 01.md
- 02.md
- 03.md

## Scope
- `llms.txt`
- docs or examples only if implementation changed public examples beyond `llms.txt`
- `impl/checkpoints/04.md`
- full repo verification commands

## Contract surfaces
- Public documentation/examples for processor/read-model descriptor reads.
- Release/rollout note: malformed projection rows now fail fast in interpreter paths.
- No code contract change should be introduced by this task unless a docs build/test issue exposes a small missing counterpart update.

## Plan-check watch items
- Update `llms.txt` if public examples or validation notes change; otherwise record no-update reason in implementation checkpoint.
- Ensure final checks cover type-level tests, lint/dependency boundaries, and runtime tests.
- Do not introduce late scope: no adapter redesign, typed app client, event change, migration, or broad cleanup.

## Work
- Inspect final implementation from tasks 01–03 against `plan/01-implementation-plan.md` and `plan/checks/01-plan-sanity.md`.
- Inspect `llms.txt` for processor/read-model read examples and validation notes.
- Update `llms.txt` if public API behavior, DSL examples, errors, or canonical examples changed.
- If no `llms.txt` update is needed, write the reason in `impl/checkpoints/04.md`.
- Run full repo gates.
- If full gates expose small missing counterpart changes directly caused by tasks 01–03, fix them inside this task and rerun relevant gates; otherwise stop and report blocker.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` either documents new descriptor read typing/validation behavior or checkpoint records why no update was needed.
- Full typecheck, lint, and test gates pass.
- Final checkpoint records alignment with plan and notes any docs/release-note decision.
- No event payload, persistence format, adapter boundary, or app dispatch contract changed.

## Suggested tests
- No new focused tests expected unless final gates expose missing direct coverage.

## Manual verification
- CLI/library QA only:
  - run full typecheck to prove handler read inference.
  - run full runtime tests to prove validation behavior and unchanged hook semantics.
  - inspect `llms.txt` for processor/read-model read examples and validation note accuracy.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
