# 02 — Record rollout note and run final API gates

## Source
- plan/01-implementation-plan.md

## Goal
Capture the breaking root export change and supported alternatives in a durable rollout note, then verify the narrowed public API with full repo gates.

## Depends on
- 01.md

## Scope
- issue-local release or rollout note, for example `release-notes/root-export-surface.md` or an equivalent issue artifact
- `src/index.ts`
- `src/__tests__/type-check.ts`
- final verification commands

## Contract surfaces
- TypeScript module API for the `esther` root export surface
- release/deploy communication for removed root exports and migration alternatives
- no runtime, event, persistence, read-model, processor, adapter, auth, or side-effect contracts

## Plan-check watch items
- Include release-note wording or a durable note listing removed root exports and supported alternatives before deployment.
- Verify the generated/type-visible root API does not accidentally require consumers to name `ReadInterpreter`, `ProjectionStore`, `SliceDeps`, `Step`, or other removed internals through exported signatures.
- Keep `BoundaryObservation` and `BoundaryObservationError` root-public and preserve type-check coverage because they are observable error/detail contracts.
- Keep deprecated `ProjectionAdapterEntry`, `ProjectionAdapterTableEntry`, `ProjectionAdapterViewEntry`, `projectionAdapters`, and `projectionQuery` compatibility unless a separate removal issue/release decision is made.

## Work
- Create an issue-local rollout/release-note artifact that names the removed root exports from the plan.
- In that note, document supported alternatives:
  - use `createApp().dispatch` or input adapters instead of `executeCommand` / `executeQuery`
  - let `createApp()` own read-interpreter and projection-store wiring
  - use `BoundaryObservation` / `BoundaryObservationError` instead of `SliceDeps` for public error/detail handling
- Re-check `src/index.ts` and `src/__tests__/type-check.ts` against the plan acceptance criteria.
- Run full repo gates after task 01 is complete.
- If full gates reveal an API leak or behavior regression, fix only the local issue needed to satisfy the approved plan, or stop and report high-risk drift.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- A durable issue-local rollout/release-note artifact lists the removed root exports and supported alternatives.
- The source inspection command finds no removed root export names in `src/index.ts`, except for an intentional comment if one was added and justified in `impl/checkpoints/02.md`.
- `BoundaryObservation`, `BoundaryObservationError`, and deprecated compatibility exports remain root-public.
- Full gates pass: `bun run typecheck`, `bun run lint`, and `bun run test`.
- No runtime behavior changes are introduced.

## Suggested tests
- No new standalone tests unless task 01 left a gap; this task validates the completed public API slice with full gates.
- If negative import checks were not feasible in `src/__tests__/type-check.ts`, keep the targeted source/export inspection command as the explicit regression check.

## Manual verification
- Starting state: task 01 is complete and its checkpoint is written.
- Review the rollout/release-note artifact created in this task.
- Confirm it names the removed root exports and gives the three supported alternatives from the plan.
- Confirm `src/__tests__/type-check.ts` still has positive root import coverage for stable DSL/app symbols, adapter extension contracts, operation helper types, and `BoundaryObservationError`.
- Regression check: confirm the rollout note does not suggest adding an `esther/unstable`, `esther/internal`, or `esther/adapter-kit` subpath in this issue.

## Verification
- `rg -n "executeCommand|executeQuery|createReadInterpreter|ReadInterpreterDeps|ProjectionStore|SliceDeps|CompileDeps|CompiledOperation|StepError|InlineResult" src/index.ts`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
