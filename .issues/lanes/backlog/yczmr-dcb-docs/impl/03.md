# 03 — Mirror DCB rules in llms guidance

## Source
- plan/01-implementation-plan.md

## Goal

Update `llms.txt` so generated Esther code follows the same DCB safety rules taught by `doc/dcb.md`.

## Depends on
- 01.md

## Scope
- `llms.txt`

## Contract surfaces
- LLM/API guidance for DCB behavior, DSL usage, examples, and sharp edges
- no runtime API, event, persistence, adapter, or schema changes

## Intent / decision vocabulary
- `observedBoundary`
- `appendGuard`
- `decisionTags`
- `projectionContext`
- `futureVisibilityTags`
- `singleBoundaryLimit`

## Plan-check watch items
- Keep `llms.txt` concise but update it because DSL behavior docs/canonical examples change.
- Keep rules consistent with `doc/dcb.md`.
- Include projection-only misuse and missing-emitted-boundary-tag misuse.
- State tag intersection semantics and global stream boundary clearly.

## Work
- Update existing `llms.txt` DCB guidance rather than duplicating a long tutorial.
- Add or tighten quick rules: only command-side `tagQuery(...)` / `castTagQuery(...)` create DCB append guards; query-side reads and projection reads do not.
- Add concise decision checklist for choosing `decisionTags`.
- Add sharp edges: tag intersection semantics, `[]`/`undefined` global stream, one observed command boundary, no emitted-tag verification, DCB is not auth.
- Add compact counterexamples matching guide: projection-only decision, too-narrow tags, emitted event missing future visibility tag.
- If `llms.txt` includes examples, ensure emitted event tags include same decision boundary tag when event can affect future decisions.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` includes DCB quick rules, decision checklist, sharp edges, and current limits matching `doc/dcb.md`.
- `llms.txt` explicitly distinguishes event-history reads from projection/read-model reads.
- `llms.txt` includes at least one common misuse and preferably all three planned misuses.
- `llms.txt` explicitly says tag queries use intersection semantics.
- `llms.txt` explicitly says `[]`/`undefined` boundary means global stream.
- `llms.txt` explicitly says framework does not verify emitted event tags match observed boundary tags.
- `llms.txt` remains concise enough to serve as LLM guidance.

## Suggested tests
- No automated test-first step; docs-only LLM guidance.
- Compare `llms.txt` DCB claims against `doc/dcb.md` from 01.md before completion.

## Manual verification
- Read `llms.txt` DCB guidance and answer: “If a command uses `lookup(...)` to read balance, is append concurrency-safe?” Expected: no; use command-side `tagQuery(...)` / `castTagQuery(...)` over tags that cover invalidating events.
- Read `llms.txt` DCB guidance and answer: “Will Esther verify emitted event tags match the observed boundary?” Expected: no; app author must ensure future visibility tags.

## Verification
- `grep -n "DCB\|tagQuery\|castTagQuery\|lookup\|intersection\|global stream\|BoundaryObservationError\|ConcurrencyError" llms.txt`
- `git diff -- llms.txt`
