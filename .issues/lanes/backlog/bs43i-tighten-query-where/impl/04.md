# 04 — Run final gates and drift check

## Source
- plan/02-implementation-plan.md
- plan/checks/02-plan-sanity.md

## Goal
Verify tightened `where` type/runtime contract across full repo gates, confirm docs are current, and catch any scope drift before review/QA.

## Depends on
- 03.md

## Scope
- full repository verification
- `llms.txt` docs check
- issue workflow checkpoint for final implementation state

## Contract surfaces
- public TypeScript DSL already changed in task 01
- runtime descriptor/query behavior already changed in tasks 02 and 03
- documented read-model query grammar in `llms.txt`
- repo quality gates: typecheck, lint, test

## Plan-check watch items
- Update `llms.txt` so object/array fields are documented as storage-only for `where`.
- Run drift check if implementation expands into `orderBy`, JSONB operators, nested paths, joins, or adapter SQL semantics.
- Preserve `defineReadModel` support for `ZodArray`/`ZodObject`; only `where` support narrows.

## Work
- Review completed diff against `plan/02-implementation-plan.md` and `plan/checks/02-plan-sanity.md`.
- Confirm `llms.txt` read-model query section documents primitive-only `where` grammar and object/array non-queryability.
- Confirm no unplanned changes landed in event model, persistence/migrations, auth, adapters, `orderBy`, JSONB operators, nested paths, or joins.
- If implementation expanded into a plan-watch drift area, stop and run/request `{{/skill:drift bs43i-tighten-query-where}}` before broadening scope.
- Run full repo gates.
- Record final implementation checkpoint with gate results and any doc/drift findings.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `bun run typecheck` passes.
- `bun run lint` passes.
- `bun run test` passes.
- `llms.txt` is current for read-model `where` grammar.
- No unplanned adapter/persistence/event/auth/query-operator scope drift remains unreviewed.
- Final checkpoint records verification evidence.

## Suggested tests
- No new tests expected in this task unless full gates reveal missing coverage from earlier slices.

## Manual verification
- not applicable — automated QA only per source plan; no UI or manual workflow exists.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
