# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-29

## Summary
- Updated public guidance to prefer `operations` for `createApp(...)` AppConfig wiring.
- Documented `slices` as deprecated AppConfig compatibility alias only.
- Kept dispatch/adapters names (`sliceName`, `route.slice`, unknown-slice text) unchanged and documented as dynamic compatibility surface.
- Updated `llms.txt` canonical examples and rule guidance.

## Files changed
- `README.md`: changed high-level wording from slices to operations.
- `llms.txt`: changed app wiring examples to `operations`; added deprecated `slices` alias wording; kept dispatch/adapters `sliceName` guidance unchanged.
- `doc/architecture.md`: changed app wiring and app-module guidance from slices to operations/commands/queries while preserving dynamic `sliceName` boundary.
- `doc/domain-language.md`: changed selected schema/output wording from slice to operation while preserving input adapter `(sliceName, input)` contract.

## Verification
- `rg "AppConfig|createApp\\(|slices:|defineSlice|sliceName|route\\.slice" README.md llms.txt doc src`: pass — docs/examples prefer `operations`; `slices` remains only deprecated AppConfig alias, explicit alias tests, internal compatibility type, and unchanged adapter/dispatch terminology; no documented `defineSlice(...)` API.
- `bun run typecheck`: pass.
- `bun run lint`: pass.
- `bun run test`: pass — 281 tests.

## Manual verification
- `README.md`: pass — public intro uses operations, not slices.
- `llms.txt`: pass — canonical `createApp(...)` examples use `operations`; `slices` documented only as deprecated AppConfig alias; `defineSlice(...)` explicitly absent; dispatch/adapters names unchanged.
- `doc/architecture.md`: pass — app wiring says registered operations; dynamic `sliceName` dispatch remains unchanged.
- `doc/domain-language.md`: pass — operation schema wording used; input adapter `(sliceName, input)` contract remains unchanged.

## Drift assessment
- aligned with `impl/03.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- `sliceMeansEventModelingBoundaryOutsideAppConfig`: public guidance stops teaching `AppConfig.slices` as canonical.
- `configureDispatchableOperations`: docs and LLM guidance use `operations` for AppConfig wiring.
- `acceptDeprecatedSlicesAlias`: docs name `slices` as deprecated AppConfig alias.
- `dynamicDispatchUnchanged`: dispatch parameter, CLI `sliceName`, Fastify `route.slice`, and unknown-slice compatibility remain untouched.

## Follow-ups
- none.
