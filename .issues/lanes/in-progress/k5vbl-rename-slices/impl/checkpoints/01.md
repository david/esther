# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-29

## Summary
- Added canonical `operations` AppConfig branch.
- Kept deprecated `slices` alias branch.
- Added explicit mixed-key runtime rejection with exact planned error.
- Added runtime and type-level coverage for `operations`, deprecated `slices`, mixed keys, missing operation list, empty operations, and unchanged unknown-slice text.

## Files changed
- `src/core/app.ts`: split `AppConfig` into base plus `operations` / deprecated `slices` branches; added `resolveOperationsConfig`; compiled resolved operations.
- `src/core/app.test.ts`: added canonical operations, deprecated alias, mixed-config, and empty operations unknown-dispatch tests.
- `src/__tests__/type-check.ts`: added `AppConfig` type checks for operations, deprecated slices, mixed keys, and missing key.
- `.issues/lanes/in-progress/k5vbl-rename-slices/`: moved issue from backlog to in-progress for implementation.

## Verification
- `bun test src/core/app.test.ts`: pass — 6 tests.
- `bun run typecheck`: pass.
- `bun run lint`: pass.
- `bun run test`: pass — 281 tests.

## Drift assessment
- aligned with `impl/01.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- `configureDispatchableOperations`: implemented through canonical `operations` config.
- `acceptDeprecatedSlicesAlias`: preserved with deprecated TypeScript branch and runtime test.
- `rejectMixedOperationConfig`: implemented with key-presence checks and exact error `AppConfig cannot define both operations and slices; prefer operations`.
- `dynamicDispatchUnchanged`: `dispatch(sliceName, input)`, `DispatchFn`, adapter fields, `OperationName`, `RegisterableOperation`, and `Unknown slice: ...` text unchanged.

## Follow-ups
- Continue with `impl/02.md` to convert canonical test/example wiring from `slices` to `operations` while preserving alias coverage.
