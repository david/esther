# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-26

## Summary
- Added typed Fastify route contracts and `defineFastifyRoutes` for preserved slice tuples.
- Added `routes?` to `FastifyAdapterConfig` as an additive optional config field while keeping existing `port` and `hostname` usage valid.
- Added compile-only route binding coverage for command/query slice names, input mapper correlation, unknown names, mismatched mapper returns, and typed `respond` results.

## Files changed
- `src/adapters/fastify/input.ts`: added Fastify route method/request/binding/config contracts and the route definition helper.
- `src/adapters/fastify/index.ts`: exported the new Fastify route helper and route contract types from the adapter entrypoint.
- `src/__tests__/type-check.ts`: added positive and negative compile-only tests for typed Fastify route bindings using the task 01 command/query tuple.
- `.issues/lanes/in-progress/hgqcm-typed-adapter-bindings/impl/checkpoints/02.md`: recorded this implementation checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 227 tests passed.

## Manual verification
- not applicable — this task defines compile-time adapter contracts only; runtime route behavior is implemented by later tasks.

## Drift assessment
- Aligned with `impl/02.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- Route `slice` literals are correlated with each route's `input` mapper and optional `respond` result via a mapped-union `FastifyRouteBinding` over the preserved tuple supplied to `defineFastifyRoutes<typeof slices>()`.
- `FastifyAdapterConfig.routes?` is additive and type-erased enough for runtime config acceptance without changing the dynamic adapter-to-core dispatch boundary.
- Fastify-specific request/reply types remain inside the Fastify adapter surface; core remains adapter-agnostic.
- New helper/types are exported from the Fastify adapter entrypoint only, matching the existing optional-adapter subpath policy; `src/index.ts` was not broadened to include Fastify exports.
- No runtime route execution behavior, event/read-model/persistence behavior, schemas, processors, or side effects were changed.

## Follow-ups
- none
