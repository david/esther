# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-26

## Summary
- Registered configured Fastify routes before the wildcard fallback.
- Explicit Fastify routes now map the documented request context, call `boundDispatch(route.slice, mappedInput)`, and use the shared default result/error mapping.
- Preserved wildcard URL-path-derived dispatch for no-routes and no-match cases.
- Added runtime Fastify coverage for explicit dispatch, request mapper context, default response mapping, unbound route handling, and wildcard compatibility.

## Files changed
- `src/adapters/fastify/input.ts`: added explicit route registration, shared default result mapping, and request-context construction for route input mappers.
- `src/__tests__/fastify-input.test.ts`: added explicit-route dispatch/input/default-mapping tests plus no-routes and fallback wildcard regression coverage.
- `.issues/lanes/in-progress/hgqcm-typed-adapter-bindings/impl/checkpoints/03.md`: recorded this implementation checkpoint.

## Verification
- `bun test src/__tests__/fastify-input.test.ts`: pass — 13 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 235 tests passed.

## Manual verification
- Fastify explicit-route CLI scenario: pass — one-off `bun --eval` configured `POST /bookings` with `slice: "create-booking"`, injected `{ tenantId: "t1" }`, observed dispatch `{ sliceName: "create-booking", input: { tenantId: "t1" } }`, and received default `{ data: { id: "b1" } }` response.
- Fastify no-routes wildcard CLI regression: pass — one-off `bun --eval` configured no `routes`, injected `GET /balance?accountId=a1`, and observed dispatch `{ sliceName: "balance", input: { accountId: "a1" } }`.

## Drift assessment
- Aligned with `impl/03.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- Explicit routes dispatch through the existing dynamic `DispatchFn` boundary using the configured route slice and mapper-produced `unknown` input; no alternate core execution path was added.
- Default success and framework-error mapping is now shared between explicit routes and wildcard routes, reducing drift risk.
- Wildcard behavior remains source-compatible when `routes` is absent and remains available when configured routes do not match.
- No casts were added; core, event stores, read models, processors, and effect adapters were not changed.
- Optional explicit-route `respond` override behavior remains intentionally deferred to `impl/04.md`.

## Follow-ups
- Continue with `impl/04.md` for optional typed Fastify response override runtime behavior and final compatibility checks.
