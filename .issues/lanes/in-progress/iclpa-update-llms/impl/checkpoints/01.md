# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-29

## Summary
- Updated `llms.txt` package export guidance to include current root exported helper/type inventory from `src/index.ts`.
- Added public subpath inventory for `esther/cli`, `esther/postgres`, `esther/filesystem`, `esther/test`, and preserved Fastify/React subpath surfaces.
- Clarified `DispatchFn` / `AppDispatchFn` dynamic dispatch aliases and kept `createInMemoryProjectionAdapter` on root `esther`, not `esther/test`.
- Labeled `EventRecordInput`, Postgres constraint helpers, and dispatch aliases as low-level interop/adapter helpers, not canonical app DSL.
- Replaced stale generic slice wording with operation wording except exact compatibility terms.

## Files changed
- `llms.txt`: public export inventory, adapter subpath exports, low-level helper labels, dynamic dispatch wording, operation vocabulary cleanup.
- `.issues/lanes/in-progress/iclpa-update-llms/impl/checkpoints/01.md`: implementation checkpoint.
- `.issues/lanes/in-progress/iclpa-update-llms/**`: issue moved from backlog to in-progress before implementation per impl protocol.

## Verification
- `rg -n "AppConfig\.slices|defineSlice|createFastifyAdapter|projectionAdapters:|projectionQuery:" llms.txt`: pass — only negative compatibility guidance for unsupported `AppConfig.slices` / `defineSlice(...)` appeared; no `createFastifyAdapter`, `projectionAdapters:`, or `projectionQuery:` snippets.
- `rg -n "InputPipeline|StateResolver|ReadDescriptor|GetDescriptor|QueryDescriptor|EventsByTagsDescriptor|ReadModelEventBinding|ProjectionQueryAdapter|WhereEntry|EventFilter|OnAfterInsertHandler|OnAfterCommitHandler|ProjectionAdapterEntry|ProjectionAdapterTableEntry|ProjectionAdapterViewEntry|ProcessorEventBinding|AppDispatchFn|CliDispatchRequest|FilesystemEventStoreConfig|PostgresEventStoreConfig|isConstraintViolation|mapConstraintError|CastTagQueryDescriptor|CommandLookupDescriptor|CommandLookupByArgsDescriptor|CommandLookupByIdDescriptor|CommandDefinition|DeriveStep|GenerateStep|OutputErrHandlers|ProjectionStep|QueryProjectionStep|TagQueryStep|ValidatePredicate|InMemoryInputAdapter" llms.txt`: pass — every required watch-list symbol found in root import block, subpath imports, or adapter helper prose.
- `rg -n "export (type |\{|function )|export type \{" src/index.ts src/adapters/cli/index.ts src/adapters/fastify/index.ts src/adapters/filesystem/index.ts src/adapters/postgres/index.ts src/adapters/react/index.ts src/adapters/in-memory/index.ts`: pass — cross-checked root and adapter index export surfaces before and after docs edit.
- manual docs QA: pass — read `llms.txt` top-to-bottom after edits; package/subpath sections match `package.json`, `src/index.ts`, and adapter `index.ts` files at useful LLM granularity; low-level exported helpers are scoped as interop; guide remains compact; no stale API examples found beyond explicit unsupported/deprecated compatibility guidance.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json`.
- `bun run lint`: pass — ESLint plus dependency-cruiser, no dependency violations.
- `bun run test`: pass — 279 pass, 0 fail, 690 expect calls, 21 files.

## Drift assessment
- Aligned with `plan/01-implementation-plan.md`: docs-only change; no source API, persistence, migration, auth, side-effect, or runtime behavior changed.
- Aligned with `plan/checks/01-plan-sanity.md`: full root export cross-check performed, additional command/descriptor helper types documented, `InMemoryInputAdapter` included, low-level helpers labeled as interop, `projectionAdapters` / `projectionQuery` kept only as deprecated compatibility prose, and `slice` wording remains only for `sliceName`, Fastify route `slice`, `SliceError`, unknown-slice text, or explicit unsupported/deprecated compatibility guidance.
- Aligned with `research/01-current-state.md`: export-surface precision, adapter subpath docs, `esther/test` exact surface, dispatch alias wording, and operation vocabulary updated without regressing existing DSL/error/read-model/Fastify guidance.

## Follow-ups
- none
