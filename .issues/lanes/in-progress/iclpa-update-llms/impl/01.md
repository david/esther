# 01 — Update llms export surface guide

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md
- research/01-current-state.md

## Goal

Update `llms.txt` so package root exports, adapter subpath exports, dynamic dispatch wording, low-level helper labeling, and operation vocabulary match current repository public API without changing source/runtime behavior.

## Depends on
- none

## Scope
- `llms.txt`
- source-of-truth cross-checks only: `package.json`, `src/index.ts`, `src/adapters/cli/index.ts`, `src/adapters/fastify/index.ts`, `src/adapters/filesystem/index.ts`, `src/adapters/postgres/index.ts`, `src/adapters/react/index.ts`, `src/adapters/in-memory/index.ts`
- checkpoint artifact written by implementation: `impl/checkpoints/01.md`

## Contract surfaces
- docs/public API guide: root `esther` exports
- docs/public API guide: subpath exports for `esther/cli`, `esther/filesystem`, `esther/postgres`, `esther/fastify`, `esther/test`, `esther/react`
- docs/runtime boundary wording: `dispatch(sliceName: string, input: unknown)` stays dynamic
- low-level adapter/helper exports: `EventRecordInput`, dispatch aliases, Postgres constraint helpers
- no source API, persistence, migration, auth, side-effect, or runtime behavior changes

## Intent / decision vocabulary
- `publicExportInventory`
- `canonicalOperationVocabulary`
- `adapterSurfacePrecision`
- `lowLevelInteropNotCanonicalDsl`
- `llmsCompactness`

## Plan-check watch items
- Cross-check full `src/index.ts` export names, not only research omission list.
- Include or intentionally scope additional command/descriptor helper types: `CastTagQueryDescriptor`, `CommandLookupDescriptor`, `CommandLookupByArgsDescriptor`, `CommandLookupByIdDescriptor`, `CommandDefinition`, `DeriveStep`, `GenerateStep`, `OutputErrHandlers`, `ProjectionStep`, `QueryProjectionStep`, `TagQueryStep`, and `ValidatePredicate`.
- Include or intentionally scope `InMemoryInputAdapter`; root and `esther/test` export it.
- Preserve `projectionAdapters` / `projectionQuery` only as deprecated compatibility prose, not primary config examples.
- Keep `slice` wording only where exact API compatibility requires it: `sliceName`, Fastify route `slice`, `SliceError`, and unknown-slice error wording.
- Keep low-level helpers (`EventRecordInput`, Postgres constraint helpers, dispatch aliases) documented as interop/adapter helpers, not preferred app authoring path.
- Record focused search results and full gate results in implementation checkpoint.

## Work
- Run source export cross-check first and capture public names from root and adapter index files.
- Update `llms.txt` package export/import section with compact grouped root export inventory.
- Update adapter subpath documentation so every public subpath in `package.json` is named and subpath export lists match source at useful LLM granularity.
- Document exact `esther/test` in-memory event/input exports and state `createInMemoryProjectionAdapter` comes from root `esther`, not `esther/test`.
- Document `DispatchFn` and `AppDispatchFn` without implying typed in-process app clients; keep runtime dispatch boundary `(sliceName: string, input: unknown)`.
- Label Postgres constraint helpers, `EventRecordInput`, and raw dispatch aliases as low-level interop/adapter helpers, not canonical app DSL.
- Replace stale conceptual “slice” prose with “operation” where not exact API compatibility wording.
- Preserve existing correct guidance: required `operations`, no `AppConfig.slices`, no `defineSlice`, `defineEvent`, `defineReducer`, DCB preconditions, `BoundaryObservationError`, `ConcurrencyError`, canonical `readModels`, Fastify route parse behavior.
- Keep `llms.txt` compact; prefer grouped bullets over tutorials or long examples for every type.
- Write `impl/checkpoints/01.md` before finishing, including focused search summaries, export cross-check summary, manual docs QA result, and full gate results.

## Implementation checkpoint
- write `impl/checkpoints/01.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` names all public package subpaths from `package.json`.
- Root export section includes or explicitly groups omitted public root exports from `src/index.ts`, including plan and plan-check watch-list symbols.
- Adapter subpath sections list CLI dispatch/request types, filesystem config/checkpoint types, Postgres config and constraint helpers, Fastify route type exports, React store/hook type exports, and exact `esther/test` in-memory event/input exports.
- `esther/test` docs state projection adapter is not exported from that subpath; use root `esther` export for `createInMemoryProjectionAdapter`.
- `DispatchFn` / `AppDispatchFn` alias situation is clear and dynamic dispatch remains `(sliceName: string, input: unknown)`.
- Conceptual prose uses “operation” except exact compatibility/API terms (`sliceName`, route `slice`, `SliceError`, unknown-slice error wording, or legacy/deprecated explanation).
- Existing correct DSL, error, DCB, read-model, and Fastify guidance remains intact.
- No source API or runtime behavior changes are made.
- Focused stale/current API searches, source export cross-check, manual docs QA, and full repo gates are recorded in `impl/checkpoints/01.md`.

## Suggested tests
- No failing test first: docs-only inventory update with no source behavior change. Use source export cross-check before editing, then focused searches after editing.
- Record command summaries from:
  ```bash
  rg -n "export (type |\{|function )|export type \{" src/index.ts src/adapters/cli/index.ts src/adapters/fastify/index.ts src/adapters/filesystem/index.ts src/adapters/postgres/index.ts src/adapters/react/index.ts src/adapters/in-memory/index.ts
  rg -n "AppConfig\.slices|defineSlice|createFastifyAdapter|projectionAdapters:|projectionQuery:" llms.txt
  rg -n "InputPipeline|StateResolver|ReadDescriptor|GetDescriptor|QueryDescriptor|EventsByTagsDescriptor|ReadModelEventBinding|ProjectionQueryAdapter|WhereEntry|EventFilter|OnAfterInsertHandler|OnAfterCommitHandler|ProjectionAdapterEntry|ProjectionAdapterTableEntry|ProjectionAdapterViewEntry|ProcessorEventBinding|AppDispatchFn|CliDispatchRequest|FilesystemEventStoreConfig|PostgresEventStoreConfig|isConstraintViolation|mapConstraintError|CastTagQueryDescriptor|CommandLookupDescriptor|CommandLookupByArgsDescriptor|CommandLookupByIdDescriptor|CommandDefinition|DeriveStep|GenerateStep|OutputErrHandlers|ProjectionStep|QueryProjectionStep|TagQueryStep|ValidatePredicate|InMemoryInputAdapter" llms.txt
  ```

## Manual verification
- Read `llms.txt` top-to-bottom after edits.
- Compare package/subpath sections against `package.json`, `src/index.ts`, and each adapter `index.ts`.
- Confirm omitted or low-level symbols are either listed as exported helpers/types or intentionally scoped as interop.
- Confirm no stale API examples: no `AppConfig.slices`, no `defineSlice`, no `createFastifyAdapter`, no command-level `projectors`/`processors`, no raw public `tagQuery({ schemas, fold })` form.
- Confirm `slice` wording remains only for exact compatibility terms: `sliceName`, route `slice`, `SliceError`, unknown-slice error wording, or legacy/deprecated explanation.
- Confirm guide remains compact and not a full tutorial.

## Verification
- ```bash
  rg -n "AppConfig\.slices|defineSlice|createFastifyAdapter|projectionAdapters:|projectionQuery:" llms.txt
  rg -n "InputPipeline|StateResolver|ReadDescriptor|GetDescriptor|QueryDescriptor|EventsByTagsDescriptor|ReadModelEventBinding|ProjectionQueryAdapter|WhereEntry|EventFilter|OnAfterInsertHandler|OnAfterCommitHandler|ProjectionAdapterEntry|ProjectionAdapterTableEntry|ProjectionAdapterViewEntry|ProcessorEventBinding|AppDispatchFn|CliDispatchRequest|FilesystemEventStoreConfig|PostgresEventStoreConfig|isConstraintViolation|mapConstraintError|CastTagQueryDescriptor|CommandLookupDescriptor|CommandLookupByArgsDescriptor|CommandLookupByIdDescriptor|CommandDefinition|DeriveStep|GenerateStep|OutputErrHandlers|ProjectionStep|QueryProjectionStep|TagQueryStep|ValidatePredicate|InMemoryInputAdapter" llms.txt
  rg -n "export (type |\{|function )|export type \{" src/index.ts src/adapters/cli/index.ts src/adapters/fastify/index.ts src/adapters/filesystem/index.ts src/adapters/postgres/index.ts src/adapters/react/index.ts src/adapters/in-memory/index.ts
  bun run typecheck
  bun run lint
  bun run test
  ```
