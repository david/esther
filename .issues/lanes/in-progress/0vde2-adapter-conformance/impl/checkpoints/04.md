# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-25

## Summary
- Audited the conformance-test diff against the six planned `EventStore.append(...)` append-precondition cases and plan-check watch items.
- Confirmed the shared helper remains test-only and is imported only by adapter `*.test.ts` files.
- Confirmed local duplicate append-precondition tests were removed only where covered by the shared suite, while adapter-specific storage, persistence, query, checkpoint, constraint, and advisory-lock coverage remains local.
- Ran the focused adapter test files and full repo verification gates successfully.

## Files changed
- `.issues/lanes/in-progress/0vde2-adapter-conformance/impl/checkpoints/04.md`: records the final audit and verification checkpoint for this task.

## Verification
- `rg "defineEventStoreAppendConformanceTests|event-store-append-conformance" -n src .issues/lanes/in-progress/0vde2-adapter-conformance`: pass — source imports are limited to `src/adapters/in-memory/event-store.test.ts`, `src/adapters/filesystem/index.test.ts`, and `src/adapters/postgres/event-store.test.ts`; the helper definition is under `src/__tests__/`.
- `rg "expectedPosition|boundaryTags|ConcurrencyError|append without preconditions|stale expectedPosition|empty global|global stream" -n src/adapters/in-memory/event-store.test.ts src/adapters/filesystem/index.test.ts src/adapters/postgres/event-store.test.ts src/__tests__/event-store-append-conformance.ts`: pass — planned append-precondition assertions are centralized in the shared conformance helper, with only postgres advisory-lock precondition ordering remaining local.
- `git diff --name-status -- . ':(exclude).issues/lanes/in-progress/0vde2-adapter-conformance/impl/checkpoints/04.md'`: pass — production files are unchanged; source changes are limited to adapter test files and the test-only helper, plus the expected issue lane move artifacts.
- `bun test src/adapters/in-memory/event-store.test.ts`: pass — 12 tests passed.
- `bun test src/adapters/filesystem/index.test.ts`: pass — 17 tests passed.
- `bun test src/adapters/postgres/event-store.test.ts`: pass — 16 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint completed and dependency-cruiser reported no dependency violations.
- `bun run test`: pass — 215 tests passed across 18 files.

## Drift assessment
- Aligned with `impl/04.md`: the final audit found the shared conformance suite present for in-memory, filesystem, and postgres; focused adapter tests and full gates all pass.
- Aligned with `plan/01-refactor-plan.md`: the implemented changes remain test-structure/coverage only, with no production event-store, adapter, persistence, SQL, processor, projector, or public API changes.
- Aligned with `plan/checks/01-plan-sanity.md` watch items: `src/__tests__/event-store-append-conformance.ts` is test-only; imports are from `*.test.ts` files only; the `undefined`/`[]` global-boundary case cross-checks both directions; failed appends are verified via `queryByTags(...)` with explicit schemas; adapter-specific persistence, query, checkpoint, constraint, handler, and advisory-lock coverage remains local.

## Follow-ups
- none
