# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-25

## Summary
- Wired the shared `EventStore.append(...)` append-precondition conformance suite into the filesystem event-store tests.
- Added a filesystem conformance factory that creates a fresh temporary store root for each conformance store instance under the per-test cleaned root.
- Removed filesystem local append-precondition tests that are exactly covered by the shared conformance suite while keeping filesystem-specific persistence, index, ordering, temp-file, and checkpoint tests local.

## Files changed
- `src/adapters/filesystem/index.test.ts`: imports and runs the shared conformance suite, allocates isolated conformance store roots, and removes duplicate local append-precondition tests.
- `.issues/lanes/in-progress/0vde2-adapter-conformance/impl/checkpoints/02.md`: records this checkpoint.

## Verification
- `bun test src/adapters/filesystem/index.test.ts`: pass — 17 tests passed, including the nested filesystem append-precondition conformance suite.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint completed and dependency-cruiser reported no dependency violations.

## Drift assessment
- Aligned with `impl/02.md`: filesystem now runs `defineEventStoreAppendConformanceTests(...)`, each conformance store instance uses an isolated `mkdtemp(...)` root below the test root, and only duplicate append-precondition tests were removed.
- Aligned with `plan/01-refactor-plan.md`: this remains test-only, with no filesystem production implementation, file layout, persistence, locking, query, or checkpoint behavior changes.
- Aligned with `plan/checks/01-plan-sanity.md` watch items: filesystem temporary roots are cleaned by the existing `afterEach` root removal, adapter-specific persistence/index/temp-file/checkpoint coverage stays local, and the shared helper remains imported only from `*.test.ts` files.

## Follow-ups
- Continue with task 03 to wire the shared conformance suite into the postgres adapter tests.
