# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-25

## Summary
- Added the shared, test-only `EventStore.append(...)` append-precondition conformance suite.
- Wired the suite into the in-memory event-store tests.
- Removed in-memory local append-precondition tests that are exactly replaced by the shared suite.

## Files changed
- `src/__tests__/event-store-append-conformance.ts`: adds the reusable append-precondition conformance fixture with explicit event schemas and failed-append visibility checks.
- `src/adapters/in-memory/event-store.test.ts`: imports and runs the shared suite for `createInMemoryEventStore()` and removes duplicate local precondition tests.
- `.issues/lanes/in-progress/0vde2-adapter-conformance/`: moved the issue from backlog to in-progress before implementation.
- `.issues/lanes/in-progress/0vde2-adapter-conformance/impl/checkpoints/01.md`: records this checkpoint.

## Verification
- `bun test src/adapters/in-memory/event-store.test.ts`: pass — 12 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 211 tests passed.

## Drift assessment
- Aligned with `impl/01.md`: the helper exists under `src/__tests__/`, exposes `defineEventStoreAppendConformanceTests(adapterName, createStore)`, supports synchronous or async store factories, and in-memory now runs it.
- Aligned with `plan/01-refactor-plan.md`: no production code, public types, adapter implementations, persistence behavior, SQL, filesystem, or app wiring changed.
- Aligned with `plan/checks/01-plan-sanity.md` watch items: the helper is test-only, imported only by a `*.test.ts` file, asserts `_tag`, `message`, `expectedPosition`, `actualPosition`, and `boundaryTags`, cross-checks `undefined` and `[]` global boundaries in both directions, and uses `queryByTags(...)` with an explicit schema to verify failed appends do not persist.

## Follow-ups
- Continue with task 02 to wire the shared conformance suite into the filesystem adapter tests.
