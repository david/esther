# 03 — Run append conformance for postgres stores

## Source
- plan/01-refactor-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Wire the shared append-precondition conformance suite into the postgres event-store tests using a fresh mock SQL harness per store, while keeping postgres-specific constraint and advisory-lock tests local.

## Depends on
- 02.md

## Scope
- `src/adapters/postgres/event-store.test.ts`
- `src/__tests__/event-store-append-conformance.ts` only if a narrow helper adjustment is required for postgres conformance

## Contract surfaces
- Postgres adapter `EventStore.append(...)` conformance at the adapter boundary
- Postgres mock SQL harness behavior needed by append and any conformance `queryByTags(...)` assertions
- Postgres advisory append-lock ordering remains adapter-specific and local
- No SQL schema, production query ordering, locking, constraint mapping, or adapter implementation behavior changes

## Plan-check watch items
- Ensure the postgres conformance factory creates a fresh mock SQL harness per store and extends the harness narrowly if the shared fixture uses a query shape the harness does not yet support.
- Assert the protected `ConcurrencyError` fields, including `_tag`, `message`, `expectedPosition`, `actualPosition`, and `boundaryTags`, without overfitting IDs/timestamps.
- When removing local duplicate tests, remove only cases exactly replaced by the shared suite; keep persistence, locking, handler ordering, query, constraint, and adapter-specific tests local.
- Keep `src/__tests__/event-store-append-conformance.ts` test-only and imported only by `*.test.ts` files.

## Work
- Wire `defineEventStoreAppendConformanceTests(...)` into `src/adapters/postgres/event-store.test.ts`.
- Create each conformance store from a fresh `createEventStoreHarness()` instance.
- If the shared suite queries event visibility after failed appends, extend the local mock SQL harness narrowly to support the existing `queryByTags(...)` `SELECT id, type, tags, payload, position, timestamp ... ORDER BY position ASC` shape.
- Preserve postgres-specific tests for constraint mapping and advisory-lock ordering.
- Remove only postgres append-precondition tests exactly replaced by the shared suite; keep the advisory-lock test local even though it appends with preconditions.
- Run the focused postgres adapter test and fix only issues within this slice.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against `plan/01-refactor-plan.md` and `plan/checks/01-plan-sanity.md`
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- The postgres adapter runs the same shared append-precondition conformance suite as in-memory and filesystem.
- Each postgres conformance store uses fresh mock harness state.
- Postgres-specific constraint and advisory-lock tests remain local.
- No postgres production implementation, SQL schema, or lock ordering changes.

## Suggested tests
- Reuse the shared conformance suite added in 01.md.
- Keep the existing advisory-lock ordering test as a local postgres test.

## Verification
- `bun test src/adapters/postgres/event-store.test.ts`
