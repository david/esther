# 02 — Run append conformance for filesystem stores

## Source
- plan/01-refactor-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Wire the shared append-precondition conformance suite into the filesystem event-store tests while preserving filesystem-specific persistence, index, temp-file, locking, and checkpoint coverage.

## Depends on
- 01.md

## Scope
- `src/adapters/filesystem/index.test.ts`
- `src/__tests__/event-store-append-conformance.ts` only if the factory contract needs a narrow test-helper adjustment discovered while wiring filesystem

## Contract surfaces
- Filesystem adapter `EventStore.append(...)` conformance at the adapter boundary
- Filesystem test isolation / temp-root cleanup
- No persistence layout, canonical event file, index, checkpoint, or query behavior changes

## Plan-check watch items
- Ensure the filesystem conformance factory either allocates roots under an existing cleaned test root or otherwise cleans temporary directories; do not leak shared state between conformance cases.
- When removing local duplicate tests, remove only cases exactly replaced by the shared suite; keep persistence, locking, handler ordering, query, constraint, and adapter-specific tests local.
- Keep `src/__tests__/event-store-append-conformance.ts` test-only and imported only by `*.test.ts` files.

## Work
- Wire `defineEventStoreAppendConformanceTests(...)` into `src/adapters/filesystem/index.test.ts`.
- Provide a filesystem store factory that creates an isolated fresh root per conformance store instance, preferably under the existing per-test cleaned root or an equivalently cleaned temporary parent.
- Verify the shared suite can create multiple fresh filesystem stores within one conformance run without cross-test state leakage.
- Remove only filesystem append-precondition tests exactly replaced by the shared suite.
- Preserve filesystem-specific tests for canonical files, global positions, query ordering, index rebuild/repair, temp-file behavior, and checkpoints.
- Run the focused filesystem adapter test and fix only issues within this slice.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against `plan/01-refactor-plan.md` and `plan/checks/01-plan-sanity.md`
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- The filesystem adapter runs the same shared append-precondition conformance suite as in-memory.
- Every conformance store instance gets isolated filesystem state.
- Filesystem-specific persistence, index, temp-file, ordering, and checkpoint tests remain local.
- No filesystem production implementation, file layout, or persistence behavior changes.

## Suggested tests
- Reuse the shared conformance suite added in 01.md.
- Keep focused filesystem assertions near filesystem-specific behavior rather than moving them into the shared helper.

## Verification
- `bun test src/adapters/filesystem/index.test.ts`
