# 05 — Implementation checkpoint

Status: aligned
Task: impl/05.md
Date: 2026-04-26

## Summary
- Added compile-only public reducer API contract coverage for reducer-backed `tagQuery`, query `state().pipe(tagQuery(...))`, `castTagQuery`, `eventsByTagsDescriptor`, and `EventStore.queryByTags` inference.
- Added compile-only negative assertions rejecting fake plain-object reducers and old raw event-history forms across public query surfaces.
- Cleaned stale source/test wording that matched raw `schemas + fold` audit patterns while still describing reducer-only contracts.

## Files changed
- `src/__tests__/type-check.ts`: expanded reducer contract inference and negative API tests.
- `src/adapters/in-memory/event-store.test.ts`: updated test name to reducer-only wording.
- `src/adapters/postgres/event-store.test.ts`: updated test name to reducer-only wording.
- `src/adapters/filesystem/index.test.ts`: updated test name to reducer-only wording.
- `.issues/lanes/in-progress/heqik-define-reducer/impl/checkpoints/05.md`: implementation checkpoint for this task.

## Verification
- `rg "schemas.*fold|fold.*schemas|queryByTags\\([^\\n]*schemas|eventsByTagsDescriptor\\([^\\n]*schemas" src`: pass — no matches; `rg` exited 1 because search found nothing.
- `bun run test`: pass — 243 tests passed, 603 expectations.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.

## Manual verification
- not-applicable — final QA contract for this library API is automated compile-only, runtime, lint, and dependency-boundary verification.

## Drift assessment
- Aligned with `impl/05.md`: compile-only coverage now proves reducer state inference for command, query, cast, read descriptor, and event store surfaces, and rejects raw public event-history forms plus fake reducers.
- Aligned with `plan/01-implementation-plan.md`: reducer brand remains private; no public raw `schemas + fold` event-history API is reintroduced; comments/test labels now describe reducer-only contracts.
- Aligned with `plan/checks/01-plan-sanity.md`: type contract checks cover old raw `tagQuery`, `castTagQuery`, and `eventsByTagsDescriptor` failure cases; no `object`, explicit `any`, or `Record<string, unknown>` was added.

## Follow-ups
- none
