# 05 — Harden type contract and run full gates

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal
Finish the strict public API contract with compile-only negative tests, stale raw-form audit, comments/docs cleanup, and whole-repo gates.

## Depends on
- 04.md

## Scope
- `src/__tests__/type-check.ts`
- `src/core/slice.ts`
- `src/core/read-model.ts`
- `src/core/read-interpreter.ts`
- `src/core/event-store.ts`
- `src/index.ts`
- any tests/comments still referencing raw public `schemas + fold` event-history forms

## Contract surfaces
- public TypeScript API for `defineReducer`
- public TypeScript API for `tagQuery`, `castTagQuery`, `eventsByTagsDescriptor`, and `EventStore.queryByTags`
- package exports and public comments

## Plan-check watch items
- Keep reducer brand symbol private and export only factory/types.
- Ensure type-check negative cases prove old raw `tagQuery`, `castTagQuery`, and `eventsByTagsDescriptor` forms fail.
- Convert every public raw event-history read surface together; partial conversion should fail typecheck.
- Do not add `object`, explicit `any`, or `Record<string, unknown>` while repairing mapped-key/type accumulation issues.

## Work
- Add or finalize compile-only tests proving `defineReducer` infers event unions from const Zod schema tuples.
- Add or finalize compile-only tests proving reducer `event.type` narrowing narrows payload shape.
- Add or finalize compile-only tests proving reducer-backed `tagQuery` accumulates command input context state.
- Add or finalize compile-only tests proving `state().pipe(tagQuery({ reducer }))` accumulates query context state.
- Add or finalize compile-only tests proving `castTagQuery({ reducer })` accumulates reducer state and `${key}Subject`.
- Add or finalize compile-only tests proving `eventsByTagsDescriptor(tags, reducer)` returns reducer state type.
- Add negative compile-only tests proving fake plain-object reducers and old raw `tagQuery`, `castTagQuery`, and `eventsByTagsDescriptor` forms fail.
- Audit production comments/docs in touched files so they describe reducer-only contracts, not raw `schemas + fold` public APIs.
- Search for stale public raw-form usage and remove or update every intended occurrence.
- Run full repo gates and fix any failures without broadening scope.

## Implementation checkpoint
- write `impl/checkpoints/05.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Compile-only coverage rejects public raw forms and fake reducers.
- Compile-only coverage proves reducer-backed state inference across command, query, cast, and read descriptor surfaces.
- No production public event-history query API still accepts raw `schemas + fold` forms.
- Touched comments/docs no longer advertise raw public event-history forms.
- `bun run test`, `bun run typecheck`, and `bun run lint` pass for the whole repo.

## Suggested tests
- `src/__tests__/type-check.ts` is the main test file for this task.
- Use `@ts-expect-error` only for intentional negative contract assertions.

## Manual verification
- not applicable — final QA contract for this library API is automated compile-only, runtime, lint, and dependency-boundary verification.

## Verification
- `rg "schemas.*fold|fold.*schemas|queryByTags\([^\n]*schemas|eventsByTagsDescriptor\([^\n]*schemas" src`
- `bun run test`
- `bun run typecheck`
- `bun run lint`
