# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-28

## Summary
- Added schema-aware runtime `where` validation for `queryDescriptor(...)`.
- Unknown fields, object/array fields, boolean ranges, wrong primitive kinds, and non-primitive `in` values now throw synchronously.
- Preserved valid primitive `WhereEntry` output and top-level `undefined` field skipping.

## Files changed
- `src/core/read-model.ts`: added schema-aware normalization path for `queryDescriptor(...)` while keeping existing read-model query normalization for later task.
- `src/core/read-model.test.ts`: added focused runtime tests for valid entries and unsafe bypass failures.
- `.issues/lanes/in-progress/bs43i-tighten-query-where/impl/checkpoints/02.md`: recorded implementation checkpoint.

## Verification
- `bun test src/core/read-model.test.ts`: pass — 44 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.

## Drift assessment
- Matches `impl/02.md`: `queryDescriptor(...)` now validates against `ReadModelHandle.schema.shape[field]` before emitting entries.
- Error messages include read model name, field, and reason substrings.
- No adapter changes, `WhereEntry` shape changes, or `defineReadModelQuery(...)` behavior changes included; query reuse remains for `impl/03.md`.
- No scope expansion into orderBy, JSONB operators, nested paths, joins, or adapter SQL semantics.

## Follow-ups
- Continue with `impl/03.md` to reuse schema-aware validation in named read-model queries.
