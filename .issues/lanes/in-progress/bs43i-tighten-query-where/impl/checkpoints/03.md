# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-28

## Summary
- Reused schema-aware `where` normalization in `defineReadModelQuery(...).buildQuery(args)`.
- Added query/source context to runtime `where` errors.
- Preserved valid query output shape, `orderBy`, `orderDirection`, and `limit` behavior.

## Files changed
- `src/core/read-model.ts`: shared schema-aware normalization between `queryDescriptor(...)` and read-model query build path, with query-specific error context.
- `src/core/read-model.test.ts`: added runtime tests for read-model query valid behavior and unsafe bypass failures.
- `.issues/lanes/in-progress/bs43i-tighten-query-where/impl/checkpoints/03.md`: recorded implementation checkpoint.

## Verification
- `bun test src/core/read-model.test.ts`: pass — 48 tests passed.
- `bun test src/core/read-interpreter.test.ts`: pass — 12 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.

## Drift assessment
- Matches `impl/03.md`: `buildQuery(args)` now fails fast using same schema-aware rules as `queryDescriptor(...)`.
- Runtime errors include query name, source read model, field, and reason.
- Adapter API and `WhereEntry` shape unchanged.
- No scope expansion into orderBy typing, JSONB operators, nested paths, joins, or adapter SQL semantics.

## Follow-ups
- Continue with `impl/04.md` final gates and drift check.
