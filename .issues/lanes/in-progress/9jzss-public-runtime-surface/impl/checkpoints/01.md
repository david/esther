# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-26

## Summary
- Narrowed the root package export surface by removing named root exports for planned runtime internals.
- Updated the public API type-check sentinel to stop importing `SliceDeps`, preserve `BoundaryObservation` / `BoundaryObservationError` coverage, and assert representative removed root exports are unavailable.
- Kept deprecated read-model compatibility exports and current watch-item helper/descriptor types root-public where they support existing public inference.

## Files changed
- `src/index.ts`: removed root exports for executor, read-interpreter, compile/dependency, projection-store, low-level compose, and inline-result internals.
- `src/__tests__/type-check.ts`: removed `SliceDeps` root API usage and added negative root API checks for removed internals.
- `.issues/lanes/in-progress/9jzss-public-runtime-surface/impl/checkpoints/01.md`: recorded implementation result and verification.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `rg -n "executeCommand|executeQuery|createReadInterpreter|ReadInterpreterDeps|ProjectionStore|SliceDeps|CompileDeps|CompiledOperation|StepError|InlineResult" src/index.ts || true`: pass — no removed internals remain in `src/index.ts`.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 236 tests passed, 0 failed.

## Manual verification
- compile-time TypeScript package boundary: not-applicable — no interactive runtime workflow; covered by typecheck, negative root API assertions, and source/export inspection.

## Drift assessment
- Matches `impl/01.md` and `plan/01-implementation-plan.md`: only `src/index.ts` and the public type-check sentinel changed; no internal runtime modules, execution behavior, event append flow, read-model query behavior, processor behavior, adapters, persistence, migrations, replay, auth, or side effects changed.
- Plan-check watch items satisfied: `BoundaryObservation` and `BoundaryObservationError` remain root-public; deprecated `ProjectionAdapterEntry`, `ProjectionAdapterTableEntry`, `ProjectionAdapterViewEntry`, `projectionAdapters`, and `projectionQuery` compatibility remains available through `AppConfig`/root types.
- Retained watch-item exports: `InputPipeline`, `StateResolver`, descriptor concrete types, `Command`, `Query`, `CommandDefinition`, `OutputErrHandlers`, and `ValidatePredicate` remain root-public because the approved plan allowed keeping them when useful for current public inference and this task did not redesign descriptor opacity.
- Negative checks use representative removed exports (`executeCommand`, `ProjectionStore`, `SliceDeps`) without brittle runtime behavior assumptions.

## Follow-ups
- none for this task; broader opaque descriptor/operation type redesign remains only a plan-listed future candidate.
