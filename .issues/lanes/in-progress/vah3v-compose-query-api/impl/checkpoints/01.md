# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-28

## Summary
- Documented intentional current split between command `compose().add(...)` and query `state().pipe(...)` APIs.
- Added command-only, query-only, and shared descriptor categories.
- Added `llms.txt` guidance keeping command examples on `compose().add(...)` and query examples on `state().pipe(...)`.
- Moved issue from backlog to in-progress before implementation.

## Files changed
- `doc/domain-language.md`: adds rationale and descriptor category guidance for command input pipeline vs query state resolver.
- `llms.txt`: adds high-signal LLM guidance for why API names differ and keeps examples aligned.
- `.issues/lanes/in-progress/vah3v-compose-query-api/index.md`: updates issue status and next handoff after task completion.
- `.issues/lanes/in-progress/vah3v-compose-query-api/impl/checkpoints/01.md`: records implementation evidence.
- `.issues/lanes/in-progress/vah3v-compose-query-api/**`: issue lane move from backlog to in-progress.

## Verification
- `rg "compose\(\)\.add|state\(\)\.pipe|why.*API|Command input pipeline" doc llms.txt src`: pass — new rationale and existing examples found.
- `rg "compose\(\)\.pipe|state\(\)\.add|generic shared public builder" doc llms.txt src -n || true`: pass — no forbidden alias wording introduced in public docs/guidance/source.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 259 tests passed, 0 failed.

## Manual verification
- Read `doc/domain-language.md` command/query sections: pass — reader can answer commands use `compose().add(...)` because command input pipelines prepare appendable command context and command-side event-history reads can derive DCB append preconditions.
- Read `llms.txt` command/query examples: pass — reader can answer queries use `state().pipe(...)` because query state resolvers prepare read-only response context.
- Shared helper check: pass — docs state `tagQuery(...)` and `generate(...)` are shared helpers with phase-specific semantics, not shared operation semantics.
- Regression check: pass — public docs/guidance/source do not introduce forbidden alias wording.

## Drift assessment
- Aligned with `impl/01.md`: docs-only change; no runtime/API files changed; no aliases or public signature changes added.
- Aligned with `plan/01-implementation-plan.md`: preserves separate public DSLs and clarifies current rationale without overstating future convergence.
- Aligned with `plan/checks/01-plan-sanity.md`: `doc/domain-language.md` explicitly says current split is intentional, and `llms.txt` stays aligned with examples.

## Follow-ups
- none
