# Deploy — PR opened 2026-04-28

## Verdict
- shipped

## Preconditions checked
- Issue lane: `.issues/lanes/in-progress/kf0q3-privatize-domain-event`.
- Implementation tasks complete: `impl/01.md`, `impl/02.md`, `impl/03.md` all have checkpoints.
- Review: `review/diff/01-review-diff.md` reports no actionable findings.
- Gates: `review/findings/01-gate-results.md` passed.
- QA: `qa/summary.md` passed; no manual QA remaining.
- Worktree was clean before deploy artifact write.
- Local `main` had two unrelated ahead commits before this issue; PR branch was created from `origin/main` and only `kf0q3` commits were cherry-picked to avoid scope mixing.

## Commands run
- `git fetch origin`
- `git switch -c kf0q3-privatize-domain-event origin/main`
- `git cherry-pick d0e22ad^..625b491`
- `git status --short --branch`
- `git diff --name-only origin/main...HEAD`
- `bun run typecheck && bun run lint && bun run test`
- `git push -u origin kf0q3-privatize-domain-event`
- `gh pr create --fill`
- `gh pr view 9 --json url,title,state,headRefName,baseRefName,isDraft,mergeable,reviewDecision,statusCheckRollup`

## PR / deploy links
- PR: https://github.com/david/esther/pull/9
- State: open
- Base: `main`
- Head: `kf0q3-privatize-domain-event`
- Draft: false
- Mergeable: `MERGEABLE`
- CI at artifact time: queued (`check` workflow)
- Release/deploy: not applicable; library repo has no documented staging deploy flow.

## QA and gate evidence
- `bun run typecheck`: passed on PR branch.
- `bun run lint`: passed on PR branch; ESLint completed with `--max-warnings=0`; dependency-cruiser reported no dependency violations across 57 modules and 173 dependencies.
- `bun run test`: passed on PR branch; 259 pass, 0 fail, 639 expect() calls, 21 files.
- Prior QA summary also passed with same content before clean PR branch recreation.

## Migration / rollout notes
- Breaking TypeScript API cleanup: root `DomainEvent` export removed.
- Replacement guidance: app authors use `defineEvent(...)` + `EventOf<typeof Definition>`; custom store authors use low-level `EventRecordInput`.
- Runtime persisted event shape unchanged.
- No migrations, backfills, deploy ordering, or replay work.

## Lane move
- from: `.issues/lanes/in-progress/kf0q3-privatize-domain-event`
- to: `.issues/lanes/done/kf0q3-privatize-domain-event`
- status: not moved; PR is open and not merged to `main` yet.

## Project-board review handoff
- status: not applicable
- evidence or exact UI action needed: repo docs define no external project-board workflow.

## External issue closure
- status: left open; no external issue linked in `description.md`, and PR review/merge is still pending.

## Next step
- Review and merge PR: https://github.com/david/esther/pull/9
- After merge: {{/skill:deploy kf0q3-privatize-domain-event --move-done}}
