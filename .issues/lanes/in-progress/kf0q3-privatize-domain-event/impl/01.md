# 01 — Introduce low-level EventRecordInput boundary

## Source
- plan/02-implementation-plan.md

## Goal
Add `EventRecordInput` as nameable low-level store/adapter append shape and retarget store-facing contracts to it, without removing root `DomainEvent` yet. Keep runtime event wire shape unchanged.

## Depends on
- none

## Scope
- `src/core/types.ts`
- `src/core/event.ts`
- `src/core/event-store.ts`
- `src/core/slice.ts`
- `src/core/pipeline.ts`
- built-in event stores and store tests under `src/adapters/**`
- store conformance helper `src/__tests__/event-store-append-conformance.ts`
- focused `EventRecordInput` checks in `src/__tests__/type-check.ts`

## Contract surfaces
- root/core type surface adds `EventRecordInput<TType extends string = string, TPayload = unknown>` as low-level event-store input
- `EventStore.append(events, options)` changes to `ReadonlyArray<EventRecordInput>`
- `StoredEvent` preserves exact `type`, `tags`, `payload`, `id`, `position`, `timestamp` fields
- store/adapter conformance helpers use `EventRecordInput` where they mean append input
- no persisted event, reducer, projector, processor, migration, replay, auth, or side-effect behavior changes

## Plan-check watch items
- Ensure `EventStore.append` public type and custom-store example compile with root `EventRecordInput`.
- Preserve readonly structural fields for `EventOf` and `defineEvent.create`.
- Preserve stored event fields exactly: `type`, `tags`, `payload`, `id`, `position`, `timestamp`.
- Run full gates: `bun run typecheck`, `bun run lint`, `bun run test`.

## Work
- Add/update type-level coverage first: prove root `EventRecordInput` is nameable and compatible with custom `EventStore.append` usage.
- Rename the low-level append shape in `src/core/types.ts` to `EventRecordInput` and keep any temporary `DomainEvent` compatibility only as needed for later task migration.
- Export `EventRecordInput` from `src/index.ts` while leaving root `DomainEvent` removal to `03.md`.
- Retarget `EventOf`, `defineEvent.create`, `EventStore.append`, `defineCommand` bounds, and pipeline bounds to `EventRecordInput` where they mean structural event records.
- Update built-in stores, filesystem validation helpers, adapter tests, and conformance helpers to use `EventRecordInput` for append/store shape.
- Preserve runtime serialization and persisted stored event fields exactly.
- Avoid app authoring guidance/doc cleanup here unless needed to keep tests compiling; final public guidance cleanup belongs to `03.md`.

## Implementation checkpoint
- write `impl/checkpoints/01.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `EventRecordInput<TType extends string = string, TPayload = unknown>` exists with readonly `type`, `tags`, and `payload` fields.
- Root `EventRecordInput` import compiles for low-level custom-store usage.
- `EventStore.append` uses `ReadonlyArray<EventRecordInput>`.
- Built-in stores and adapter/store conformance tests use `EventRecordInput` for structural append input.
- `StoredEvent` fields remain exactly `type`, `tags`, `payload`, `id`, `position`, `timestamp`.
- No event JSON/row/checkpoint/replay behavior changes.

## Suggested tests
- Update `src/__tests__/type-check.ts` before implementation to prove root `EventRecordInput` is usable with `EventStore.append`.
- Keep or add type-level field checks for `StoredEvent` and `EventOf` structural compatibility.
- Run store-focused tests while iterating if useful: `bun test src/adapters/in-memory/event-store.test.ts src/adapters/filesystem/index.test.ts src/adapters/postgres/event-store.test.ts`.

## Manual verification
- not applicable — library API/type boundary and store tests only; no UI/API workflow to manually exercise.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
