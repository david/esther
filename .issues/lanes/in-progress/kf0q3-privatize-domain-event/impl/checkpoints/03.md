# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-28

## Summary
- Removed `DomainEvent` from root exports and removed remaining source-level `DomainEvent` type alias/use.
- Added type-level coverage proving root `DomainEvent` is unavailable while root `EventRecordInput` remains usable.
- Updated `llms.txt` public export and event guidance to use `defineEvent(...)`/`EventOf`, with `EventRecordInput` documented only for low-level store/adapter interop.

## Files changed
- `src/index.ts`: removed root `DomainEvent` type export; kept `EventRecordInput`.
- `src/core/types.ts`: removed `DomainEvent` compatibility alias; kept `EventRecordInput` and `StoredEvent` shape.
- `src/core/slice.test.ts`: replaced internal test `DomainEvent` use with `EventRecordInput`.
- `src/__tests__/type-check.ts`: added `@ts-expect-error` assertion for removed root `DomainEvent`.
- `llms.txt`: removed `DomainEvent` from public imports/guidance and scoped `EventRecordInput` to low-level interop.

## Verification
- `bun run typecheck`: pass — tsgo completed with no errors.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 259 tests passed, 0 failed.
- `rg "DomainEvent|EventRecordInput" src llms.txt`: pass — `DomainEvent` remains only in the intentional root-removal type assertion; `EventRecordInput` remains low-level/type-test/docs usage.

## Drift assessment
- aligned with `impl/03.md` and `plan/02-implementation-plan.md`.
- Public app-facing event guidance now uses `defineEvent(...)` and `EventOf<typeof Definition>`.
- `EventRecordInput` remains root-exported for custom `EventStore`/adapter append input only.
- Runtime event wire shape and stored event fields unchanged.

## Follow-ups
- none.
