# 02 — Convert app-like event authoring tests

## Source
- plan/02-implementation-plan.md

## Goal
Move app-like tests and type examples from raw `DomainEvent<...>` authoring to schema-owned `defineEvent(...)` plus `EventOf<typeof EventDefinition>`, while root `DomainEvent` still exists temporarily.

## Depends on
- 01.md

## Scope
- `src/__tests__/type-check.ts`
- `src/__tests__/pipeline.test.ts`
- `src/__tests__/pipeline-wiring.test.ts`
- nearby fixtures/helpers in those files only when needed

## Contract surfaces
- app-facing event authoring examples use `defineEvent(...)` and `EventOf<typeof EventDefinition>`
- command event callback types derive from event definitions instead of raw structural `DomainEvent`
- empty payload examples use `Record<never, never>` when intentionally empty
- no store/adapter append contract changes beyond consuming `EventRecordInput` from `01.md`

## Plan-check watch items
- Keep `EventRecordInput` docs/examples out of app event-authoring sections; use it only for custom store/adapter interop.
- Preserve readonly structural fields for `EventOf` and `defineEvent.create`.
- Run full gates: `bun run typecheck`, `bun run lint`, `bun run test`.

## Work
- Update focused type-level app event assertions first in `src/__tests__/type-check.ts`: replace app-like `DomainEvent<...>` aliases with `EventOf<typeof ...>` derived from `defineEvent(...)`.
- Convert `pipeline.test.ts` command/reducer event type aliases from raw `DomainEvent<...>` to event definitions plus `EventOf`.
- Convert `pipeline-wiring.test.ts` probe/app event types from raw `DomainEvent<...>` to event definitions plus `EventOf`.
- Keep low-level store/conformance uses on `EventRecordInput`; do not use `EventRecordInput` for app event-authoring examples.
- Preserve test behavior and event names/payloads exactly.
- Use `Record<never, never>` for intentionally empty payloads; do not introduce `Record<string, unknown>` or bare `object`.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- App-like command examples/tests no longer rely on raw `DomainEvent<...>` for authored events.
- `EventOf<typeof Definition>` remains the app-facing event type in affected tests.
- Event names, tags, payload fields, reducer behavior, command outputs, and pipeline runtime behavior are unchanged.
- Low-level `EventRecordInput` usage remains limited to store/adapter interop surfaces.

## Suggested tests
- Update/keep `src/__tests__/type-check.ts` assertions for `EventOf` inference before broad test conversion.
- Run focused tests while iterating if useful: `bun test src/__tests__/pipeline.test.ts src/__tests__/pipeline-wiring.test.ts`.

## Manual verification
- not applicable — compile-time and runtime library tests only; no UI/API workflow to manually exercise.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
