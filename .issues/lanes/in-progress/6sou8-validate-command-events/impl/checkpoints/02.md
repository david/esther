# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-29

## Summary
- Added command event schema validation after `slice.event(ctx)` and before append.
- Definition-backed command validation failures now return `SchemaError("Event validation failed", issues)` directly, bypassing `outputErr` and append.
- Appends and `output(event, ctx)` now use parsed event data from the event schema when validation is present.
- Added runtime coverage for malformed definition-backed events, valid definition-backed events, and raw unvalidated event interop.

## Files changed
- `src/core/pipeline.ts`: validates optional `slice.eventSchema` before append and uses parsed event for append/output.
- `src/core/slice.ts`: types optional `eventSchema` as `z.ZodType<TEvent>` for typed pipeline parsing.
- `src/__tests__/pipeline-wiring.test.ts`: covers pre-append `SchemaError`, no append/downstream work, valid append/output, and raw unvalidated path.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/02.md`: records task checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 24 tests, 106 assertions.
- `bun run typecheck`: pass.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.

## Drift assessment
- Matches task ordering: input parse → input pipeline → boundary check → command validation → event build → event schema validation → append → output.
- Parsed-vs-original decision: parsed event is appended and passed to output, preserving Zod transform behavior if event schemas use transforms later.
- Validation failure stays framework-error path and does not call `outputErr`.
- Raw command form remains unvalidated; test proves malformed raw payload can append when caller chooses raw interop.
- No event-store or adapter persistence contract changed.

## Follow-ups
- Continue with task 03 to update public guidance and run full gates.
