# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-29

## Summary
- Updated `llms.txt` to prefer event-definition-backed command emission with `event`, `tags(ctx)`, and `payload(ctx)`.
- Documented runtime `SchemaError` before append for malformed definition-backed events.
- Documented raw `event(ctx) => EventRecordInput` as deliberate low-level interop that remains unvalidated by event definitions.
- Updated command terminology in `doc/domain-language.md`.

## Files changed
- `llms.txt`: refreshed event and command guidance, command execution order, rules, and canonical examples.
- `doc/domain-language.md`: updated command event-emission description and framework-error bypass note.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/03.md`: records task checkpoint.

## Verification
- `bun run typecheck`: pass.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.
- `bun run test`: pass — 275 tests, 672 assertions.

## Drift assessment
- Matches task scope: docs updated where command/event examples were stale.
- `README.md` inspected and left unchanged because it contains no command/event emission example and already points readers to `llms.txt`.
- No docs claim `defineEvent.create(...)` validates payloads; `llms.txt` now states it does not parse/validate by itself.
- Public helper exports unchanged; new API uses existing `defineEvent`, `EventOf`, and `EventPayloadOf` guidance.

## Follow-ups
- none
