# 07 — Implementation checkpoint

Status: aligned
Task: impl/07.md
Date: 2026-04-29

## Summary
- Hardened `defineCommand` runtime event-path discrimination so raw command definitions are identified by `event` being a function, not by absence of sibling `tags`/`payload` fields.
- Added regression coverage for a reusable raw command definition variable with extra helper fields named `tags` and `payload`; direct `command.event(ctx)` and dispatch append both preserve raw event type, tags, and payload.
- Added focused definition-backed coverage proving candidates still use `EventDefinition.type`, copied `tags(ctx)`, `payload(ctx)`, and retain `eventSchema` metadata.
- Left `llms.txt` unchanged because current guidance already states raw `event(ctx) => EventRecordInput` remains low-level interop and definition-backed commands use `event: EventDefinition`, `tags(ctx)`, and `payload(ctx)`.

## Files changed
- `src/core/slice.ts`: changes runtime command event discriminator from helper-field presence to raw event function detection; keeps definition-backed candidate construction and schema metadata.
- `src/core/slice.test.ts`: adds raw helper-field collision regression and definition-backed candidate metadata coverage.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/07.md`: records implementation, verification, and drift assessment.

## Verification
- `bun test src/core/slice.test.ts`: pass — 16 tests, 43 assertions.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json`.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.
- `bun run test`: pass — 279 tests, 690 assertions.
- `rg -n "RawInteropEvent|raw command|definition-backed|eventSchema|CommandEventCandidate|payload\(ctx\)|event\(ctx\)" llms.txt doc/domain-language.md README.md src/core/slice.ts src/core/slice.test.ts`: pass — docs already cover raw and definition-backed command event semantics; no `llms.txt` update needed.

## Manual verification
- not applicable — library command DSL contract with no product UI/API workflow; automated runtime, typecheck, lint, and full test gates are the right verification.

## Drift assessment
- Matches `impl/07.md` and `review/findings/02-raw-command-discriminator.md`: raw `RawInteropEvent` path remains selected when `definition.event` is a function even if helper fields named `tags` and `payload` exist.
- `DefinitionBackedCommand` behavior remains aligned: candidates are built from `EventDefinition.type`, copied tags, and payload input, with `eventSchema` present for dispatch validation.
- Plan-check watch items satisfied: new cast is local to overload normalization with an inline reason; object-schema and transform-schema definition-backed tests pass through full gates.
- No persistence shape, replay semantics, adapters, public API signatures, or side-effect ordering changed beyond preventing raw-path misclassification.

## Follow-ups
- none
