# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-29

## Summary
- Split command event typing into pre-parse candidate and parsed/stored event shapes.
- Changed definition-backed command `payload(ctx)` typing to event payload schema input.
- Changed definition-backed candidate construction to build `{ type, tags: [...], payload }` directly instead of `EventDefinition.create(...)`.
- Added transform-schema type coverage proving candidate input payload, parsed output payload, and raw interop compatibility.
- Updated public docs/LLM guidance for candidate-vs-parsed command event semantics.

## Files changed
- `src/core/slice.ts`: adds candidate event generic, schema-input payload helper, definition-backed overload return typing, and direct candidate construction.
- `src/__tests__/type-check.ts`: adds transform payload schema type assertions for `payload(ctx)`, direct `command.event(ctx)`, `output(event, ctx)`, and raw command compatibility.
- `llms.txt`: keeps public command DSL guidance current for schema-input candidates and parsed appended events.
- `doc/domain-language.md`: clarifies command event emission and output callback event shape.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/04.md`: records task checkpoint.

## Verification
- `bun run typecheck`: pass — transform command type assertions compile and expected errors are consumed.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.
- `bun run test`: pass — 275 tests, 672 assertions.

## Drift assessment
- Matches task goal: definition-backed `payload(ctx)` is schema input, direct `Command.event(ctx)` returns a pre-parse candidate, and `output(event, ctx)` remains parsed/stored output-shaped.
- `Command` candidate-vs-parsed event generic is trailing/defaulted, preserving raw command compatibility and existing operation helper inference.
- `EventPayloadOf<TDefinition>`, `EventOf<TDefinition>`, and `EventDefinition.create(...)` output-payload semantics are unchanged.
- Definition-backed candidates are built directly with copied tags; `EventDefinition.create(...)` is no longer used for command candidates.
- No new casts or public helper exports were needed.
- Docs updates are limited counterpart updates required to keep public API guidance current.

## Follow-ups
- Continue with task 05.
