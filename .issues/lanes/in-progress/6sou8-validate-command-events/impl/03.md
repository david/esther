# 03 — Document command event validation and run gates

## Source
- plan/01-implementation-plan.md

## Goal
Update public guidance for the additive event-definition-backed command API, record any docs intentionally left unchanged, and run full repository gates.

## Depends on
- 02.md

## Scope
- `llms.txt`
- `README.md` if current command examples mention event emission
- `doc/domain-language.md` if command DSL description needs public behavior update
- implementation checkpoint notes for docs not changed

## Contract surfaces
- public DSL documentation/examples
- LLM guidance for event authoring and command emission
- release/gate evidence

## Plan-check watch items
- Update README/docs only where current command examples exist; always update `llms.txt` because public DSL behavior changes.
- Public exports remain compatible; new helper types only exported if needed.

## Work
- Inspect current command/event examples in `README.md`, `doc/domain-language.md`, and `llms.txt`.
- Update `llms.txt` to describe:
  - preferred event-definition-backed command form with `event: Definition`, `tags(ctx)`, and `payload(ctx)`
  - runtime `SchemaError` before append for malformed definition-backed events
  - raw `event(ctx) => EventRecordInput` path remains deliberate low-level interop and unvalidated by event definition
- Update README/domain docs only where existing examples or command descriptions would otherwise be stale.
- If a docs file is inspected but not changed, record why in `impl/checkpoints/03.md`.
- Run full repository gates.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` reflects new public command event-definition API and validation behavior.
- README/domain docs are updated if their command examples became stale, or checkpoint explains no update needed.
- No docs claim `defineEvent.create(...)` validates payloads.
- Full gates pass.

## Suggested tests
- Documentation review plus full gates; no new standalone tests in this task unless docs examples are executable.

## Manual verification
- not applicable — no product UI/API workflow; QA is auto-runnable via repository gates.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
