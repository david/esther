# 02 — Update app, read-model, processor, and adapter docs

## Source
- plan/02-implementation-plan.md
- plan/checks/02-revised-plan-sanity.md
- research/01-current-state.md

## Goal

Finish `llms.txt` current-API update by correcting read-model/query guidance, projection and processor examples, app wiring, Fastify adapter guidance, error mapping, compatibility notes, and final document-level verification.

## Depends on
- 01.md

## Scope
- `llms.txt` Read models and Read model queries sections
- `llms.txt` Projectors and processors section
- `llms.txt` Wiring section
- `llms.txt` Event store hooks section if needed to avoid stale manual hook guidance
- `llms.txt` Errors and Rules sections
- Representative source cross-checks in `src/index.ts`, `src/core/read-model.ts`, `src/core/read-model-registration.ts`, `src/core/processor.ts`, `src/core/app.ts`, `src/adapters/fastify/index.ts`, `src/adapters/fastify/input.ts`, and `src/__tests__/type-check.ts`

## Contract surfaces
- read-model docs: Zod string, number, boolean, array, and object fields; Postgres arrays/objects as JSONB
- read-model query docs: `where`, `orderBy`, `orderDirection`, `limit`
- projection docs: direct lookup, named query lookup, `many: true`
- projector docs: `readModelEvent(...)` attached to `defineReadModel({ events: [...] })`, pure projector handler, app `readModels: [projection]`
- processor docs: `defineProcessor(...)`, `processorEvent(...)`, app `processors`, `effectAdapters`
- app wiring docs: optional `inputAdapter`, dynamic `app.dispatch(sliceName: string, input: unknown)`, typed entrypoints at adapter route/binding config
- Fastify docs: `createFastifyInputAdapter`, `defineFastifyRoutes`, host-owned auth
- errors: `ConcurrencyError`, `BoundaryObservationError`, Fastify 409 mapping for `ConcurrencyError`
- compatibility docs: legacy `projectionAdapters` / `projectionQuery` only terse compatibility text

## Plan-check watch items
- Keep `llms.txt` compact; avoid full tutorial creep.
- Cross-check representative snippets against `src/__tests__/type-check.ts`, `src/index.ts`, and `src/adapters/fastify/index.ts`.
- Use compile-safe read-model event example shape; avoid self-reference pitfalls if TypeScript rejects inline `orderSummaryModel` use.
- Keep legacy `projectionAdapters` / `projectionQuery` terse and compatibility-only.
- Do not imply Fastify route helpers provide authorization.
- Do not introduce examples with direct I/O in slices, read models, read-model event bindings, or processors.
- Record focused stale/current API searches plus full `typecheck`, `lint`, and `test` results in implementation checkpoint.

## Work
- Cross-check current read-model, processor, app, and Fastify examples against `src/__tests__/type-check.ts`, `src/index.ts`, and relevant source files before editing snippets.
- Update read-model schema text to include arrays/objects and note Postgres JSONB storage for array/object fields.
- Update named read-model query examples to include `orderDirection` where useful and document query-many `projection({ many: true })` result shape.
- Replace command-level `projectors` snippets with `readModelEvent(...)` bindings attached to `defineReadModel({ events: [...] })`.
- Replace command-level `processors` snippets with `defineProcessor(...)` / `processorEvent(...)`, registered in `createApp({ processors: [...] })` with effect adapters.
- Rewrite wiring docs to show `inputAdapter` optional and direct dynamic `app.dispatch("slice", input)` allowed.
- Add Fastify typed route snippet using `defineFastifyRoutes` and `createFastifyInputAdapter`; state authorization/session/token checks remain host responsibility.
- Update Errors section so `SliceError` includes `ConcurrencyError` and `BoundaryObservationError`; update Fastify mapping to include `ConcurrencyError` as 409.
- Tighten Rules section so typed invocation lives at adapter boundaries and app modules stay I/O-free.
- Ensure legacy `projectionAdapters` / `projectionQuery` remain only terse compatibility text, not primary examples.
- Run final focused stale/current API searches, full repo gates, and manual top-to-bottom documentation review.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` shows current read-model field support, `orderDirection`, and `many: true` projection lookups.
- Projector docs use `readModelEvent(...)`; no command-level `projectors` field remains as current guidance.
- Processor docs use `defineProcessor(...)` and `processorEvent(...)`; no command-level `processors` field remains as current guidance.
- App wiring shows optional `inputAdapter` and direct dynamic `app.dispatch("slice", input)`.
- Docs state typed invocation belongs at adapter route/binding config, not a public in-process typed app client.
- Fastify docs use `createFastifyInputAdapter` and `defineFastifyRoutes` and do not imply built-in authorization.
- Error docs include `ConcurrencyError` and `BoundaryObservationError`; Fastify mapping includes `ConcurrencyError` as 409.
- Legacy `projectionAdapters` / `projectionQuery` are only compatibility/deprecated text, not primary examples.
- Final `llms.txt` is compact enough for quick-reference use and internally consistent top-to-bottom.
- Focused stale/current API searches and full `typecheck`, `lint`, and `test` results are recorded in `impl/checkpoints/02.md`.

## Suggested tests
- No new runtime tests expected; this is docs-only.
- Before finishing, use focused searches to confirm stale adapter/projector/processor names are absent and current API names appear.
- Cross-check representative snippets against source exports and type-check fixtures before finalizing.

## Manual verification
- Read `llms.txt` top-to-bottom after edits.
- Confirm every code block uses current public APIs from research.
- Confirm removed APIs are absent or explicitly compatibility-only: no `createFastifyAdapter`; no raw public `tagQuery({ schemas, fold })`; no raw positional reducer inputs in `eventsByTagsDescriptor(...)` / `eventStore.queryByTags(...)`; no command-level `projectors` / `processors`.
- Confirm examples are internally consistent: imports cover shown helpers, event names match reducers/read-model events/processors, and `createApp` wiring matches snippets.
- Confirm docs remain quick-reference sized, not a full tutorial.

## Verification
- `rg -n "createFastifyAdapter|projectors:|processors:" llms.txt || true`
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt || true`
- `rg -n "defineEvent|defineReducer|createFastifyInputAdapter|defineFastifyRoutes|readModelEvent|defineProcessor|processorEvent|BoundaryObservationError|ConcurrencyError" llms.txt`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
