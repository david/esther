# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-27

## Summary
- Updated `llms.txt` read-model, query, projection, processor, app wiring, Fastify, hook, and rules docs to current public API.
- Replaced command-level projector/processor guidance with `readModelEvent(...)`, `defineProcessor(...)`, `processorEvent(...)`, app `readModels`, app `processors`, and effect adapters.
- Documented `orderDirection`, `projection({ many: true })`, optional `inputAdapter`, dynamic `app.dispatch(...)`, typed Fastify route binding, host-owned auth, and compatibility-only legacy projection APIs.

## Files changed
- `llms.txt`: current read-model/query/projector/processor/app/Fastify docs and final consistency pass.
- `.issues/lanes/in-progress/q8xeq-update-llms/impl/checkpoints/02.md`: implementation checkpoint.

## Verification
- `rg -n "createFastifyAdapter|projectors:|processors:" llms.txt || true`: pass with note — no `createFastifyAdapter`; no `projectors:`; one `processors: [sendOrderEmail]` remains as valid current `createApp` wiring, not command-level stale guidance.
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt || true`: pass — no `fold:` remains; `schemas:` appears only in `defineReducer(...)` snippets; no raw positional `eventsByTagsDescriptor(...)` / `queryByTags(...)` examples present.
- `rg -n "defineEvent|defineReducer|createFastifyInputAdapter|defineFastifyRoutes|readModelEvent|defineProcessor|processorEvent|BoundaryObservationError|ConcurrencyError" llms.txt`: pass — current API names all present.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 255 tests passed, 0 failed.

## Manual verification
- `llms.txt` top-to-bottom review: pass — code blocks use current public APIs from research, imports cover shown helpers, event names match reducers/read-model bindings/processors, and app wiring matches current source.
- Removed/stale APIs check: pass — no `createFastifyAdapter`, no raw public `tagQuery({ schemas, fold })`, no raw positional reducer inputs, no command-level `projectors` / `processors` fields.
- Compactness check: pass — guide remains quick-reference sized, not tutorial rewrite.

## Drift assessment
- Aligned with `impl/02.md`, `plan/02-implementation-plan.md`, `plan/checks/02-revised-plan-sanity.md`, and `research/01-current-state.md`.
- Low-risk local nuance: stale-search regex still matches the valid app-level `processors:` field required by the plan; checkpoint records it explicitly.
- No source API, runtime behavior, persistence, replay, auth, or adapter code changes introduced.

## Follow-ups
- none
