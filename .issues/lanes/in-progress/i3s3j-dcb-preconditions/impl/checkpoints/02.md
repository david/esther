# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-25

## Summary
- Enforced postgres append option-presence semantics so only omitted `options` skips precondition validation.
- Normalized postgres boundary selection with `options.boundaryTags ?? []`, preserving `boundaryTags: undefined` as the global stream boundary.
- Added a stable transaction-scoped postgres advisory append lock as the first statement inside each append transaction.
- Added focused postgres adapter tests for empty-boundary/global-boundary preconditions and lock/query ordering through in-transaction handlers.

## Files changed
- `src/adapters/postgres/index.ts`: updated precondition validation, added transaction-scoped advisory append lock acquisition, and kept position allocation/inserts/projectors inside the locked transaction.
- `src/adapters/postgres/event-store.test.ts`: added a mock postgres event-store harness and regression coverage for option-present preconditions, global boundary behavior, `ConcurrencyError` shape, and advisory lock ordering.
- `.issues/lanes/in-progress/i3s3j-dcb-preconditions/impl/checkpoints/02.md`: recorded this checkpoint.

## Verification
- `bun test src/adapters/postgres/event-store.test.ts`: pass — 13 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint:code`: pass — ESLint completed with zero warnings.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 203 tests passed.

## Drift assessment
- Aligned with `impl/02.md`: changed only postgres append semantics/locking and colocated postgres adapter tests.
- Aligned with `plan/02-implementation-plan.md`: present append options now activate postgres preconditions even when `expectedPosition` is `undefined`; undefined boundary tags select the global stream; stale failures return the existing `ConcurrencyError` value path.
- Aligned with `plan/checks/02-plan-sanity.md` watch items: adapter semantics match task 01's option-present behavior, advisory lock acquisition is the first transaction query before precondition reads or position allocation, and the existing `ConcurrencyError` shape is preserved.
- Minor implementation detail: the lock-ordering coverage uses the existing mock SQL harness rather than a real concurrent postgres race; this matches the task's preferred narrow transaction-query sequence fallback.
- No high-risk behavior, persistence/replay, auth, side-effect, rollout, or verification drift found.

## Follow-ups
- Continue with `impl/03.md` for command-side boundary observation wiring.
