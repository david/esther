# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-25

## Summary
- Added command-side `tagQuery(...)` boundary observation recording with copied tag arrays and returned `maxPosition`.
- Threaded an optional boundary-observation sink through command input pipeline dependencies into `executeCommand(...)`.
- Derived append preconditions from exactly one command-side observation and preserved no-options appends for zero observations.
- Added exported `BoundaryObservation` and `BoundaryObservationError` public surfaces and included the new framework error in `SliceError`.
- Added multi-observation fail-fast handling after successful input resolution and before validation, event construction, append, success output, projectors, processors, or effects.
- Added focused pipeline/type coverage for stale non-empty and empty observed boundaries, copied multi-observation errors, query-slice read-only behavior, and public type exports.

## Files changed
- `src/core/types.ts`: added `BoundaryObservation`, `BoundaryObservationError`, its constructor, and `SliceError` union membership.
- `src/core/slice.ts`: extended `SliceDeps` with `recordBoundaryObservation?` and recorded copied command-side `tagQuery(...)` observations.
- `src/core/compose.ts`: allowed command `InputPipeline.execute(...)` dependencies to carry the optional observation sink.
- `src/core/pipeline.ts`: collected command input observations, failed fast on multiple observations, and passed one observation as append preconditions.
- `src/index.ts`: exported the new public observation and error surfaces.
- `src/__tests__/pipeline-wiring.test.ts`: added command stale-boundary, empty-boundary, multi-observation, side-effect-negative, and query-slice read-only coverage.
- `src/__tests__/type-check.ts`: added compile-only coverage for public observation/error exports, `SliceError`, and `SliceDeps.recordBoundaryObservation`.
- `.issues/lanes/in-progress/i3s3j-dcb-preconditions/impl/checkpoints/03.md`: recorded this checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 19 tests passed.
- `bun test src/__tests__/pipeline.test.ts`: pass — 26 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint:code`: pass — ESLint completed with zero warnings.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 207 tests passed.

## Drift assessment
- Aligned with `impl/03.md`: implemented command-side `tagQuery(...)` observation recording, append precondition derivation, exported multi-observation error surfaces, and focused tests.
- Aligned with `plan/02-implementation-plan.md`: zero observations still append without options; one observation uses copied `{ boundaryTags, expectedPosition }`; multiple observations return `BoundaryObservationError` before downstream command work or side effects.
- Aligned with `plan/checks/02-plan-sanity.md` watch items: multi-observation checking happens only after input resolution succeeds; framework DCB errors return directly; existing `ConcurrencyError` shape is preserved; public exports are intentional; query-slice `state().pipe(tagQuery(...))` remains read-only/non-appending.
- Scope intentionally leaves `castTagQuery(...)` observation recording and non-observation descriptor guardrails for `impl/04.md`.
- No high-risk behavior, persistence/replay, auth, side-effect, rollout, or verification drift found.

## Follow-ups
- Continue with `impl/04.md` for `castTagQuery(...)` observations and remaining non-observation guardrails.
