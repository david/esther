# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-25

## Summary
- Enforced append option-presence semantics for in-memory and filesystem event stores: only omitted `options` skips precondition validation.
- Preserved global-boundary selection with `options.boundaryTags ?? []`, including `boundaryTags: undefined` and `boundaryTags: []`.
- Added focused adapter regression tests for empty tagged boundaries, empty global stream checks, and global latest-position checks.
- Documented `AppendOptions` empty-boundary/global-boundary semantics in the core event store contract.

## Files changed
- `src/core/event-store.ts`: documented present-options semantics for `expectedPosition: undefined` and global boundary selection.
- `src/adapters/in-memory/event-store.ts`: changed precondition bypass to `options === undefined` only.
- `src/adapters/in-memory/event-store.test.ts`: added option-presence and global-boundary regression coverage.
- `src/adapters/filesystem/index.ts`: changed precondition bypass to `options === undefined` only while preserving existing append lock behavior.
- `src/adapters/filesystem/index.test.ts`: added option-presence and global-boundary regression coverage.
- `.issues/lanes/in-progress/i3s3j-dcb-preconditions/index.md`: updated lane metadata after moving the issue to `in-progress`.
- `.issues/lanes/in-progress/i3s3j-dcb-preconditions/impl/checkpoints/01.md`: recorded this checkpoint.

## Verification
- `bun test src/adapters/in-memory/event-store.test.ts`: pass — 10 tests passed.
- `bun test src/adapters/filesystem/index.test.ts`: pass — 16 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint:code`: pass — ESLint completed with zero warnings during focused iteration.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 199 tests passed.

## Drift assessment
- Aligned with `impl/01.md`: implemented only local in-memory/filesystem store option-presence behavior and matching tests.
- Aligned with `plan/02-implementation-plan.md`: raw `EventStore.append(...)` now treats present options as an active precondition, including `expectedPosition: undefined`, and treats undefined/empty `boundaryTags` as the global stream boundary.
- Aligned with `plan/checks/02-plan-sanity.md` watch items: the existing `ConcurrencyError` fields and meanings were preserved; adapter logic is duplicated in the same small local pattern without cross-adapter imports.
- No high-risk behavior, persistence/replay, auth, side-effect, rollout, or verification drift found.

## Follow-ups
- Continue with `impl/02.md` for postgres append preconditions and transaction-scoped locking.
