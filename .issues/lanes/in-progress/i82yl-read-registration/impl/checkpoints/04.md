# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-25

## Summary
- Made the postgres projection adapter factory return an app-ready canonical writable read-model registration with `kind: "readModel"` and `handle` while preserving `adapter`, `get`, and `query`.
- Added focused postgres adapter coverage for the registration fields and direct destructuring/`adapter.execute()` usage.
- Preserved existing DDL generation, write, get, query, and replay-style adapter behavior.

## Files changed
- `src/adapters/postgres/read-model.ts`: updates the factory result type and returned value to implement the writable read-model registration contract.
- `src/adapters/postgres/read-model.test.ts`: adds regression coverage for the postgres factory registration shape and low-level adapter usage.
- `.issues/lanes/in-progress/i82yl-read-registration/impl/checkpoints/04.md`: records this implementation checkpoint.

## Verification
- `bun test src/adapters/postgres/read-model.test.ts`: pass — 22 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 227 tests passed.

## Manual verification
- not applicable — this is an adapter factory API shape change with automated adapter coverage and no manual runtime workflow.

## Drift assessment
- Aligned with task scope: postgres factory results now include `kind: "readModel"`, the original `handle`, `adapter`, `get`, and `query`, while direct destructuring and `adapter.execute()` remain runtime-compatible and typechecked.
- Preserved postgres behavior: DDL generation code is unchanged, and existing write/get/query tests remain green.
- Preserved plan-check watch items: the postgres factory is now app-ready like the in-memory factory; no legacy metadata behavior was changed; no broad casts, `Record<string, unknown>` value types, or dependency-boundary violations were introduced.
- No high-risk behavior, contract, persistence, replay, auth, side-effect, rollout, or verification drift found.

## Follow-ups
- Continue with `impl/05.md`.
