# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-25

## Summary
- Made the in-memory projection adapter factory return an app-ready canonical writable registration with `kind: "readModel"` and `handle` while preserving `adapter`, `get`, and `query` destructuring.
- Updated `createApp()` to route normalized canonical writable registrations, alongside legacy entries, into projection write/getter wiring, constraint metadata registration, and read-model event binding wiring.
- Added focused adapter and app-wiring tests for canonical registration shape, canonical constraint metadata derivation, automatic read-model event bindings, and projection lookup reads.

## Files changed
- `src/adapters/in-memory/read-model.ts`: returns the canonical writable registration shape from the in-memory factory.
- `src/adapters/in-memory/read-model.test.ts`: verifies the factory result is app-ready and low-level adapter/get/query use remains unchanged.
- `src/core/app.ts`: consumes normalized read-model registrations for app wiring instead of limiting runtime wiring to legacy projection adapter entries.
- `src/__tests__/pipeline.test.ts`: adds canonical in-memory app-wiring coverage for constraint metadata, event bindings, projection writes, and point lookups.

## Verification
- `bun test src/adapters/in-memory/read-model.test.ts src/__tests__/pipeline.test.ts`: pass — 42 tests passed.
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 21 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 222 tests passed.

## Manual verification
- not applicable — library runtime API change with no UI/CLI workflow; focused integration and full automated tests cover the observable behavior.

## Drift assessment
- Aligned with task scope: canonical in-memory registrations now carry the handle, are accepted directly by `readModels`, populate app getter/write wiring, derive constraint metadata from `handle.constraints` and `handle.name`, and wire `handle.events` automatically.
- Preserved legacy behavior: legacy `projectionAdapters` entries still flow through the same normalized wiring and remain authoritative for their explicit `constraints` and `tableName` values.
- Preserved planned task boundary: per-model query dispatch is still left to `impl/03.md`; this task only preserves the factory `query` capability on the registration shape.
- No high-risk behavior, persistence, replay, auth, side-effect, or verification drift found.

## Follow-ups
- Continue with `impl/03.md` to register per-model query capability.
