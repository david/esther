# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-25

## Summary
- Added per-model query registration in `createApp()` from normalized canonical writable and read-only read-model registrations.
- Updated slice query/query-many dispatch to use per-model queries before legacy `projectionQuery`, while preserving `ReadModelNotFound(sourceName, "query")` for missing slice query capability.
- Updated app-created `ReadInterpreter` query dispatch to use per-model queries before legacy `projectionQuery`, then return `[]` when no query capability exists.
- Added focused query-listing coverage for canonical `readModels` query/listing, read-only query registrations, missing-query errors, legacy fallback compatibility, precedence, and read-interpreter empty fallback.

## Files changed
- `src/core/app.ts`: builds a per-model projection query map from normalized registrations and wires `ProjectionStore` plus `ReadInterpreter` query dispatch through it with legacy fallback.
- `src/__tests__/query-listing.test.ts`: adds runtime coverage for canonical/read-only query capability, precedence over legacy `projectionQuery`, missing-query split, read-interpreter query behavior, and legacy compatibility.
- `.issues/lanes/in-progress/i82yl-read-registration/impl/checkpoints/03.md`: records this implementation checkpoint.

## Verification
- `bun test src/__tests__/query-listing.test.ts`: pass — 9 tests passed.
- `bun test src/__tests__/pipeline.test.ts src/__tests__/pipeline-wiring.test.ts src/core/read-interpreter.test.ts`: pass — 62 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 226 tests passed.

## Manual verification
- not applicable — library-only query behavior with no manual UI/CLI workflow; focused and full automated tests cover the observable behavior.

## Drift assessment
- Aligned with task scope: per-model query functions from canonical writable registrations and read-only registrations now back slice query reads and app-created read-interpreter query descriptors.
- Preserved legacy behavior: `projectionQuery` remains the fallback path, and existing legacy query-listing coverage remains green.
- Preserved planned precedence: per-model query capability is consulted before legacy `projectionQuery` for the same source name.
- Preserved missing-query split: `ProjectionStore.query`/`queryMany` return `ReadModelNotFound(sourceName, "query")` when no per-model or legacy capability exists; app-created `ReadInterpreter` query reads return `[]` in the same absence case.
- No high-risk behavior, contract, persistence, replay, auth, side-effect, or verification drift found.

## Follow-ups
- Continue with `impl/04.md`.
