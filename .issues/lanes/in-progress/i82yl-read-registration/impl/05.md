# 05 — Finish public API coverage and migration examples

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Complete the public export/type-level surface and update representative docs or examples so callers see `readModels` as the preferred app wiring path while legacy compatibility remains covered.

## Depends on
- 04.md

## Scope
- `src/index.ts`
- `src/__tests__/type-check.ts`
- `src/__tests__/pipeline.test.ts`, `src/__tests__/query-listing.test.ts`, and/or `src/__tests__/pipeline-wiring.test.ts` for any remaining legacy compatibility coverage
- docs/examples that mention manual projection table entries, including `doc/domain-language.md` if still stale

## Contract surfaces
- public package exports for new registration types and helper type aliases
- public `AppConfig.readModels` type-flow
- public adapter factory result type-flow for in-memory and postgres factories
- docs/examples: canonical `readModels: [create...ProjectionAdapter(handle)]` path preferred over manual table entries
- legacy `projectionAdapters` and `projectionQuery` remain accepted and tested

## Plan-check watch items
- Export the new registration types from `src/index.ts` without removing current compatibility exports.
- Preserve exact missing-query split: `ProjectionStore` returns `ReadModelNotFound(sourceName, "query")`; `ReadInterpreter` returns `[]` when no per-model or legacy query exists.
- Keep legacy table entries authoritative for their existing `constraints`/`tableName` behavior; derive metadata only for canonical writable registrations.
- Ensure postgres and in-memory factory return values remain destructurable and app-ready.

## Work
- Add or update type-level coverage in `src/__tests__/type-check.ts` for `AppConfig.readModels` accepting factory results.
- Add or update type-level coverage that factory results remain destructurable as `{ adapter, get, query }` with typed row values.
- Add or update type-level coverage for `ReadOnlyReadModelRegistration<T>` typed `get` and optional `query` without write capability.
- Export the new registration types from `src/index.ts` while keeping existing app, adapter, read-model, and legacy projection adapter exports intact.
- Audit docs/examples for manual `projectionAdapters` wiring and update representative user-facing examples to prefer canonical `readModels` where appropriate; do not remove legacy compatibility notes if they explain existing behavior.
- Add any missing focused regression test that legacy `projectionAdapters` table/view entries and legacy `projectionQuery`-only usage still work after all canonical wiring changes.
- Run full project verification and capture any uncovered high-risk drift in the checkpoint.

## Implementation checkpoint
- write `impl/checkpoints/05.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `src/index.ts` exports `ProjectionGetter`, `ProjectionQuery`, `WritableReadModelRegistration`, `ReadOnlyReadModelRegistration`, and `ReadModelRegistration`.
- Typecheck coverage proves factory results are accepted by `AppConfig.readModels`.
- Typecheck coverage proves factory results remain destructurable with typed row values.
- Typecheck coverage proves typed read-only registrations can provide `get` and optional `query` without write capability.
- Representative docs/examples prefer `readModels: [create...ProjectionAdapter(handle)]` for normal app wiring.
- Existing legacy `projectionAdapters` and `projectionQuery` compatibility remains tested and green.
- `bun run typecheck`, `bun run lint`, and `bun run test` pass.

## Suggested tests
- `src/__tests__/type-check.ts`: public type-flow assertions for canonical registrations and destructuring.
- `src/__tests__/query-listing.test.ts`: legacy `projectionQuery` fallback still works if not already covered by task 03.
- `src/__tests__/pipeline.test.ts` or `src/__tests__/pipeline-wiring.test.ts`: legacy table/view entries still work if not already covered by earlier tasks.

## Manual verification
- not applicable — plan QA defines this as library-only app wiring; public API and examples are verified by typecheck, tests, and docs inspection.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
