# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-30

## Summary
- Updated `llms.txt` public export list for command descriptor API and event candidate/input helper types.
- Replaced stale documented `CommandDefinition` root type import with `RawCommandDefinition`, `DefinitionBackedCommandDefinition`, and `AnyCommandDefinition`.
- Documented `commandDefinition(...)` wrapper guidance, migration away from ambiguous `CommandDefinition`, and schema-input candidate vs parsed event output behavior.
- Ran stale-name search and full repo gates.

## Files changed
- `llms.txt`: public API/docs update for descriptor names, helper types, migration guidance, and command DSL schema-input/output semantics.
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/04.md`: task checkpoint.

## Verification
- `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput"`: pass/classified — no `EventDefinitionCommandDefinition`, `CommandEventCandidate`, or `DefinitionBackedCommandPayloadInput`; remaining `CommandDefinition` hits are intentional public new names containing the substring, negative root-export test, internal runtime/helper names, and `llms.txt` migration guidance for removed alias.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint plus dependency-cruiser completed with no dependency violations.
- `bun run test`: pass — 280 tests, 693 assertions.

## Manual verification
- not applicable — task contract is automated API/docs gates only.

## Drift assessment
- Aligned with task 04 and approved plan-check: `llms.txt` now documents `RawCommandDefinition`, `DefinitionBackedCommandDefinition`, `AnyCommandDefinition`, `commandDefinition`, `EventPayloadInputOf`, and `EventCandidateOf`; it no longer recommends public `CommandDefinition`; it explains `payload(ctx)`/`command.event(ctx)` use schema input while `output(event, ctx)` receives parsed `EventOf<typeof Event>`.
- Root exports in `src/index.ts` already matched the planned public surface from earlier tasks, so no source export changes were needed.
- Stale-name search found only intentional remaining hits; no behavior, persistence, replay, auth, side-effect, or runtime contract drift found.

## Follow-ups
- none
