# 08 — Implementation checkpoint

Status: aligned
Task: impl/08.md
Date: 2026-05-01

## Summary
- Added focused CMS-style authenticated wrapper type proof for definition-backed wrapper composition.
- Proved wrapper-owned payload/output/event surfaces preserve definition-backed event typing while enriched session context remains visible.
- Proved `mergeOutputErrHandlers(definition.outputErr, authHandlers)` result type is the widened base-plus-auth handler map, and merged base handlers accept wrapped input/context.

## Files changed
- `src/__tests__/type-check.ts`: extended authenticated/session wrapper fixture for payload context, explicit merged handler type, definition-backed event/payload/output checks, and wrapped-input base error handler checks.
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/08.md`: recorded implementation evidence and drift status.

## Verification
- `bun run typecheck`: pass — wrapper fixture compiles with enriched `TCtx`, replaced input, widened `TError` / `TInputError`, merged outputErr handlers, and preserved definition-backed event surface.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.
- `bun run test`: pass — 281 tests, 0 fail.
- `rg "as unknown as" src/__tests__/type-check.ts src/core/slice.ts`: pass — no downstream-style double assertion in type fixture; only pre-existing bounded dispatch normalization cast remains in `src/core/slice.ts`.
- `rg "DefinitionBackedCommandDefinitionWithOutputErr|mergeOutputErrHandlers|as unknown as" src llms.txt`: pass — new API still present in public exports, docs, tests, and core; `as unknown as` hits are pre-existing runtime/test/storage casts plus docs mention.

## Manual verification
- compile-time wrapper API contract: not-applicable — no user-visible UI, CLI workflow, persistence, or external side effect.

## Drift assessment
- Aligned with `impl/08.md`, `plan/02-wrapper-safe-outputerr-plan.md`, and `plan/checks/02-wrapper-safe-outputerr-plan-sanity.md`.
- `wrapperAddsTypedErrorHandling` fixture starts from `DefinitionBackedCommandDefinition` and returns `DefinitionBackedCommandDefinitionWithOutputErr` without downstream `as unknown as ...`, private descriptor mirrors, or raw event downgrade.
- Wrapped input replacement and enriched `TCtx` are visible in validate, tags, payload, output, and outputErr handler coverage; base and added handlers keep safe `TWrappedCtx | TWrappedInput` use.
- `preserveDefinitionBackedEventValidation` remains visible at type boundary: wrapped descriptor keeps `event: typeof BookingConfirmedEvent`, `payload(ctx)` returns `EventPayloadInputOf<typeof BookingConfirmedEvent>`, and `output(event, ctx)` accepts `EventOf<typeof BookingConfirmedEvent>`.
- No core auth policy, denial semantics, storage, read-model, runtime command behavior, or docs change introduced; `llms.txt` already had accurate wrapper guidance from task 07, so no update needed.

## Follow-ups
- none
