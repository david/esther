# 06 — Implementation checkpoint

Status: blocked
Task: impl/06.md
Date: 2026-04-30

## Summary
- No source change made.
- Direct inline `function wrap<T extends AnyCommandDefinition>(definition: T): T` support is blocked by TypeScript contextual typing limits: a single generic constraint cannot infer callback parameter types from sibling descriptor properties (`input`, `event`) while preserving exact `T`.
- Working alternatives require changing wrapper-author API shape, requiring caller `commandDefinition({...})` pre-wrap, or duplicating family-specific wrapper overloads/generics; all conflict with task acceptance.

## Files changed
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/06.md`: recorded blocker and evidence.

## Verification
- `bun run typecheck`: pass — current repo state still typechecks before task source changes.
- `./node_modules/.bin/tsgo --noEmit --strict --ignoreConfig /tmp/ts-experiment.ts`: fail as expected for probe — `T extends Any` cannot derive exact callback context from `input`; direct descriptor-family generic parameter works only when wrapper signature duplicates family generics.
- `git status --short`: pass before checkpoint — clean tree before artifact write.

## Manual verification
- not-applicable — public TypeScript inference behavior only.

## Drift assessment
- High-risk acceptance drift against task 06 and review finding 02: required exact wrapper shape remains unsupported without broadening public wrapper design.
- Plan-check watch item says keep `AnyCommandDefinition` narrow enough to preserve inference. Current TypeScript behavior makes that unachievable for a bare alias used as `T extends AnyCommandDefinition` because contextual typing cannot express dependent relationships between object properties through one non-generic union constraint.
- `commandDefinition(...)` remains runtime identity; no cloning, validation, normalization, raw-command behavior, storage, replay, auth, or side-effect behavior changed.
- `llms.txt` unchanged because no accepted public wrapper guidance could be implemented; docs should change only after product/API decision.

## Follow-ups
- Choose one API path in plan/drift: document pre-wrapping with `commandDefinition({...})`, add reusable family-specific wrapper overload helper, or revise acceptance to allow wrapper signatures with explicit descriptor-family generics instead of `T extends AnyCommandDefinition` alone.
