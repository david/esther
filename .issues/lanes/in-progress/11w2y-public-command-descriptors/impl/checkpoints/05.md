# 05 — Implementation checkpoint

Status: aligned
Task: impl/05.md
Date: 2026-04-30

## Summary
- Added `commandDefinition(...)` descriptor-family overloads so inline definition-backed descriptors get the same contextual typing as inline `defineCommand({...})`.
- Removed `never` callback contexts from the broad `AnyCommandDefinition` helper shapes used by the generic wrapper overload.
- Added compile-only regression coverage for inline `commandDefinition({...})` inference, wrapper forwarding, bad payload rejection through `defineCommand(commandDefinition(...))`, and transform schema input/output separation.

## Files changed
- `src/core/slice.ts`: added inference-preserving `commandDefinition(...)` overloads and loosened broad `AnyCommandDefinition` helper shapes away from public `never` callback contexts.
- `src/__tests__/type-check.ts`: added type-level regressions for inline descriptor callback ctx/event inference, wrapper forwarding, bad schema-input payload rejection, and transform event candidate vs parsed output typing.
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/05.md`: task checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint plus dependency-cruiser completed with no dependency violations.
- `bun run test`: pass — 280 tests, 693 assertions.
- `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput" src llms.txt .issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints || true`: pass/classified — no stale private helper names in source/docs; remaining `CommandDefinition` hits are intentional public new names containing the substring, negative root-export coverage, migration guidance, and historical checkpoints.

## Manual verification
- not applicable — public TypeScript inference behavior verified by compile-time tests.

## Drift assessment
- Aligned with task 05, review finding, and plan-check watch items: inline `commandDefinition({...})` no longer gives `never` callback contexts for `validate`, `tags`, `payload`, or `output`; wrapper forwarding through `T extends AnyCommandDefinition` preserves returned descriptor type; bad definition-backed payload fields still fail once passed to `defineCommand`; transform tests prove `payload(ctx)` and command candidates use schema input while `output(event, ctx)` receives parsed `EventOf<typeof Event>`.
- `commandDefinition(...)` remains runtime identity; no cloning, validation, normalization, event append behavior, raw-event behavior, storage, replay, auth, or side-effect change.
- `llms.txt` already documents public command descriptor guidance and schema-input vs parsed-output behavior from task 04; no doc update needed for this inference-only correction.

## Follow-ups
- none
