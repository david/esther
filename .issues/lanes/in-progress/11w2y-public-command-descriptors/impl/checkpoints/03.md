# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-30

## Summary
- Added runtime coverage proving `commandDefinition(definition)` returns the exact same descriptor object.
- Strengthened definition-backed runtime coverage by asserting `eventSchema` is the original `eventDefinition.schema` before malformed candidate validation blocks append, output, projector, processor, and effect fanout.
- Strengthened raw command coverage by asserting raw command slices have no `eventSchema`, so raw event factory path remains unvalidated by event definitions.
- Removed stale type-like `CommandEventCandidate` wording from pipeline comments.

## Files changed
- `src/__tests__/pipeline-wiring.test.ts`: runtime identity, definition-backed validation boundary, and raw-path regression assertions.
- `src/core/pipeline.ts`: comment cleanup from stale private helper name to generic candidate wording.
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/03.md`: task checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 27 tests, 121 assertions.
- `bun run typecheck`: pass — TypeScript public/runtime contracts compile.
- `bun run test`: pass — 280 tests, 693 assertions.
- `bun run lint`: pass — ESLint and dependency-cruiser checks pass.
- `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput" src llms.txt .issues/lanes/in-progress/11w2y-public-command-descriptors -g '!impl/checkpoints/**' -g '!impl/output/**'`: pass/classified — source hits are intentional public names, internal helper names, and negative type-test coverage; `.issues` hits are source artifacts/tasks; `llms.txt` stale `CommandDefinition` mention remains task 04 scope.

## Manual verification
- not applicable — runtime library invariant has deterministic automated tests and no UI/API workflow beyond framework command execution.

## Drift assessment
- Aligned with task 03 and approved plan-check watch items: `commandDefinition(...)` remains pure identity; definition-backed commands still build candidates from event definition metadata and retain `eventSchema = eventDefinition.schema`; malformed candidates fail before append and before output/projector/processor/effect fanout; raw event factory commands keep `eventSchema` undefined and remain raw.
- No runtime command semantics, serialized event shape, migration/replay, auth, read-model, or side-effect behavior changed.
- Only local drift is a comment wording cleanup in `src/core/pipeline.ts`; it removes stale private-helper terminology without changing behavior.

## Follow-ups
- Continue with task 04 to update `llms.txt` public descriptor documentation and clear the remaining docs-scope `CommandDefinition` mention.
