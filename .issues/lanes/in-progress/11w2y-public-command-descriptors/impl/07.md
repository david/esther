# 07 — Add wrapper-safe outputErr public contract

## Source
- plan/02-wrapper-safe-outputerr-plan.md
- plan/checks/02-wrapper-safe-outputerr-plan-sanity.md

## Goal

Expose Esther-owned command wrapper outputErr composition API: `DefinitionBackedCommandDefinitionWithOutputErr` and `mergeOutputErrHandlers`, with root exports and public docs. Keep runtime command behavior unchanged.

## Depends on
- none

## Scope
- `src/core/slice.ts`
- `src/index.ts`
- `src/__tests__/type-check.ts`
- `llms.txt`

## Contract surfaces
- TypeScript public API: definition-backed command descriptor types and root exports
- TypeScript public API: `OutputErrHandlers` merge helper
- docs/LLM contract: `llms.txt`
- runtime command semantics: unchanged

## Intent / decision vocabulary
- `DefinitionBackedCommandDefinitionWithOutputErr`
- `DefinitionBackedCommandDefinitionBase`
- `mergeOutputErrHandlers`
- `wrapperAddsTypedErrorHandling`
- required `outputErr` stays required for widened non-`never` error unions

## Plan-check watch items
- Preserve plan intent over exact proposed helper signature if TypeScript needs slightly different generics.
- Keep any cast inside `mergeOutputErrHandlers(...)` limited to handler-map key-space/object-spread covariance, with comment.
- Define collision precedence: `addedHandlers` wins.
- Verify `mergeOutputErrHandlers(undefined, addedHandlers)` works for base commands with `TError = never` without weakening required `outputErr` for widened error unions.
- Update `src/index.ts` and `llms.txt` in same slice because public API changed.

## Work
- Add or extract shared definition-backed descriptor base fields near existing command descriptor types in `src/core/slice.ts`; keep helper internal unless public reuse is needed.
- Add public `DefinitionBackedCommandDefinitionWithOutputErr` type that requires `outputErr` and preserves `event: EventDefinition`, `tags`, `payload`, and `output` typing.
- Add public `mergeOutputErrHandlers(...)` near `OutputErrHandlers`; implement deterministic object-spread merge where added handlers win, and keep any cast local with comment explaining bounded key-space covariance.
- Export new type and helper from `src/index.ts`.
- Add focused type-check coverage in `src/__tests__/type-check.ts` for root imports, required-outputErr descriptor naming, base+added discriminated handler typing, and `mergeOutputErrHandlers(undefined, addedHandlers)`.
- Update `llms.txt` package export list and command wrapper guidance for new helper/type.
- Run `rg "DefinitionBackedCommandDefinitionWithOutputErr|mergeOutputErrHandlers|as unknown as" src llms.txt` and classify any `as unknown as` hit before checkpoint.

## Implementation checkpoint
- write `impl/checkpoints/07.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `DefinitionBackedCommandDefinitionWithOutputErr` is public, root-exported, and names definition-backed descriptors with required `outputErr`.
- `mergeOutputErrHandlers` is public, root-exported, accepts optional base handlers plus required added handlers, returns handlers for `TBaseError | TAddedError`, and documents added-handler-wins precedence.
- Casts do not erase event, ctx, input, output, or error relationships outside bounded handler-map merge.
- `llms.txt` mentions wrapper-safe outputErr composition and new public exports.
- Runtime command execution remains unchanged.

## Suggested tests
- Add type-level assertions/imports in `src/__tests__/type-check.ts` before implementation where practical.
- Use `bun run typecheck` to prove public API and handler discriminant typing.
- Use `rg "DefinitionBackedCommandDefinitionWithOutputErr|mergeOutputErrHandlers|as unknown as" src llms.txt` to inspect API/docs coverage and unsafe assertion usage.

## Manual verification
- not applicable — library TypeScript API/docs slice with no UI, CLI workflow, storage, or side effect behavior.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `rg "DefinitionBackedCommandDefinitionWithOutputErr|mergeOutputErrHandlers|as unknown as" src llms.txt`
