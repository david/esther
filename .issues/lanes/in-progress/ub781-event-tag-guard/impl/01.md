# 01 — Publish EventTagMismatchError contract

## Source
- plan/01-implementation-plan.md

## Goal

Add public framework error contract for observed-tag mismatch before behavior uses it.

## Depends on
- none

## Scope
- `src/core/types.ts`
- `src/index.ts`
- `src/__tests__/type-check.ts`

## Contract surfaces
- `EventTagMismatchError` type and constructor
- `SliceError` public union
- main package export surface
- compile-only public API/type test

## Intent / decision vocabulary
- `EventTagMismatchError`
- `missingObservedTags`
- `observedBoundaryTagsSubset`
- developer-facing framework diagnostic, not authorization

## Plan-check watch items
- Keep `EventTagMismatchError` as framework error bypassing `outputErr`; update `isFrameworkInputError(...)` only if implementation introduces any path where descriptors can return it.

## Work
- Add focused failing type-check coverage in `src/__tests__/type-check.ts` importing `EventTagMismatchError` constructor/type from main export.
- Assert `_tag` literal is `"EventTagMismatchError"`.
- Assert `SliceError` accepts `EventTagMismatchError`.
- Add `EventTagMismatchError` type and constructor in `src/core/types.ts` with stable message `Command emitted event missing observed DCB tags`.
- Constructor must copy `observedTags`, `eventTags`, and `missingTags` arrays.
- Export constructor and type from `src/index.ts`.
- Do not change adapter mappings or pipeline behavior in this task.

## Implementation checkpoint
- write `impl/checkpoints/01.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `EventTagMismatchError` constructor exists and returns shape from plan.
- `EventTagMismatchError` type is part of `SliceError`.
- Main `esther` export exposes constructor and type.
- Type-check assertions cover public import, `_tag`, and `SliceError` compatibility.
- No command pipeline behavior changes yet.

## Suggested tests
- Write type-check assertions first in `src/__tests__/type-check.ts` so `bun run typecheck` fails before contract exists.

## Manual verification
- not applicable — type/public API contract only; compile-only test is clearer than manual workflow.

## Verification
- `bun run typecheck`
