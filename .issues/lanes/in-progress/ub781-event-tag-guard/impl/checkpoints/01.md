# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-05-01

## Summary
- Added public `EventTagMismatchError` error contract before pipeline behavior uses it.
- Widened `SliceError` and root export surface.
- Added compile-only public API coverage for constructor import, `_tag`, and `SliceError` compatibility.

## Files changed
- `src/core/types.ts`: added `EventTagMismatchError` type/constructor and included it in `SliceError`.
- `src/index.ts`: exported `EventTagMismatchError` from root package surface.
- `src/__tests__/type-check.ts`: added public API/type assertions for `EventTagMismatchError`.
- `.issues/lanes/in-progress/ub781-event-tag-guard/impl/checkpoints/01.md`: recorded task checkpoint.
- `.issues/lanes/in-progress/ub781-event-tag-guard/**`: moved issue from backlog to in-progress before coding per workflow.

## Verification
- `bun run typecheck`: fail — expected test-first failure before contract export existed; TS2305 for missing `EventTagMismatchError` export.
- `bun run typecheck`: pass — public API/type contract compiles after implementation.

## Manual verification
- not applicable — type/public API contract only; compile-only typecheck covers required surface.

## Drift assessment
- Aligned with `impl/01.md`: added only public error contract, `SliceError`, root export, and type-check assertions.
- Aligned with `plan/01-implementation-plan.md`: stable message is `Command emitted event missing observed DCB tags`; copied tag arrays in constructor; no adapter mappings or command pipeline behavior changed.
- Plan-check watch item satisfied: `EventTagMismatchError` remains framework error contract only; no descriptor path or `isFrameworkInputError(...)` change introduced.

## Follow-ups
- `llms.txt` public export docs intentionally deferred to `impl/04.md`, which owns docs/LLM-guide updates for the full behavior slice.
