# 02 — Guard definition-backed observed tags before append

## Source
- plan/01-implementation-plan.md

## Goal

Enforce `observedBoundary.tags ⊆ emittedEvent.tags` for definition-backed command events after event schema validation and before append.

## Depends on
- 01.md

## Scope
- `src/core/pipeline.ts`
- `src/__tests__/pipeline-wiring.test.ts`

## Contract surfaces
- command execution order
- event schema validation order
- event-store append precondition options
- `EventTagMismatchError` result path
- command-side `tagQuery(...)` observation behavior

## Intent / decision vocabulary
- `ensureObservedTagsVisibleOnEvent`
- `missingObservedTags`
- `observedBoundaryTagsSubset`
- `noMismatchFanout` append portion only
- preserve DCB decision visibility

## Plan-check watch items
- Preserve missing tag order from observed tags; use set membership so event tag order and extra tags do not matter.
- Verify no append by querying store or append counter, not only by result error.
- Update command execution order docs in code comments so guard appears between event schema validation and append.
- Keep core free of adapter imports; do not duplicate guard in event-store adapters.

## Work
- Add focused failing tests in `src/__tests__/pipeline-wiring.test.ts` for definition-backed commands:
  - observed tag plus matching emitted tag succeeds.
  - observed tag plus extra emitted tags succeeds.
  - missing observed tag returns `EventTagMismatchError` with `commandName`, `eventType`, `observedTags`, `eventTags`, and `missingTags`.
  - mismatch appends no event; prove with event-store query or append counter.
  - definition-backed malformed event still returns `SchemaError` before tag guard.
  - empty observed tags/global boundary does not require emitted tag.
- Implement smallest core helper in `src/core/pipeline.ts` after parsed event result and before append.
- Use set membership for inclusion; preserve missing tags in first-seen observed order.
- Return `err(EventTagMismatchError(...))` directly, before `eventStore.append(...)`.
- Keep existing append options unchanged when guard passes.
- Update command execution order comment in `executeCommand(...)`.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Matching observed tags append successfully.
- Extra emitted tags are allowed.
- Missing observed tags return `EventTagMismatchError` after event schema validation and before append.
- Empty observed tags impose no tag requirement.
- Malformed definition-backed event still returns `SchemaError` before mismatch guard.
- Existing append precondition options remain unchanged when guard passes.

## Suggested tests
- Add pipeline tests first in `src/__tests__/pipeline-wiring.test.ts`, then implement helper.

## Manual verification
- not applicable — library command pipeline behavior has no CLI/HTTP workflow here; focused automated dispatch tests are concrete.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun run typecheck`
