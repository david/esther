# 05 — Fix DCB guide withdraw snippet typing

## Source
- review/findings/01-dcb-guide-snippet-does-not-typecheck.md

## Goal

Make the primary `withdraw` command snippet in `doc/dcb.md` typecheck against the current public API while preserving the DCB teaching points from the review finding.

## Depends on
- 04.md

## Scope
- `doc/dcb.md` first `typescript` code block / primary `withdraw` command example
- `llms.txt` consistency check only, if snippet wording changes canonical API guidance
- temporary local snippet extraction file for verification only; do not commit it

## Contract surfaces
- canonical documentation example for `defineCommand`
- domain error/output typing around `outputErr`
- docs-only public API guidance; no runtime API, event, persistence, adapter, or schema changes

## Intent / decision vocabulary
- `observedBoundary`
- `appendGuard`
- `decisionTags`
- `futureVisibilityTags`
- `InsufficientFunds` domain error handling via `outputErr`

## Plan-check watch items
- Ensure snippets use current public API names and compile mentally against `defineEvent`, `defineReducer`, `defineCommand`, `compose`, `tagQuery`, `castTagQuery`, `lookup`, `generate`.
- In examples, emitted event tags should include same decision boundary tag when event can affect future decisions.
- Keep `llms.txt` concise but update it because DSL behavior docs/canonical examples change; if no update is needed, record why.

## Work
- Extract the first `typescript` block from `doc/dcb.md` into a temporary repo-local file and run the strict snippet typecheck from the finding to reproduce the overload/domain-error failure.
- Update the `withdraw` snippet to use the supported explicit `defineCommand` type/context pattern so `outputErr` accepts `InsufficientFunds` and `AccountDebited` remains the emitted event definition.
- Preserve the teaching points: command-side `tagQuery(...)` observes the decision boundary, emitted `AccountDebited` carries the same account decision tag, and `outputErr` handles insufficient funds.
- Check whether `llms.txt` needs a matching update for the corrected canonical command shape; update it only if guidance would otherwise drift, otherwise record why no update is needed in the checkpoint.
- Re-extract the snippet and rerun the strict temp-file typecheck. Ignore unrelated environment errors only if the snippet-specific overload/domain-error errors are gone.
- Run focused docs diff review for `doc/dcb.md` and any `llms.txt` change.

## Implementation checkpoint
- write `impl/checkpoints/05.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Primary `withdraw` snippet in `doc/dcb.md` no longer produces the review finding's snippet-specific TypeScript errors.
- Snippet still teaches `tagQuery(...)` observed boundary, matching emitted decision tag, and `outputErr` for `InsufficientFunds`.
- No docs imply runtime/API behavior changed.
- `llms.txt` is either updated for consistency or checkpoint explains why no update was needed.
- Temporary snippet extraction file is removed or left untracked only if intentionally documented.

## Suggested tests
- Reproduce the current failure first with the extracted first `typescript` block from `doc/dcb.md`.
- After the edit, rerun the same strict snippet typecheck and confirm the snippet-specific overload/domain-error errors are gone.

## Manual verification
- not applicable; docs-only type correctness follow-up, best verified by strict snippet typecheck and docs diff review.

## Verification
- `bunx tsgo --noEmit --ignoreConfig --moduleResolution bundler --module esnext --target es2022 --strict .dcb-snippet.tmp.ts`
- `git diff -- doc/dcb.md llms.txt`
- `bun run typecheck`
