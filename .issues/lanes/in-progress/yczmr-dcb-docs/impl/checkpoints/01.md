# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-30

## Summary
- Added `doc/dcb.md` as canonical short DCB guide.
- Covered mental model, observed boundary, append guard, decision tags, projection context, future visibility tags, and single-boundary limit.
- Included correct account withdraw example plus projection-only, too-narrow tag, and missing emitted boundary tag misuses.

## Files changed
- `doc/dcb.md`: new DCB guide for command-side event-history reads and current limits.
- `.issues/lanes/in-progress/yczmr-dcb-docs/index.md`: updated lane/status and next task after issue moved to in-progress.
- `.issues/lanes/in-progress/yczmr-dcb-docs/impl/checkpoints/01.md`: task checkpoint.

## Verification
- `grep -n "tagQuery\|castTagQuery\|lookup\|BoundaryObservationError\|ConcurrencyError\|intersection\|global stream" doc/dcb.md`: pass — all required terms present.
- `git diff --cached -- doc/dcb.md`: pass — reviewed new guide diff before commit.

## Manual verification
- “What is DCB in Esther?”: pass — tag-based optimistic concurrency for command-side event-history reads.
- “Which tags do I query for a withdraw command?”: pass — tags that include all debit/credit events affecting account balance, e.g. `account:<id>`.
- Unsafe examples identified: pass — projection-only lookup, too-narrow intersection tags, and emitted event missing boundary tag.

## Drift assessment
- Aligned with `impl/01.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- Kept guide concise; no runtime, API, adapter, persistence, or schema changes.
- Snippet uses current public API names: `defineEvent`, `defineReducer`, `defineCommand`, `compose`, `tagQuery`, and references `lookup` misuse.
- Intersection semantics are stated next to the first `tagQuery(...)` example.
- Correct example emits the same account boundary tags it observes.
- Current limits explicitly name `BoundaryObservationError`, `ConcurrencyError`, global stream boundary, and lack of emitted-tag verification.

## Follow-ups
- Continue with `impl/02.md` to add README entry-point docs and link to `doc/dcb.md`.
