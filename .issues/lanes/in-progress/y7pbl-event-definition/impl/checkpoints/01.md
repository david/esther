# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-27

## Summary
- Added core `defineEvent(...)` helper, event definition types, generated event schema, typed constructor, and event-type extraction owner.
- Preserved `src/core/processor.ts` `extractEventType` compatibility by re-exporting from event module.
- Exported public helper and types from package root.

## Files changed
- `src/core/event.ts`: owns event definition helper, derived types, generated schema, constructor, and `extractEventType`.
- `src/core/event.test.ts`: covers helper runtime contract, wrong type rejection, constructor copy/reference rules, no-parse behavior, and event-type extraction.
- `src/core/processor.ts`: imports and re-exports `extractEventType` from event module for compatibility.
- `src/core/app.ts`: imports `extractEventType` from event module for read-model event wiring.
- `src/index.ts`: exports `defineEvent`, `EventDefinition`, `EventOf`, and `EventPayloadOf`.
- `.issues/lanes/in-progress/y7pbl-event-definition/**`: moved issue from backlog to in-progress and recorded checkpoint.

## Verification
- `bun test src/core/event.test.ts`: pass — 6 tests.
- `bun test src/core/processor.test.ts`: pass — 4 tests.
- `bun run typecheck`: pass.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.

## Drift assessment
- Aligned with task and approved plan. Helper is additive, schemas remain raw Zod object schemas with top-level literal `type`, `.create(...)` returns object only, copies tags, and preserves payload reference.
- No direct `EventDefinition` acceptance added to reducer, read-model, or processor APIs; users still pass `.schema`.
- No casts were needed in `src/core/event.ts`.

## Follow-ups
- Continue with `impl/02.md` for public type-inference coverage.
