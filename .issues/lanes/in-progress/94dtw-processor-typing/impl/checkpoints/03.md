# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-27

## Summary
- Added read-model event ctx read inference type checks for get reads, query reads, and events-by-tags reducer reads.
- Added wrong-usage `@ts-expect-error` assertions for read-model event ctx reads.
- Preserved and pinned `ctx.project(...)` and `ctx.get(...)` helper types through an explicit `ReadModelEventBinding` context-helper check.
- Removed representative manual runtime narrowing from read-model event read handler.
- Added malformed read row coverage proving projection handler/adapter do not run when ctx read validation fails.

## Files changed
- `src/__tests__/type-check.ts`: pins read-model event ctx read inference and ctx helper types.
- `src/core/read-model.test.ts`: direct typed `ctx.current` access and malformed row projection-gating test.
- `.issues/lanes/in-progress/94dtw-processor-typing/impl/checkpoints/03.md`: task checkpoint.

## Verification
- `bun test src/core/read-model.test.ts`: pass — 35 tests, 0 fail.
- `bun run typecheck`: pass.
- `bun run lint:code`: pass.

## Manual verification
- not applicable — read-model DSL typing and projection gating are CLI/library behavior covered by typecheck and tests.

## Drift assessment
- Aligned with task scope: descriptor-derived ctx reads are pinned without manual handler ctx read annotation.
- Aligned with plan-check watch item: wrong ctx read usage fails through `@ts-expect-error` assertions.
- `ctx.project(...)` and `ctx.get(...)` behavior/types are preserved; `ctx.get(...)` remains existing direct projection getter and is not routed through descriptor validation.
- Malformed descriptor read rows reject before handler/projection adapter execution.
- No read-model registration, projection adapter contract, event hook timing, projection storage, event payload, or adapter boundary changed.

## Follow-ups
- none
