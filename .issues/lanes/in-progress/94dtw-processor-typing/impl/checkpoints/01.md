# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-27

## Summary
- Changed `ReadInterpreter.resolve(...)` to preserve `ReadDescriptor<T>` as `Promise<T>`.
- Added shared core read-model row validation helper and reused it from slice projection reads.
- Added interpreter validation for `getDescriptor(...)` hits and `queryDescriptor(...)` rows.
- Updated interpreter tests to use typed get/query results without manual `unknown` extraction.
- Added malformed get/query row rejection coverage with `ReadModelSchemaError`.

## Files changed
- `src/core/read-model-validation.ts`: extracted shared read-model row validation and descriptor-result helpers with local documented casts.
- `src/core/read-interpreter.ts`: returns typed descriptor results and validates projection get/query rows before returning.
- `src/core/read-interpreter.test.ts`: pins typed direct interpreter results and malformed row rejection behavior.
- `src/core/slice.ts`: imports shared validation helper so slice and interpreter validation stay aligned.
- `.issues/lanes/in-progress/94dtw-processor-typing`: moved issue from backlog to in-progress and recorded checkpoint.

## Verification
- `bun test src/core/read-interpreter.test.ts`: pass — 12 tests, 0 fail.
- `bun test src/core/slice.test.ts src/__tests__/query-listing.test.ts`: pass — 23 tests, 0 fail.
- `bun test src/core/read-interpreter.test.ts src/core/slice.test.ts src/__tests__/query-listing.test.ts`: pass — 35 tests, 0 fail.
- `bun run typecheck`: pass.
- `bun run lint:code`: pass.

## Manual verification
- not applicable — core library contract verified by focused runtime tests and typecheck; no browser/UI workflow exists.

## Drift assessment
- Aligned with task goal: `ReadInterpreter.resolve(...)` now returns `Promise<T>` for `ReadDescriptor<T>`.
- Aligned with validation scope: read-model get hits and query rows validate through model schema and throw existing `ReadModelSchemaError` on malformed rows.
- Missing get still resolves `undefined`; query empty/fallback behavior unchanged.
- Slice validation diagnostics preserved by moving existing formatter/validation logic into shared helper.
- Casts are localized in `src/core/read-model-validation.ts` with comments explaining schema-output/descriptor-result equivalence.
- No event payload, storage format, adapter boundary, query fallback, or missing-row behavior changed.

## Follow-ups
- none
