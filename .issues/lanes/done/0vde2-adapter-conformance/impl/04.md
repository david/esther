# 04 — Audit conformance coverage and run full gates

## Source
- plan/01-refactor-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Perform the final duplicate-coverage audit and run focused adapter tests plus full repo gates before handing the issue to review/checks.

## Depends on
- 03.md

## Scope
- `src/__tests__/event-store-append-conformance.ts`
- `src/adapters/in-memory/event-store.test.ts`
- `src/adapters/filesystem/index.test.ts`
- `src/adapters/postgres/event-store.test.ts`
- no production files unless a prior task left an explicitly documented verification blocker

## Contract surfaces
- Test coverage and architecture boundaries only
- Dependency-cruiser import boundaries for test helper imports
- No production event-store, adapter, persistence, SQL, processor, projector, or public API changes

## Plan-check watch items
- Keep `src/__tests__/event-store-append-conformance.ts` test-only and imported only by `*.test.ts` files.
- When removing local duplicate tests, remove only cases exactly replaced by the shared suite; keep persistence, locking, handler ordering, query, constraint, and adapter-specific tests local.
- Make the `undefined`/`[]` global-boundary conformance case cross-check both directions (`undefined` seed then `[]`, and `[]` seed then `undefined`).
- If the helper verifies failed appends do not persist events, use `queryByTags(...)` with explicit schemas rather than weakening the assertion to `isErr()` only.

## Work
- Audit the final diff against the six planned conformance cases and the plan-check watch items.
- Confirm only test files and test-only helpers changed, unless a prior checkpoint documents a necessary local harness-only adjustment.
- Confirm local duplicate tests removed from adapter files are exactly covered by the shared suite.
- Confirm adapter-specific tests stayed local:
  - in-memory storage, query, and handler ordering;
  - filesystem persistence, index repair, temp-file behavior, query ordering, and checkpoints;
  - postgres constraint mapping and advisory-lock ordering.
- Run the three focused adapter test files.
- Run the full repo verification gates.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against `plan/01-refactor-plan.md` and `plan/checks/01-plan-sanity.md`
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Shared conformance coverage is present for in-memory, filesystem, and postgres adapters.
- The suite covers every contract listed in `description.md` and `plan/01-refactor-plan.md`.
- Adapter-specific persistence, locking, handler, query, checkpoint, and constraint coverage remains local.
- Focused adapter tests pass.
- Full `bun run typecheck`, `bun run lint`, and `bun run test` pass.

## Suggested tests
- No new behavior tests should be added here unless the audit finds a missed planned conformance case; if so, add it to the shared helper and re-run all adapter suites.

## Verification
- `bun test src/adapters/in-memory/event-store.test.ts`
- `bun test src/adapters/filesystem/index.test.ts`
- `bun test src/adapters/postgres/event-store.test.ts`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
