# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-25

## Summary
- Wired the shared `EventStore.append(...)` append-precondition conformance suite into the postgres event-store tests.
- Added a postgres conformance factory that creates a fresh mock SQL harness for each conformance store instance.
- Extended the local postgres mock SQL harness narrowly to support the `queryByTags(...)` event-row `SELECT id, type, tags, payload, position, timestamp ... ORDER BY position ASC` shape used by the shared conformance suite.
- Removed postgres local append-precondition tests covered by the shared conformance suite while keeping the advisory-lock ordering test local.

## Files changed
- `src/adapters/postgres/event-store.test.ts`: imports and runs the shared conformance suite, stores enough mock event-row data for conformance `queryByTags(...)`, supports the event-row SELECT shape, and removes duplicate local append-precondition tests.
- `.issues/lanes/in-progress/0vde2-adapter-conformance/impl/checkpoints/03.md`: records this checkpoint.

## Verification
- `bun test src/adapters/postgres/event-store.test.ts`: pass — 16 tests passed, including the nested postgres append-precondition conformance suite.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint completed and dependency-cruiser reported no dependency violations.

## Drift assessment
- Aligned with `impl/03.md`: postgres now runs `defineEventStoreAppendConformanceTests(...)`, each conformance store is backed by a fresh `createEventStoreHarness()` instance, and the harness was extended only for the existing event-row query shape needed by conformance failed-append visibility assertions.
- Aligned with `plan/01-refactor-plan.md`: this remains test-only, with no postgres production implementation, SQL schema, production query ordering, locking, constraint mapping, or adapter behavior changes.
- Aligned with `plan/checks/01-plan-sanity.md` watch items: protected `ConcurrencyError` fields are asserted by the shared suite, duplicate local precondition tests were removed only where covered by conformance, postgres constraint/advisory-lock coverage remains local, and the shared helper remains imported only from a `*.test.ts` file.

## Follow-ups
- Continue with task 04.
