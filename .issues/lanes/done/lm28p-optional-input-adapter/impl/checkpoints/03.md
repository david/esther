# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-27

## Summary
- Removed obsolete local noop input adapter helpers from processor and read-model core tests.
- Updated affected `createApp()` test setup to omit `inputAdapter` while preserving original assertions.
- Updated architecture docs to describe optional input adapter binding and direct dynamic dispatch without transport.

## Files changed
- `src/core/processor.test.ts`: removed local noop input adapter helper and omitted `inputAdapter` in processor wiring tests.
- `src/core/read-model.test.ts`: removed local noop input adapter helper and omitted `inputAdapter` in read-model event wiring tests.
- `doc/architecture.md`: changed app wiring from mandatory one input adapter binding to optional transport/runtime binding; documented no-transport direct dynamic dispatch.
- `.issues/lanes/in-progress/lm28p-optional-input-adapter/impl/checkpoints/03.md`: task checkpoint.

## Verification
- `bun test src/core/processor.test.ts src/core/read-model.test.ts`: pass — 39 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 255 tests passed.

## Drift assessment
- Aligned with `impl/03.md`, `plan/01-implementation-plan.md`, and plan-check watch items.
- Cleanup stayed limited to local noop helpers in core tests and docs; broader `createInMemoryAdapter()` integration-test usage was not touched.
- Processor and read-model behavior remained covered through unchanged assertions.
- No concrete adapter behavior, core/adapters boundary, event, persistence, read-model, processor, effect, or dispatch contract changes introduced.

## Follow-ups
- none
