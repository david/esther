# 04 — Run docs consistency QA and full gates

## Source
- plan/01-implementation-plan.md

## Goal

Finish the docs slice by checking all DCB documentation surfaces for consistency, running repo gates, and recording final implementation checkpoint evidence.

## Depends on
- 01.md
- 02.md
- 03.md

## Scope
- `doc/dcb.md`
- `README.md`
- `doc/domain-language.md`
- `llms.txt`
- final verification only; no new DCB semantics unless prior tasks missed acceptance criteria

## Contract surfaces
- documentation consistency across human guide, README, glossary, and LLM guidance
- repo gate evidence
- no runtime API, event, persistence, adapter, or schema changes

## Intent / decision vocabulary
- `observedBoundary`
- `appendGuard`
- `decisionTags`
- `projectionContext`
- `futureVisibilityTags`
- `singleBoundaryLimit`

## Plan-check watch items
- Final implementation should run full repo gates unless user explicitly approves lighter docs-only verification.
- Verify snippets use current public API names and do not imply unsupported multi-boundary commands.
- Keep `llms.txt` concise and aligned with `doc/dcb.md`.

## Work
- Review `doc/dcb.md`, README DCB section, `doc/domain-language.md` DCB entry, and `llms.txt` DCB guidance side by side.
- Confirm all required sharp edges appear somewhere clear and not contradictory: event-history reads vs projection reads, tag intersection, global stream boundary, one-boundary limit, no emitted-tag verification, DCB is not authorization.
- Confirm examples and counterexamples preserve `decisionTags`, `projectionContext`, and `futureVisibilityTags` semantics.
- Fix only local wording or omissions needed to satisfy the plan; stop and report if fix would change runtime/API semantics or broaden scope.
- Run full repo gates.
- Record manual docs QA answers from plan in checkpoint.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- All acceptance criteria from `plan/01-implementation-plan.md` are met.
- DCB terminology and limits match across `doc/dcb.md`, README, `doc/domain-language.md`, and `llms.txt`.
- No docs imply runtime/API behavior changed.
- Manual docs QA checklist from plan has concrete pass/fail notes in `impl/checkpoints/04.md`.
- Full repo gates are run, or any gate failure is recorded with exact output summary and classified for follow-up.

## Suggested tests
- No test-first step; final consistency and gate task.
- Run gates after local consistency review.

## Manual verification
- Read README intro and answer: “What is DCB in Esther?” Expected: tag-based optimistic concurrency for command-side event-history reads.
- Read `doc/dcb.md` checklist and answer: “Which tags do I query for a withdraw command?” Expected: tags that include all credit/debit events affecting account balance, e.g. `account:<id>`.
- Identify unsafe command from docs counterexamples: projection-only lookup before append, too-narrow intersection tags, emitted event missing boundary tag.
- Confirm limits are visible: one observed boundary, no automatic emitted-tag verification, `[]`/`undefined` global stream boundary.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
- `git diff -- doc/dcb.md README.md doc/domain-language.md llms.txt`
