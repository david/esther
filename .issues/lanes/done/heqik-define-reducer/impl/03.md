# 03 — Prove reducer-backed event-store and tagQuery behavior

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal
Add/update runtime coverage proving reducer-backed event-store reads and `tagQuery` preserve parsing, state resolution, max-position boundary observations, and stale-boundary preconditions.

## Depends on
- 02.md

## Scope
- `src/adapters/in-memory/event-store.test.ts`
- `src/adapters/filesystem/index.test.ts`
- `src/adapters/postgres/event-store.test.ts`
- `src/__tests__/event-store-append-conformance.ts`
- `src/__tests__/pipeline.test.ts`
- `src/__tests__/pipeline-wiring.test.ts`
- small fixes in event-store adapters or `src/core/slice.ts` only if these tests expose cutover regressions

## Contract surfaces
- event-store adapter parsing/folding contract
- command-side `tagQuery` state resolution
- query-side `state().pipe(tagQuery(...))`
- boundary observation and append precondition behavior for tag queries

## Plan-check watch items
- Convert every public raw event-history read surface together; partial conversion should fail typecheck.
- Watch existing `schemas: []` tests; replace with reducer definitions without widening event/state types or introducing unsafe casts.
- Ensure type-check negative cases prove old raw `tagQuery`, `castTagQuery`, and `eventsByTagsDescriptor` forms fail.

## Work
- Update or add in-memory event-store tests proving `queryByTags(tags, reducer)` parses matching events with `reducer.schemas` and returns `reducer.fold` state.
- Update or add filesystem and Postgres event-store tests for the same reducer contract and tag-intersection behavior.
- Update conformance coverage so append/read history tests use reducer definitions.
- Update command pipeline tests proving reducer-backed `tagQuery` returns reducer state and records the same boundary `maxPosition` observations as before.
- Update stale non-empty and empty boundary precondition tests to use reducers and assert conflicts still reject appends.
- Update query-side `state().pipe(tagQuery(...))` coverage proving query remains read-only and returns reducer state.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- All event-store adapter tests exercise reducer definitions instead of raw schema/fold pairs.
- Adapter tests still prove tag matching, schema parsing, folding order, and returned state.
- Command-side `tagQuery` tests prove boundary observations and stale-boundary rejection remain unchanged.
- Query-side `tagQuery` tests prove read-only behavior remains unchanged.

## Suggested tests
- Prefer real framework primitives and existing adapter stores.
- Add focused assertions before code fixes when a regression appears.

## Manual verification
- not applicable — library event-store and command/query behavior has no manual UI/CLI workflow; targeted tests are the executable QA.

## Verification
- `bun test src/adapters/in-memory/event-store.test.ts src/adapters/filesystem/index.test.ts src/adapters/postgres/event-store.test.ts src/__tests__/event-store-append-conformance.ts src/__tests__/pipeline.test.ts src/__tests__/pipeline-wiring.test.ts`
