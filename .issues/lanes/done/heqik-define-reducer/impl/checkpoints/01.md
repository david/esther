# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-26

## Summary
- Added the branded `defineReducer` core DSL primitive.
- Added runtime coverage for reducer fold order and fresh initial-state folding.
- Added compile-only coverage for schema tuple event inference, `event.type` narrowing, fold state inference, and plain-object reducer rejection.
- Exported reducer factory and public reducer types from the root package.

## Files changed
- `src/core/reducer.ts`: new private-brand reducer factory and public reducer types.
- `src/core/reducer.test.ts`: focused runtime reducer fold coverage.
- `src/__tests__/type-check.ts`: compile-only reducer inference, narrowing, and brand rejection coverage.
- `src/index.ts`: root export for `defineReducer`, `ReducerDefinition`, and `ReducerEvent`.
- `.issues/lanes/in-progress/heqik-define-reducer`: moved issue from backlog to in-progress before implementation.
- `.issues/lanes/in-progress/heqik-define-reducer/index.md`: updated lane/status after starting implementation.
- `.issues/lanes/in-progress/heqik-define-reducer/impl/checkpoints/01.md`: implementation checkpoint for this task.

## Verification
- `bun test src/core/reducer.test.ts`: pass — 2 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.

## Manual verification
- library-level reducer foundation: not-applicable — no UI, server route, or CLI workflow.

## Drift assessment
- Aligned with `impl/01.md`: this slice is additive only and does not convert `tagQuery`, `castTagQuery`, read descriptors, or event-store APIs.
- Aligned with `plan/01-implementation-plan.md`: reducer owns schemas, initial state, reduce, and fold; DCB tags remain outside reducer definitions.
- Aligned with `plan/checks/01-plan-sanity.md`: reducer brand symbol stays private; only factory/types are exported; core reducer module imports no adapters; no `object`, explicit `any`, or `Record<string, unknown>` introduced.

## Follow-ups
- Continue with `impl/02.md` to start converting public query surfaces to reducer definitions.
