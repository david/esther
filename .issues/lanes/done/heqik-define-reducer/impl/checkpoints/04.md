# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-26

## Summary
- Expanded reducer-backed `castTagQuery` runtime coverage to fold selected history, preserve subject binding, and skip event-store reads on absent subjects.
- Added read descriptor coverage proving `eventsByTagsDescriptor(tags, reducer)` stores reducer definitions.
- Added read interpreter coverage proving `eventsByTagsDescriptor` forwards tags and reducer identity to `eventStore.queryByTags` and returns reducer state.
- Strengthened pipeline wiring coverage so reducer-backed `castTagQuery` exposes folded state and subject binding before stale-boundary rejection.

## Files changed
- `src/core/slice.test.ts`: added reducer-backed history fold coverage for `castTagQuery` hit path and absent-path event-store read guard.
- `src/core/read-model.test.ts`: added `eventsByTagsDescriptor(tags, reducer)` descriptor helper coverage.
- `src/core/read-interpreter.test.ts`: added read interpreter forwarding coverage for descriptor tags and reducer identity.
- `src/__tests__/pipeline-wiring.test.ts`: added assertions that reducer-backed cast query resolves state and subject before stale-boundary conflict blocks side effects.
- `.issues/lanes/in-progress/heqik-define-reducer/impl/checkpoints/04.md`: implementation checkpoint for this task.

## Verification
- `bun test src/core/slice.test.ts src/core/read-interpreter.test.ts src/core/read-model.test.ts src/__tests__/pipeline-wiring.test.ts`: pass — 78 tests passed, 197 expectations.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 243 tests passed, 603 expectations.

## Manual verification
- not-applicable — library DSL/read-interpreter behavior has no UI, server route, or CLI workflow; runtime tests are executable QA for this task.

## Drift assessment
- Aligned with `impl/04.md`: `castTagQuery` tests now use reducer definitions, preserve subject lookup success, absent/error behavior, schema-error behavior, `${key}Subject`, and deferred `toStep(deps)` behavior.
- Aligned with `plan/01-implementation-plan.md`: no raw public `schemas + fold` form was reintroduced; `castTagQuery` state is reducer-only while subject remains separately bound.
- Aligned with `plan/checks/01-plan-sanity.md`: empty-schema reducers remain typed with `Record<never, never>` where needed; no `object`, explicit `any`, or `Record<string, unknown>` was added.
- Boundary observation behavior remains covered: reducer-backed cast query records selected tags and stale-boundary append preconditions still reject conflicting writes before output, projections, processors, or effects run.

## Follow-ups
- Continue with `impl/05.md` for final reducer contract polish and gates.
