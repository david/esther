# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-04-26

## Summary
- Expanded reducer-backed `queryByTags` runtime coverage for in-memory, filesystem, and Postgres event stores.
- Added adapter assertions that reducer schemas parse matching events, reducer folds produce state, tag filtering/intersection still works, and `maxPosition` stays unchanged.
- Kept command/query pipeline and append precondition coverage on reducer-backed tag queries green.

## Files changed
- `src/adapters/in-memory/event-store.test.ts`: added reducer-schema parsing and fold-state coverage with coerced payload values and max-position assertion.
- `src/adapters/filesystem/index.test.ts`: replaced generic event-type fold test with issue-title reducer coverage using event payload schemas and max-position assertion.
- `src/adapters/postgres/event-store.test.ts`: added reducer-backed `queryByTags` tests for parsed fold state and tag-intersection behavior.
- `.issues/lanes/in-progress/heqik-define-reducer/impl/checkpoints/03.md`: implementation checkpoint for this task.

## Verification
- `bun test src/adapters/in-memory/event-store.test.ts src/adapters/filesystem/index.test.ts src/adapters/postgres/event-store.test.ts src/__tests__/event-store-append-conformance.ts src/__tests__/pipeline.test.ts src/__tests__/pipeline-wiring.test.ts`: pass — 101 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.

## Manual verification
- not-applicable — library event-store and command/query behavior has no manual UI/CLI workflow; targeted tests are executable QA.

## Drift assessment
- Aligned with `impl/03.md`: all named adapter event-store tests now exercise reducer definitions and explicitly verify parsing, folding, tag filtering/intersection, returned state, and max-position behavior.
- Aligned with `plan/01-implementation-plan.md`: event names, payloads, tags, positions, append ordering, replay semantics, and stale-boundary behavior remain unchanged; only reducer-backed test coverage changed.
- Aligned with `plan/checks/01-plan-sanity.md`: no public raw `schemas + fold` form was reintroduced, no unsafe cast was added, and adapter/core dependency direction remains unchanged.
- Existing command-side and query-side `tagQuery` runtime coverage remains reducer-backed and passing, including stale non-empty and empty boundary precondition tests and read-only query behavior.

## Follow-ups
- Continue with `impl/04.md` for expanded `castTagQuery` and read descriptor runtime coverage.
