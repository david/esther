# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-28

## Summary
- Ran full repo gates after tasks 01–03.
- Confirmed `llms.txt` documents primitive-only `where` grammar and object/array storage-only behavior.
- Removed obsolete non-schema-aware normalization helpers after lint exposed them as dead code.
- Checked implementation scope against approved plan and plan-check watch items.

## Files changed
- `src/core/read-model.ts`: removed dead pre-validation helper code and made Zod kind switch exhaustive for lint.
- `.issues/lanes/in-progress/bs43i-tighten-query-where/impl/checkpoints/04.md`: recorded final gate and drift evidence.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 272 tests passed across 21 files.
- Initial `bun run lint` attempt: fail — obsolete `normalizeWhere` was unused and Zod kind switch was non-exhaustive; fixed before final gates.

## Drift assessment
- Public DSL change stayed within approved `Where<T>` / `WhereClause<V>` grammar tightening.
- Runtime validation stayed core-only and schema-aware for `queryDescriptor(...)` and `defineReadModelQuery(...).buildQuery(args)`.
- `WhereEntry` shape, adapter query APIs, event model, persistence/migrations, auth, `orderBy`, JSONB operators, nested paths, and joins were not changed.
- `defineReadModel` support for `ZodArray` and `ZodObject` storage/projection remains covered by tests.
- `llms.txt` current: documents equality for string/number/boolean, range for string/number, membership for string/number/boolean, and object/array fields as not queryable by `where`.

## Follow-ups
- none
