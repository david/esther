# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-28

## Summary
- Tightened public `Where<T>` grammar to omit non-queryable object/array fields.
- Restricted `WhereRange<V>` to string/number values and `WhereIn<V>` to string/number/boolean values.
- Preserved valid string/number/boolean equality, range, and membership typing, including literal primitive values.
- Documented primitive-only read-model `where` grammar in `llms.txt`.

## Files changed
- `src/core/read-model.ts`: narrowed public `Where*` helper types without changing `WhereEntry` runtime shape.
- `src/__tests__/type-check.ts`: added compile-only positive and negative query grammar assertions.
- `llms.txt`: documented primitive-only `where` support and object/array storage-only behavior.
- `.issues/lanes/in-progress/bs43i-tighten-query-where`: moved issue from backlog to in-progress before implementation.
- `.issues/lanes/in-progress/bs43i-tighten-query-where/impl/checkpoints/01.md`: recorded implementation checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run format`: pass — ran Biome formatting, then reverted unrelated formatting-only changes outside task scope.

## Drift assessment
- Matches `impl/01.md` and `plan/02-implementation-plan.md`: type-level grammar now rejects object/array where clauses and boolean ranges while preserving primitive clauses.
- `WhereEntry` runtime contract unchanged; runtime validation remains for later tasks.
- `defineReadModel` still accepts `z.array(...)` and `z.object(...)` fields through type-check coverage on `searchModel`.
- No scope expansion into runtime validation, adapters, orderBy, JSONB querying, joins, or nested paths.

## Follow-ups
- Continue with `impl/02.md` runtime validation.
