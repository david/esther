# 03 — Reuse schema-aware validation in read-model queries

## Source
- plan/02-implementation-plan.md
- plan/checks/02-plan-sanity.md

## Goal
Make `defineReadModelQuery(...).buildQuery(args)` use the same schema-aware `where` normalization path as `queryDescriptor(...)`, with query/source context in runtime errors.

## Depends on
- 02.md

## Scope
- `src/core/read-model.ts`
- `src/core/read-model.test.ts`
- existing read-interpreter/query tests if focused failures reveal caller expectations

## Contract surfaces
- runtime read-model query constructor: `defineReadModelQuery(...).buildQuery(args)`
- shared runtime normalization helper from task 02
- error surface: thrown `Error` with query name or source read model, field, and reason
- read-model query projection path indirectly consuming `WhereEntry[]`

## Plan-check watch items
- Preserve `undefined` field-entry skipping for conditional filter construction.
- Keep implementation core-only; no core imports from adapters.
- Assert error substrings that prove read model/query name, field, and reason without overfitting whole messages.
- Run drift check if implementation expands into `orderBy`, JSONB operators, nested paths, joins, or adapter SQL semantics.

## Work
- Add or extend `src/core/read-model.test.ts` coverage for `defineReadModelQuery(...).buildQuery(args)` before implementation:
  - existing valid equality/range/`in` query entries stay unchanged.
  - resolver returning unsafe unknown field throws.
  - resolver returning unsafe array/object field primitive-shaped clause throws.
  - resolver returning unsafe boolean range throws.
  - resolver returning unsafe wrong primitive kind / non-primitive `in` values throws.
  - errors identify query name or source read model plus field and reason.
- Update `defineReadModelQuery(...).buildQuery(args)` to call the shared schema-aware normalization helper.
- Pass enough context to error helper to distinguish read model query failures when cheap, without changing public return shape.
- Preserve `orderBy`, `orderDirection`, and `limit` behavior exactly.
- Do not change adapter query API or `WhereEntry` shape.
- Run existing read-interpreter tests only if this task changes descriptor shape or failing focused tests point there.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `defineReadModelQuery(...).buildQuery(args)` emits same `WhereEntry[]`, `orderBy`, `orderDirection`, and `limit` for valid primitive filters.
- `buildQuery(args)` fails fast for unsupported unsafe-cast clauses through the same schema-aware rules as `queryDescriptor(...)`.
- Runtime errors include enough context to identify query/source, field, and reason.
- Adapter code remains unchanged unless a focused failure proves an adapter-side contract break.

## Suggested tests
- Runtime tests in `src/core/read-model.test.ts` should fail before implementation.
- Reuse or share fixtures with task 02 only when it keeps test intent clear.

## Manual verification
- not applicable — runtime library query-builder behavior; automated unit tests cover observable behavior.

## Verification
- `bun test src/core/read-model.test.ts`
- `bun test src/core/read-interpreter.test.ts`
- `bun run typecheck`
