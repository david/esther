# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-05-01

## Summary
- Added required-`outputErr` definition-backed overloads for `commandDefinition(...)` and `defineCommand(...)`.
- Added CMS-shaped generic type fixture that calls both helpers inside generic wrapper boundary and checks name/event typing.
- Updated `llms.txt` guidance for direct required-`outputErr` descriptor acceptance.

## Files changed
- `src/core/slice.ts`: added public overloads and broadened runtime-compatible command definition union without changing runtime body.
- `src/__tests__/type-check.ts`: added generic wrapper assertions for descriptor identity, named/unnamed commands, event candidate typing, and parsed event output typing.
- `llms.txt`: documented direct `defineCommand(descriptor)` / `commandDefinition(descriptor)` support for required-`outputErr` descriptors.
- `.issues/lanes/in-progress/ovl0d-command-outputerr-overloads/`: moved issue from backlog to in-progress and recorded checkpoint.

## Verification
- `bun run typecheck`: pass — TypeScript public API/type fixture compiles.
- `bun run lint`: pass — ESLint and dependency-cruiser pass.
- `bun run test`: pass — 284 tests pass.

## Manual verification
- type-level library API change: not-applicable — task defines automated CLI verification only.

## Drift assessment
- Matches task and approved plan: required-outputErr descriptors now accepted directly by public helpers.
- `descriptorIdentityForWrappers`: `commandDefinition(descriptor)` preserves exact CMS descriptor intersection including literal `name`.
- `preserveNamedCommandIdentity`: named overload returns command with `TName`; unnamed path returns `string` name.
- `preserveDefinitionBackedEventContract`: overloads return `EventOf<TEventDefinition>` output and `EventCandidateOf<TEventDefinition>` event candidate; runtime event-schema branch unchanged.
- `conditionalOutputErrBypass`: required-outputErr overloads appear before conditional `DefinitionBackedCommandDefinition` overloads.
- No runtime semantics, persistence, replay, read model, adapter, processor, or auth policy changed.

## Follow-ups
- none
