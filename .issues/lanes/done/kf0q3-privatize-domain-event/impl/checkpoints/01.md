# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-28

## Summary
- Added `EventRecordInput` as low-level structural append/input event shape while keeping temporary `DomainEvent` compatibility.
- Retargeted `EventStore.append`, event definition internals, command/pipeline bounds, filesystem store helpers, and store conformance fixtures to `EventRecordInput`.
- Added type-level root `EventRecordInput` coverage for custom `EventStore.append` use.

## Files changed
- `src/core/types.ts`: added `EventRecordInput`, kept `DomainEvent` as compatibility alias, retargeted `StoredEvent`.
- `src/core/event.ts`: made `EventDefinition.create` and `EventOf` use `EventRecordInput` internally.
- `src/core/event-store.ts`: changed append contract to `ReadonlyArray<EventRecordInput>`.
- `src/core/slice.ts`: changed command event bounds to `EventRecordInput`.
- `src/core/pipeline.ts`: changed command execution bounds to `EventRecordInput`.
- `src/index.ts`: root-exported `EventRecordInput` while retaining `DomainEvent` for later removal.
- `src/adapters/filesystem/index.ts`: retargeted append validation/planning helpers to `EventRecordInput`.
- `src/__tests__/event-store-append-conformance.ts`: retargeted low-level append fixture to `EventRecordInput`.
- `src/adapters/in-memory/event-store.test.ts`: retargeted store append helper to `EventRecordInput`.
- `src/adapters/filesystem/index.test.ts`: retargeted store append helper to `EventRecordInput`.
- `src/adapters/postgres/event-store.test.ts`: retargeted store append helper to `EventRecordInput`.
- `src/__tests__/type-check.ts`: added root `EventRecordInput` import and custom append input check.

## Verification
- `bun run typecheck`: pass — tsgo completed with no errors.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 259 tests passed, 0 failed.

## Drift assessment
- aligned with `impl/01.md` and `plan/02-implementation-plan.md`.
- Runtime event wire shape unchanged: `{ type, tags, payload }`.
- Stored event fields preserved: `type`, `tags`, `payload`, `id`, `position`, `timestamp`.
- Root `DomainEvent` removal, app-like test conversion, and `llms.txt` guidance intentionally left for later tasks.

## Follow-ups
- none for task 01.
