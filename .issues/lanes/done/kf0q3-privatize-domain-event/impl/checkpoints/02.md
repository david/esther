# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-28

## Summary
- Converted app-like event type aliases in type and pipeline tests from raw `DomainEvent<...>` to `defineEvent(...)` plus `EventOf<typeof ...>`.
- Updated command event constructors in affected pipeline tests to use event definitions for authored events.
- Kept low-level `EventRecordInput` usage confined to existing store interop type checks from task 01.

## Files changed
- `src/__tests__/type-check.ts`: replaced app-like `DomainEvent` aliases with `EventOf` from event definitions and used `.create(...)` in command event callbacks.
- `src/__tests__/pipeline.test.ts`: introduced event definitions for account events, converted command generics to `EventOf`, and used `.create(...)` for authored command events.
- `src/__tests__/pipeline-wiring.test.ts`: introduced probe event definition and derived `ProbeEvent` from `EventOf`.

## Verification
- `bun run typecheck`: pass — tsgo completed with no errors.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 259 tests passed, 0 failed.

## Drift assessment
- aligned with `impl/02.md` and `plan/02-implementation-plan.md`.
- Event names, tags, payload fields, reducer behavior, command outputs, and pipeline runtime behavior unchanged.
- Root `DomainEvent` export intentionally retained for task 03.
- `llms.txt` intentionally left for task 03.

## Follow-ups
- none for task 02.
