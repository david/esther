# 03 — Remove root DomainEvent and finalize guidance

## Source
- plan/02-implementation-plan.md

## Goal
Remove `DomainEvent` from root public API, make typecheck prove root access is unavailable, and update `llms.txt` so app guidance uses `defineEvent(...)`/`EventOf` while `EventRecordInput` appears only as low-level store interop.

## Depends on
- 02.md

## Scope
- `src/index.ts`
- `src/__tests__/type-check.ts`
- `llms.txt`
- residual `DomainEvent` imports/usages found by `rg` under `src/` and docs

## Contract surfaces
- root `esther` type exports remove `DomainEvent`
- root `esther` type exports keep `EventRecordInput` for custom `EventStore`/adapter append input
- `llms.txt` public export block and examples stop listing/recommending `DomainEvent`
- `EventRecordInput` docs/examples are explicitly low-level store/adapter interop only
- no persisted event, reducer, projector, processor, migration, replay, auth, or side-effect behavior changes

## Plan-check watch items
- Keep `EventRecordInput` docs/examples out of app event-authoring sections; use it only for custom store/adapter interop.
- Make root `DomainEvent` absence fail at typecheck, not just disappear from imports.
- Ensure `EventStore.append` public type and custom-store example compile with root `EventRecordInput`.
- Update `llms.txt` in same slice as API changes.
- Run full gates: `bun run typecheck`, `bun run lint`, `bun run test`.

## Work
- Add/update type-level failing assertion first: root `DomainEvent` import/access from `esther` must produce `@ts-expect-error`.
- Remove `DomainEvent` from `src/index.ts` root type exports.
- Ensure root `EventRecordInput` export remains available and custom-store append example compiles.
- Search for residual app-facing `DomainEvent` references under `src/` and remove or confine any non-root internal compatibility alias to non-public internals only if still necessary.
- Update `llms.txt` public exports, event authoring examples, and checklist language to use `defineEvent(...)` plus `EventOf` for app code.
- Add/keep `llms.txt` custom-store guidance showing `EventRecordInput` only for low-level `EventStore.append` interop.
- Verify docs do not recommend raw event hand-building for app code.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `DomainEvent` is not exported from `src/index.ts` root public API.
- `src/__tests__/type-check.ts` asserts root `DomainEvent` is unavailable.
- `EventRecordInput` remains root-exported and usable for low-level custom-store append input.
- `llms.txt` no longer lists or recommends root `DomainEvent`.
- App examples in `llms.txt` use `defineEvent(...)` and `EventOf<typeof Definition>`.
- Any `EventRecordInput` mention in `llms.txt` is explicitly low-level store/adapter interop.
- Full gates pass.

## Suggested tests
- Add `@ts-expect-error` coverage for root `DomainEvent` before removing the export.
- Keep/verify custom `EventStore.append` type example with root `EventRecordInput`.
- Use `rg "DomainEvent" src llms.txt` to confirm no stale public/app-facing usage remains.

## Manual verification
- not applicable — library API/type/docs change only; no UI/API workflow to manually exercise.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
