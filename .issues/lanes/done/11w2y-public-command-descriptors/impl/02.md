# 02 — Prove reusable definition-backed wrapper inference

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Make extension-style wrappers compose public command descriptor types without casts through `unknown`, while preserving input enrichment, `outputErr`, schema-input payloads, and parsed output events.

## Depends on
- 01.md

## Scope
- `src/__tests__/type-check.ts`
- `src/core/slice.ts` if inference fixes are needed
- `src/core/event.ts` if helper generic constraints need adjustment

## Contract surfaces
- public wrapper constraint: `T extends AnyCommandDefinition`
- `DefinitionBackedCommandDefinition` inference for input pipeline context
- `outputErr` handler typing
- `EventPayloadInputOf` and `EventOf` helper distinction

## Intent / decision vocabulary
- reusable wrapper constraint
- `AnyCommandDefinition`
- `DefinitionBackedCommandDefinition`
- input composition / enriched `TCtx`
- output error handler typing
- schema-input payload candidate vs validated output event

## Plan-check watch items
- Define `AnyCommandDefinition` generics narrowly enough to preserve inference; avoid `unknown`/wide union erasing `TCtx`, `TError`, `TInputError`, or `TEventDefinition`.
- Preserve existing local cast comments if overload normalization still needs casts; do not add broad wrapper-facing casts.
- When updating `llms.txt`, mention `payload(ctx)`/`command.event(ctx)` uses schema input while `output(event, ctx)` receives parsed `EventOf<typeof Event>`.

## Work
- Add failing type-level wrapper fixture that accepts `DefinitionBackedCommandDefinition` through `T extends AnyCommandDefinition` and returns the original descriptor type.
- Add type-level coverage where wrapper composes or forwards `input` and enriched `TCtx` is visible to `tags`, `payload`, `validate`, and `output`.
- Add `outputErr` wrapper coverage proving merged/forwarded error handler typing survives.
- Add bad-payload-field negative assertion for definition-backed command descriptors.
- Add transform-schema coverage proving `payload(ctx)` and command event candidates use schema input while `output(event, ctx)` receives `EventOf<typeof Event>` / schema output.
- Tighten descriptor generics or overloads only as needed to make these wrapper tests pass without `unknown` casts in wrapper-facing APIs.

## Implementation checkpoint
- write `impl/checkpoints/02.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Public wrapper accepts `DefinitionBackedCommandDefinition` without copying private shapes or casting through `unknown`.
- Wrapper composition preserves enriched context for `tags`, `payload`, `validate`, and `output`.
- `outputErr` typing is preserved when wrapper forwards or merges handlers.
- Bad definition-backed payload field fails typecheck.
- Transform-schema test proves `payload(ctx)`/candidate payload is schema input and `output(event, ctx)` is parsed output.

## Suggested tests
- Extend `src/__tests__/type-check.ts` with positive assertions and `@ts-expect-error` negative cases before fixing inference.

## Manual verification
- not applicable — wrapper behavior is compile-time type inference; automated typecheck is direct verification.

## Verification
- `bun run typecheck`
