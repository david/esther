# 03 — Guard command runtime validation boundaries

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Prove public descriptor cleanup did not change command execution: definition-backed commands still validate event candidates before append/fanout, and raw commands remain raw.

## Depends on
- 01.md

## Scope
- `src/__tests__/pipeline-wiring.test.ts`
- `src/core/slice.ts` if runtime normalization regressed
- `src/core/pipeline.ts` only if tests reveal existing validation path no longer matches plan

## Contract surfaces
- `commandDefinition(...)` runtime identity
- `eventSchema = eventDefinition.schema` for definition-backed commands
- malformed definition-backed event candidate validation before append
- raw event command path has no event-definition validation
- projector/processor/effect/output fanout blocked after malformed candidate

## Intent / decision vocabulary
- `commandDefinition` identity builder
- `validatedCommandEvent` behavior
- definition-backed command candidate validation
- raw command path remains raw
- no downstream fanout on invalid candidate

## Plan-check watch items
- Keep public `commandDefinition(...)` pure identity; no cloning/normalization/validation.
- Preserve existing local cast comments if overload normalization still needs casts; do not add broad wrapper-facing casts.
- Run `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput"` after implementation and classify any remaining hits as intentional internal names or fix stale docs/types.

## Work
- Add or strengthen runtime test that `commandDefinition(definition)` returns the exact same object identity.
- Add or strengthen malformed definition-backed event candidate test: command builds `{ type, tags, payload }`, `eventSchema` rejects it, and event is not appended.
- Ensure same malformed-candidate test proves no output, projector, processor, effect, or read-model fanout runs after validation failure where existing harness supports those assertions.
- Add or strengthen raw event command regression test proving raw path remains unchanged and unvalidated by event definitions.
- Fix implementation only if these tests expose behavior drift from plan.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `commandDefinition` has identity runtime behavior.
- Definition-backed commands still set `eventSchema` from `eventDefinition.schema` and malformed candidates fail before append.
- Invalid definition-backed candidate causes no append and no downstream fanout.
- Raw event command path remains unchanged and unvalidated by event definitions.
- No serialized event shape, migration, replay, auth, read-model, or side-effect behavior changes are introduced.

## Suggested tests
- Add or update runtime coverage in `src/__tests__/pipeline-wiring.test.ts` before implementation fixes where practical.

## Manual verification
- not applicable — runtime library invariant has deterministic automated tests and no UI/API workflow beyond framework command execution.

## Verification
- `bun run test`
- `bun run typecheck`
