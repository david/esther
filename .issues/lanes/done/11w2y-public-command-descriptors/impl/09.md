# 09 — Guard outputErr runtime and event validation invariants

## Source
- plan/02-wrapper-safe-outputerr-plan.md
- plan/checks/02-wrapper-safe-outputerr-plan-sanity.md

## Goal

Add runtime coverage proving merged outputErr handlers route by error `type`, wrapped definition-backed commands still reject malformed event candidates before append/fanout, and raw command behavior remains unchanged.

## Depends on
- 07.md
- 08.md

## Scope
- `src/__tests__/pipeline-wiring.test.ts` or focused core command/pipeline tests
- `src/core/slice.ts` and `src/core/pipeline.ts` only if tests reveal invariant drift
- `llms.txt` only if runtime behavior documentation needs correction

## Contract surfaces
- runtime command outputErr normalization and handler routing
- definition-backed event candidate validation before append/fanout
- raw command path unchanged
- side effects/processors/fanout: unchanged, no new I/O

## Intent / decision vocabulary
- `mergeOutputErrHandlers`
- `preserveDefinitionBackedEventValidation`
- added-error handler routing
- base-error handler routing
- raw command path unchanged

## Plan-check watch items
- Definition-backed wrapper must not accidentally return raw descriptor or skip `eventSchema` path.
- Runtime tests should avoid duplicate error `type` names; collision precedence is documented, but duplicate type names are app error-design smell.
- Helper must not execute effects or perform I/O; it only constructs handler maps.
- Raw-event command path remains unchanged if helper/export edits touched shared command code.

## Work
- Add or update runtime tests so `mergeOutputErrHandlers(baseHandlers, addedHandlers)` routes a base error to the base handler after `defineCommand(...)` normalization.
- Add or update runtime tests so merged handlers route an added wrapper error to the added handler.
- Add or update runtime tests so `mergeOutputErrHandlers(undefined, addedHandlers)` works for a base command with no original domain errors after the wrapper widens the error union.
- Add or update runtime test for wrapped definition-backed command rejecting malformed event candidate with `SchemaError("Event validation failed", issues)` before append/fanout.
- Add or keep a focused regression check that raw command path behavior is unchanged if shared command compilation code was touched.
- Fix only failures directly tied to helper/export changes; stop and report if broader command runtime semantics need change.

## Implementation checkpoint
- write `impl/checkpoints/09.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Runtime tests prove base and added error handlers both route by `type` through existing command execution.
- Runtime tests prove `undefined` base handlers plus required added handlers work after error-union widening.
- Wrapped definition-backed malformed event candidate is rejected before append/fanout and still reports schema validation failure.
- Raw command path remains unchanged.
- Full project gates pass.

## Suggested tests
- Add failing runtime tests first in `src/__tests__/pipeline-wiring.test.ts` or focused core command/pipeline tests.
- Run focused Bun test for changed runtime test file during iteration.
- Run full gates before checkpoint.

## Manual verification
- not applicable — automated runtime/type/lint coverage fully exercises library API and pipeline invariants; no UI, CLI workflow, or external integration.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
