# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-30

## Summary
- Added public event candidate/input helper types.
- Renamed raw command descriptor public API to `RawCommandDefinition`.
- Promoted definition-backed command descriptor to `DefinitionBackedCommandDefinition`.
- Added public `AnyCommandDefinition` and identity `commandDefinition(...)`.
- Updated `defineCommand(...)` overloads and root exports to use public descriptor names.
- Added type-check coverage for new root exports, removed root `CommandDefinition`, event input/candidate helpers, and descriptor identity typing.

## Files changed
- `src/core/event.ts`: added `EventPayloadInputOf` and `EventCandidateOf`.
- `src/core/slice.ts`: added public command descriptor contract and identity helper; replaced private descriptor shadow in overloads.
- `src/index.ts`: exported new public descriptor and event helper names; removed root `CommandDefinition` export.
- `src/__tests__/type-check.ts`: added compile-only coverage for exports, helper types, descriptor identity, and removed root export.
- `.issues/lanes/backlog/11w2y-public-command-descriptors/**` -> `.issues/lanes/in-progress/11w2y-public-command-descriptors/**`: moved issue to in-progress before implementation.

## Verification
- `bun run typecheck`: pass — public API/type assertions compile.
- `bun run lint:code`: pass — ESLint code checks pass for `src`.

## Drift assessment
- Aligned with task 01 and approved plan: runtime command path unchanged; definition-backed overloads consume public `DefinitionBackedCommandDefinition`; raw descriptor is public `RawCommandDefinition`; event candidate/input helpers are public; root `CommandDefinition` is absent.
- `AnyCommandDefinition` uses broad structural raw/definition-backed descriptor shapes so `T extends AnyCommandDefinition` preserves exact inferred descriptor type through `commandDefinition(...)`; narrower wrapper inference is covered by task 02.
- `llms.txt` and stale-name sweep remain for task 04 by design.

## Follow-ups
- Continue with task 02 wrapper inference coverage.
