# 09 — Implementation checkpoint

Status: aligned
Task: impl/09.md
Date: 2026-05-01

## Summary
- Added runtime coverage for `mergeOutputErrHandlers(...)` routing base and added error types after `defineCommand(...)` normalization.
- Added runtime coverage for `mergeOutputErrHandlers(undefined, addedHandlers)` when wrapper-only errors widen a no-error base command.
- Added wrapped definition-backed malformed event candidate regression proving `SchemaError("Event validation failed", issues)` occurs before append, output, projection fanout, processor fanout, or effects.
- Kept raw command path coverage unchanged; existing raw command regression still proves raw event factories do not get definition-backed schema validation.

## Files changed
- `src/__tests__/pipeline-wiring.test.ts`: added runtime tests for merged `outputErr` routing, undefined base handlers, and wrapped definition-backed event validation invariants.
- `.issues/lanes/in-progress/11w2y-public-command-descriptors/impl/checkpoints/09.md`: recorded task result and verification evidence.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 31 tests, 0 fail; new outputErr merge and wrapped event validation cases pass.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.
- `bun run test`: pass — 284 tests, 0 fail.
- `rg "DefinitionBackedCommandDefinitionWithOutputErr|mergeOutputErrHandlers|as unknown as" src llms.txt`: pass — new runtime tests reference `mergeOutputErrHandlers`; `as unknown as` hits are intentional malformed-event/output-shape test casts or pre-existing bounded framework/storage casts.

## Manual verification
- not applicable — automated runtime/type/lint coverage fully exercises library API and pipeline invariants; no UI, CLI workflow, or external integration.

## Drift assessment
- Aligned with `impl/09.md`, `plan/02-wrapper-safe-outputerr-plan.md`, and `plan/checks/02-wrapper-safe-outputerr-plan-sanity.md`.
- `mergeOutputErrHandlers(baseHandlers, addedHandlers)` now has runtime proof for base-error handler routing and added-error handler routing by unique `type` names through existing command execution.
- `mergeOutputErrHandlers(undefined, addedHandlers)` now has runtime proof for wrapper-only error handling when base command had no original domain errors.
- `preserveDefinitionBackedEventValidation` now has wrapped-command runtime proof: wrapper metadata preserves `eventSchema`, malformed candidate returns `SchemaError("Event validation failed", issues)`, and append/fanout/output/effects remain skipped.
- Raw command behavior remains covered by existing focused regression; no core command or pipeline code changed, and no `llms.txt` correction was needed.

## Follow-ups
- none
