# 04 — Update command descriptor docs and run gates

## Source
- plan/01-implementation-plan.md
- plan/checks/01-plan-sanity.md

## Goal

Update public documentation for the new command descriptor API, remove stale ambiguous names, and run repo gates for the whole change.

## Depends on
- 01.md
- 02.md
- 03.md

## Scope
- `llms.txt`
- `src/index.ts` if export docs reveal drift
- `src/core/slice.ts` / `src/core/event.ts` only for stale-name cleanup discovered by final search
- test files only for final expectation cleanup discovered by gates

## Contract surfaces
- public API documentation and canonical examples
- root export list drift
- stale private/public descriptor names
- repo verification gates

## Intent / decision vocabulary
- `RawCommandDefinition`
- `DefinitionBackedCommandDefinition`
- `AnyCommandDefinition`
- `commandDefinition`
- `EventPayloadInputOf`
- `EventCandidateOf`
- payload schema input vs parsed event output
- intentional removal of public `CommandDefinition`

## Plan-check watch items
- Negative root-export test for removed `CommandDefinition` should prove absence from package root, not only unused local type behavior.
- When updating `llms.txt`, mention `payload(ctx)`/`command.event(ctx)` uses schema input while `output(event, ctx)` receives parsed `EventOf<typeof Event>`.
- Run `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput"` after implementation and classify any remaining hits as intentional internal names or fix stale docs/types.

## Work
- Update `llms.txt` public export list and command DSL guidance for `RawCommandDefinition`, `DefinitionBackedCommandDefinition`, `AnyCommandDefinition`, `commandDefinition`, `EventPayloadInputOf`, and `EventCandidateOf`.
- Document migration guidance: use `RawCommandDefinition` for raw event factory descriptors and `DefinitionBackedCommandDefinition` for `event: EventDefinition` descriptors; do not keep `CommandDefinition` alias.
- Document schema-input vs schema-output distinction: `payload(ctx)` and command event candidates use schema input; `output(event, ctx)` receives parsed `EventOf<typeof Event>`.
- Run stale-name search and fix stale docs/types, or record intentional remaining internal/runtime mentions in checkpoint.
- Run full repo gates and fix failures caused by this issue.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` describes new public descriptor names and helper types.
- `llms.txt` no longer recommends ambiguous public `CommandDefinition`.
- Docs explain schema-input candidates vs validated output events.
- Stale-name search has no unintended hits for removed/private names.
- Whole repo `typecheck`, `lint`, and `test` pass.

## Suggested tests
- No new standalone tests required unless gates reveal drift; this task verifies and fixes documentation/export surface after earlier test slices.

## Manual verification
- not applicable — QA contract is automated gates only for library API/docs.

## Verification
- `rg "CommandDefinition|EventDefinitionCommandDefinition|CommandEventCandidate|DefinitionBackedCommandPayloadInput"`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
