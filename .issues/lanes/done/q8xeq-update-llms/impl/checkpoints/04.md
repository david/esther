# 04 — Implementation checkpoint

Status: minor-local-drift
Task: impl/04.md
Date: 2026-04-27

## Summary
- Clarified `llms.txt` full example so `transfer-money` models only the source-account debit leg.
- Added text that the command emits one `MoneyDebited` event and target-account credit is produced by another command/process not shown.
- Added an inline comment explaining `MoneyCredited` is reducer input for credits produced by another flow, not by this debit command.
- Added repo guidance to keep `llms.txt` current when public API, DSL behavior, adapter usage, errors, or canonical examples change.

## Files changed
- `llms.txt`: docs-only clarification for transfer debit/credit semantics in full example.
- `AGENTS.md`: agent-facing gotcha to keep `llms.txt` current with public API and canonical example changes.
- `doc/project-management.md`: workflow/ready-to-merge guidance requiring `llms.txt` updates or explicit no-update rationale when public surfaces change.
- `.issues/lanes/in-progress/q8xeq-update-llms/impl/checkpoints/04.md`: implementation checkpoint.

## Verification
- `rg -n "Full example|transfer-money|MoneyCredited|MoneyDebited|counterpartyAccountId|target|credit|debit|tagQuery" llms.txt`: pass — full example now states source-account debit leg only, one `MoneyDebited` event, and target-account credit produced by another command/process not shown.
- `rg -n "createFastifyAdapter|projectors:|processors:" llms.txt || true`: pass with note — no `createFastifyAdapter`; no `projectors:`; one `processors: [sendOrderEmail]` remains as valid app-level `createApp` wiring.
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt`: pass — no `fold:`; no raw `eventsByTagsDescriptor` / `queryByTags`; `schemas:` remains only inside `defineReducer(...)` snippets.
- `rg -n "defineEvent|defineReducer|createFastifyInputAdapter|defineFastifyRoutes|readModelEvent|defineProcessor|processorEvent|BoundaryObservationError|ConcurrencyError" llms.txt`: pass — current API names all present.
- `rg -n "llms\.txt|public API|DSL behavior|adapter usage|canonical examples" AGENTS.md doc/project-management.md`: pass — upkeep guidance present in both docs.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 255 tests passed, 0 failed.

## Manual verification
- Full example heading through closing code block: pass — reader can tell exactly that `transfer-money` emits one `MoneyDebited` event for the source account and does not show target-account credit production.
- Regression check for one-event command semantics and reducer-backed `tagQuery`: pass — command still emits one event directly and uses reducer-backed `tagQuery` for `fromBalance`.
- `AGENTS.md` gotchas and `doc/project-management.md` workflow/readiness sections: pass — future public surface changes now have an explicit `llms.txt` upkeep rule without adding process bloat.
- Compactness check: pass — fix adds one sentence, one code comment, and two short process-doc bullets.

## Drift assessment
- Minor local drift from `impl/04.md`: user requested an additional process-doc update after the task edit; this is low-risk docs-only scope tied to keeping `llms.txt` current.
- Aligned with `review/findings/02-transfer-example-credit-counterpart.md`: full example no longer implies complete transfer behavior without target credit path.
- Aligned with `plan/02-implementation-plan.md` and `plan/checks/02-revised-plan-sanity.md`: docs-only, compact, no runtime/API behavior change, no multi-event command guidance.
- `MoneyCredited` remains as reducer input and is now explained as produced by another flow.
- No source API, runtime behavior, persistence, replay, auth, side-effect, or adapter changes introduced.

## Follow-ups
- none
