# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-27

## Summary
- Updated `llms.txt` event-history docs to prefer `defineEvent(...)` and `defineReducer(...)`.
- Replaced public command/query/full-example raw `schemas + fold` history-read snippets with reducer-backed `tagQuery(...)` / `castTagQuery(...)` snippets.
- Added compact DCB append precondition note and `ConcurrencyError` / `BoundaryObservationError` error coverage.

## Files changed
- `llms.txt`: event, reducer, event-history, DCB, error, and full-example documentation updates.
- `.issues/lanes/in-progress/q8xeq-update-llms/impl/checkpoints/01.md`: implementation checkpoint.
- `.issues/lanes/in-progress/q8xeq-update-llms/**`: moved issue from backlog to in-progress before implementation.

## Verification
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt || true`: pass — no `fold:` remains; `schemas:` appears only in `defineReducer(...)` snippets; no raw positional `eventsByTagsDescriptor(...)` / `queryByTags(...)` examples present.
- `rg -n "defineEvent|defineReducer|BoundaryObservationError|ConcurrencyError" llms.txt`: pass — current helper and error names present.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser completed with no violations.
- `bun run test`: pass — 255 tests passed, 0 failed.

## Manual verification
- Events, Command DSL, Query state pipeline, DCB/precondition note, Errors, Rules, and Full example in `llms.txt`: pass — snippets are compact, internally consistent, imports cover current event/reducer helpers, and touched public event-history docs no longer teach raw `schemas + fold` APIs.
- Regression check for remaining app/Fastify/projector/processor/read-model drift: pass — stale sections still present only as known `impl/02.md` scope; task 01 did not make them worse.

## Drift assessment
- Aligned with `impl/01.md`, `plan/02-implementation-plan.md`, `plan/checks/02-revised-plan-sanity.md`, and `research/01-current-state.md`.
- `schemas:` remains only inside `defineReducer(...)` examples, matching watch item.
- No source API, runtime behavior, persistence, replay, auth, or adapter changes introduced.

## Follow-ups
- Continue with `impl/02.md` to update app wiring, read-model, projector/processor, and adapter docs.
