# 04 — Clarify transfer credit counterpart

## Source
- review/findings/02-transfer-example-credit-counterpart.md

## Goal

Make the `llms.txt` full example internally consistent about account-transfer semantics while preserving the current one-event command contract. The example must not teach a complete money transfer that only debits the source account.

## Depends on
- 03.md

## Scope
- `llms.txt` full example around `transfer-money`, `MoneyCredited`, `MoneyDebited`, `accountBalanceReducer`, and `transferSlice`
- Source cross-checks in `src/__tests__/type-check.ts` and `src/index.ts` only if snippet helper names or contracts need confirmation

## Contract surfaces
- Documentation contract: the full example's domain meaning and event-production story
- Command contract: `defineCommand` emits one event directly
- Event/reducer docs: `MoneyCredited` remains a reducer input only if docs explain who can produce it outside the shown command
- Runtime/source behavior: none

## Plan-check watch items
- Keep `llms.txt` compact; avoid full tutorial creep.
- Cross-check representative snippets against `src/__tests__/type-check.ts`, `src/index.ts`, and `src/adapters/fastify/index.ts` when helper shapes are touched.
- Do not imply `defineEvent` changes serialized event shape.
- Do not introduce examples with direct I/O in slices, read models, read-model event bindings, or processors.
- Record focused stale/current API searches plus full `typecheck`, `lint`, and `test` results in implementation checkpoint.

## Work
- Inspect the `llms.txt` full example and confirm current mismatch: `accountBalanceReducer` handles `MoneyCredited`, but `transferSlice` emits only `MoneyDebited` for `fromAccountId`.
- Apply the smallest docs-only fix from the source finding: add concise text near the full example stating the shown command models the debit leg only, and a target-account credit must be produced by another command/process not shown; do not present it as a complete two-account transfer.
- If clearer with less ambiguity, rename/reframe nearby prose to source debit / withdrawal instead, but keep the command one-event contract and reducer-backed `tagQuery` teaching point intact.
- Do not add framework behavior, multi-event command guidance, adapter I/O, or new runtime APIs.
- Keep snippet imports, event names, reducer use, and output types internally consistent after the wording change.
- Run focused searches and full repo gates listed below.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` no longer suggests the shown `transfer-money` command is a complete two-account transfer with no visible target-account credit path.
- Docs explicitly state the shown command emits one debit event, and target credit is produced elsewhere/not shown, or the example is reframed so one emitted event fully matches the domain behavior.
- `MoneyCredited` is either explained as an event produced by another flow or removed/renamed as part of a consistent single-subject example.
- The example still demonstrates `defineEvent`, `defineReducer`, command-side reducer-backed `tagQuery`, validation, one emitted event, and output mapping.
- No source/runtime behavior changes are made.

## Suggested tests
- No new runtime tests expected; this is docs-only.
- Before finishing, use focused searches to inspect `transfer-money`, `MoneyCredited`, `MoneyDebited`, `counterpartyAccountId`, and `tagQuery` wording.
- If implementation changes compiled examples or source contracts, add/update focused type-level coverage and report high-risk drift.

## Manual verification
- Read the `llms.txt` full example from heading through closing code block.
- Confirm a reader can tell exactly what event the command emits and why the target account credit is not shown in that command.
- Regression check: confirm the same example still teaches one-event command semantics and reducer-backed `tagQuery`, not multi-event command output.

## Verification
- `rg -n "Full example|transfer-money|MoneyCredited|MoneyDebited|counterpartyAccountId|target|credit|debit|tagQuery" llms.txt`
- `rg -n "createFastifyAdapter|projectors:|processors:" llms.txt || true`
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt`
- `rg -n "defineEvent|defineReducer|createFastifyInputAdapter|defineFastifyRoutes|readModelEvent|defineProcessor|processorEvent|BoundaryObservationError|ConcurrencyError" llms.txt`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
