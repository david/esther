# 01 — Update event-history docs to reducer API

## Source
- plan/02-implementation-plan.md
- plan/checks/02-revised-plan-sanity.md
- research/01-current-state.md

## Goal

Make `llms.txt` teach current event definition and event-history read contracts. Replace stale raw `schemas + fold` command/query examples with `defineEvent(...)` and `defineReducer(...)` examples, including the full example.

## Depends on
- none

## Scope
- `llms.txt` imports and Events section
- `llms.txt` Command DSL section
- `llms.txt` Query state pipeline section
- `llms.txt` Full example section
- Representative source cross-checks in `src/index.ts`, `src/core/event.ts`, `src/core/reducer.ts`, `src/core/slice.ts`, `src/core/pipeline.ts`, `src/core/event-store.ts`, `src/core/types.ts`, and `src/__tests__/type-check.ts`

## Contract surfaces
- event helper docs: `defineEvent`, `.schema`, `.create(...)`, unchanged `{ type, tags, payload }` wire shape
- reducer docs: `defineReducer({ name, schemas, initial, reduce })`
- event-history descriptors: `tagQuery({ key, tags, reducer })`, `castTagQuery({ key, cast, tags, reducer })`, `eventsByTagsDescriptor(tags, reducer)`, `eventStore.queryByTags(tags, reducer)`
- command DCB behavior: one observation derives append precondition, multiple observations fail with `BoundaryObservationError`
- errors mentioned by event-history docs: `ConcurrencyError`, `BoundaryObservationError`

## Plan-check watch items
- Keep `llms.txt` compact; avoid full tutorial creep.
- Cross-check representative snippets against `src/__tests__/type-check.ts`, `src/index.ts`, and current core files.
- Ensure stale-search checks catch positional raw reducer APIs, not only object-literal `schemas:` / `fold:` tokens.
- Ensure `schemas:` that remain are only inside `defineReducer(...)`, not public `tagQuery` / `castTagQuery` config.
- Do not imply `defineEvent` changes serialized event shape.
- Record focused stale/current API searches plus full `typecheck`, `lint`, and `test` results in implementation checkpoint.

## Work
- Cross-check current event/reducer/history-read examples against `src/__tests__/type-check.ts`, `src/index.ts`, and relevant core files before editing snippets.
- Update imports so event/reducer examples use current public root exports.
- Rewrite Events section to prefer `defineEvent(...)`; keep raw `DomainEvent<...>` only as advanced type interop if still useful.
- Rewrite command `tagQuery(...)` examples to pass a reducer definition, not `schemas` and `fold` directly.
- Rewrite `castTagQuery(...)` docs so reducer state binds under `key` and lookup subject binds under ``${key}Subject``; move subject-dependent logic out of reducer examples.
- Rewrite query `tagQuery(...)` examples to use `defineReducer(...)`.
- Add compact DCB/precondition note for command-side event-history reads and direct append option semantics.
- Rewrite full example to use `defineEvent(...)`, `defineReducer(...)`, and reducer-backed `tagQuery(...)`.
- Run focused stale/current API searches and full repo gates listed below.

## Implementation checkpoint
- write `impl/checkpoints/01.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `llms.txt` uses `defineEvent(...)` as primary event definition pattern.
- Public event-history examples in command, query, and full-example sections use reducer-backed APIs.
- No raw public `tagQuery({ schemas, fold })` or `castTagQuery({ schemas, fold })` example remains in the sections touched by this task.
- Any remaining `schemas:` token is inside `defineReducer(...)` or clearly non-event-history context.
- `castTagQuery` docs state subject binds under ``${key}Subject`` and reducer state binds under `key`.
- DCB docs mention single observation append preconditions, multiple observation `BoundaryObservationError`, and `ConcurrencyError` in `SliceError` context.
- Event docs do not imply `defineEvent` changes serialized event shape.

## Suggested tests
- No new runtime tests expected; this is docs-only.
- Before finishing, use the focused searches to confirm stale event-history API forms are absent and current helper names appear.
- If examples are copied from or mirrored in type-check fixtures, cross-check against `src/__tests__/type-check.ts` before finalizing.

## Manual verification
- Read Events, Command DSL, Query state pipeline, DCB/precondition note, and Full example in `llms.txt`.
- Confirm examples are compact, internally consistent, imports cover shown helpers, and no touched section teaches raw `schemas + fold` event-history APIs.
- Regression check: confirm remaining stale app/Fastify/projector/processor docs, if any, are known follow-up scope for `02.md`, not accidentally made worse.

## Verification
- `rg -n "tagQuery|castTagQuery|eventsByTagsDescriptor|queryByTags|schemas:|fold:" llms.txt || true`
- `rg -n "defineEvent|defineReducer|BoundaryObservationError|ConcurrencyError" llms.txt`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
