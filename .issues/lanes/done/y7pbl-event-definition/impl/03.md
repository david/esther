# 03 — Use event definitions in read-model projection binding

## Source
- plan/01-implementation-plan.md

## Goal
Prove generated event schemas work end-to-end with `readModelEvent({ schema: Event.schema })` and existing read-model wiring.

## Depends on
- 01.md

## Scope
- `src/core/read-model.test.ts` or `src/__tests__/pipeline-wiring.test.ts`
- `src/__tests__/type-check.ts` for read-model handler inference if not already covered
- `src/core/event.ts` only if generated schema typing needs refinement
- `src/core/app.ts` only if event-type extraction import/wiring needs correction

## Contract surfaces
- read-model event binding schema input
- generated event schema extraction for `onAfterInsert` registration
- read-model projector handler event inference
- projection adapter execution behavior

## Plan-check watch items
- Keep direct `EventDefinition` acceptance in reducer/read-model/processor APIs out of this slice; users must pass `.schema`.
- Ensure generated `.schema` remains a real `z.ZodObject` with literal `type`, not wrapped by transform/refine that breaks `extractEventType`.
- Add root-import type tests proving public API, not only relative module imports.

## Work
- Add a focused failing runtime test using `defineEvent(...)` and `readModelEvent({ schema: Event.schema, handler })`.
- Use existing framework primitives, not mocks when practical: read model, in-memory event store/adapter, `createApp`, and input adapter path already used by tests.
- Verify only matching stored/emitted events trigger projection and project expected payload values.
- Verify handler receives typed event payload from generated schema; add compile-only assertion in `src/__tests__/type-check.ts` if runtime test cannot prove inference.
- Keep read-model API unchanged: the binding must pass `schema: Event.schema`, not the whole event definition.
- Fix only event helper/schema typing or event-type extraction wiring needed to make existing read-model path work.

## Implementation checkpoint
- write `impl/checkpoints/03.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `readModelEvent({ schema: Event.schema })` registers and handles generated-schema events.
- Matching event payload values project into the read model as expected.
- Unrelated event types do not project through this binding.
- Handler event type inference exposes the event payload from the event definition schema.
- No read-model DSL signature changes to accept `EventDefinition` directly.

## Suggested tests
- Add or extend one focused read-model projection test before production changes.
- Add compile-only handler inference assertion in `src/__tests__/type-check.ts` if needed.

## Manual verification
- not applicable; framework library projection behavior is covered by automated tests and has no UI/manual workflow.

## Verification
- `bun test src/core/read-model.test.ts`
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun run typecheck`
- `bun run lint`
