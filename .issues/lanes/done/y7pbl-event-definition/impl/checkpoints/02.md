# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-27

## Summary
- Added package-root type-check coverage for `defineEvent`, `EventDefinition`, `EventOf`, and `EventPayloadOf`.
- Proved schema-derived event/payload typing through `.create(...)`, command event callback return typing, and reducer event narrowing with `Event.schema`.

## Files changed
- `src/__tests__/type-check.ts`: adds compile-only public API assertions for event definition helper, payload mismatch failures, reducer schema inference, command return typing, and rejection of whole event definitions in reducer schemas.
- `.issues/lanes/in-progress/y7pbl-event-definition/impl/checkpoints/02.md`: records implementation checkpoint.

## Verification
- `bun run typecheck`: pass.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.

## Drift assessment
- Aligned with task and approved plan. Public root imports are covered, event/payload types derive from Zod payload schema output, and reducer/command APIs remain schema/event-value based as planned.
- Minor type-test note: reducer events inferred from Zod schema have mutable `tags` at schema output level, so test asserts assignability to `EventOf` plus handler narrowing instead of exact bidirectional type equality.

## Follow-ups
- Continue with `impl/03.md` for read-model projection binding coverage.
