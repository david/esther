# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-25

## Summary
- Added the core read-model registration type surface and normalization helper.
- Extended `AppConfig` with optional canonical `readModels` while keeping deprecated legacy `projectionAdapters` and `projectionQuery` accepted.
- Moved duplicate-name validation into registration normalization across canonical writable registrations, canonical views, and legacy entries.
- Added canonical writable adapter/handle name mismatch validation.
- Added focused app-wiring tests for canonical registration acceptance, duplicate detection, and mismatch errors.

## Files changed
- `src/core/read-model-registration.ts`: new focused core module for registration/getter/query types, legacy entry types, normalized shapes, duplicate validation, and canonical mismatch validation.
- `src/core/app.ts`: imports registration types/normalizer, exposes `readModels` in `AppConfig`, marks legacy config fields deprecated, and preserves legacy projection wiring behavior from normalized legacy entries.
- `src/index.ts`: exports the new public registration/getter/query types.
- `src/__tests__/pipeline.test.ts`: adds canonical registration validation coverage and updates duplicate-name error expectation.

## Verification
- `bun test src/__tests__/pipeline.test.ts`: pass — 30 tests passed.
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 21 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 219 tests passed.

## Manual verification
- not applicable — library-only contract foundation; automated runtime/type tests are the verification path.

## Drift assessment
- Aligned with task scope: canonical `readModels` is accepted at the type level, normalization lives outside `src/core/app.ts`, duplicate names are checked across canonical and legacy inputs, and canonical writable adapter/handle name mismatches throw a clear app-creation error.
- Preserved planned slice boundary: canonical registrations are normalized and validated, but legacy `projectionAdapters` remain the only source used for write/get/event/constraint behavior in this task; wiring canonical behavior is left to later tasks.
- Intentional diagnostic update: duplicate legacy registrations now use the clearer planned message `Duplicate read model registration name: "..."`.
- No high-risk behavior, persistence, replay, auth, side-effect, or verification drift found.

## Follow-ups
- Continue with `impl/02.md` to wire canonical in-memory registrations for writes and lookups.
