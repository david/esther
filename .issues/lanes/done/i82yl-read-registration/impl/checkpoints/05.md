# 05 — Implementation checkpoint

Status: aligned
Task: impl/05.md
Date: 2026-04-25

## Summary
- Added public type-flow coverage for canonical read-model registrations from in-memory and postgres adapter factories.
- Added type-level coverage that factory results remain destructurable as `{ adapter, get, query }` with typed row values.
- Added type-level coverage for typed read-only registrations with `get` and optional `query`, without write capability.
- Updated representative docs/LLM guidance to prefer `readModels: [create...ProjectionAdapter(handle)]` while preserving legacy compatibility notes.
- Verified the public registration type exports are already present in `src/index.ts` and legacy compatibility tests remain green.

## Files changed
- `src/__tests__/type-check.ts`: adds AppConfig/readModels, factory destructuring, and read-only registration type-flow checks.
- `doc/domain-language.md`: updates projection adapter terminology to describe app-ready `readModels` registrations and destructuring compatibility.
- `llms.txt`: updates the wiring example from legacy `projectionAdapters` to canonical `readModels` and keeps compatibility guidance.
- `.issues/lanes/in-progress/i82yl-read-registration/impl/checkpoints/05.md`: records this implementation checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 227 tests passed.

## Drift assessment
- Aligned with task scope: type-level coverage now proves `AppConfig.readModels` accepts in-memory and postgres factory results, factory results remain destructurable with typed `adapter`, `get`, and `query`, and typed read-only registrations can provide lookup/query capability without write capability.
- Public export acceptance is satisfied: `src/index.ts` already exports `ProjectionGetter`, `ProjectionQuery`, `WritableReadModelRegistration`, `ReadOnlyReadModelRegistration`, and `ReadModelRegistration`; no compatibility exports were removed.
- Documentation/examples now prefer canonical `readModels` in representative user-facing guidance, while legacy `projectionAdapters`/`projectionQuery` compatibility notes remain.
- Legacy table/view entries and legacy `projectionQuery` behavior remain covered by the existing pipeline/query-listing regression tests and full suite.
- Plan-check watch items remain satisfied: missing-query behavior, legacy metadata authority, and factory destructuring/app-readiness stayed green under full verification.

## Follow-ups
- none
