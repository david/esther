# 04 — Document strict DCB tag visibility guard

## Source
- plan/01-implementation-plan.md

## Goal

Update user-facing docs and LLM guide to describe strict observed-tag visibility guard, exported error, and rollout impact.

## Depends on
- 03.md

## Scope
- `doc/dcb.md`
- `doc/domain-language.md`
- `llms.txt`
- `src/core/pipeline.ts` comments only if previous task missed execution-order wording

## Contract surfaces
- public DCB behavior docs
- public package export guide
- command execution order docs
- release/rollout note for stricter runtime behavior

## Intent / decision vocabulary
- strict runtime guard, no opt-out
- `observedBoundary.tags ⊆ emittedEvent.tags`
- `EventTagMismatchError`
- DCB is not authorization
- default Fastify 422 remains acceptable unless explicit mapping changes later

## Plan-check watch items
- Update command execution order docs in both code comments and `llms.txt` so guard appears between event schema validation and append.
- Keep docs from describing mismatch guard as access control.
- Preserve docs for intersection semantics and single-boundary limit.

## Work
- Update `doc/dcb.md` to replace old sharp edge saying framework does not verify emitted tags.
- Document strict guard: command-side `tagQuery(...)` / `castTagQuery(...)` observed tags must be included in emitted event tags; extra event tags allowed; empty/global boundary passes.
- Keep intersection semantics, append precondition, single-boundary limit, and DCB-not-auth wording intact.
- Update `doc/domain-language.md` command/event emission notes with guard order after event schema validation and before append.
- Update `llms.txt` public exports list to include `EventTagMismatchError` constructor/type.
- Update `llms.txt` DCB/command pipeline guidance with mismatch behavior, stable error, no opt-out, and no adapter-owned policy.
- If no `llms.txt` update seems needed after code review, record why in `impl/checkpoints/04.md`; default expectation is update required.
- Run full repo gates.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- `doc/dcb.md` no longer says app authors alone own emitted-tag verification.
- Docs state missing observed tags return `EventTagMismatchError` before append.
- Docs state extra tags are allowed and DCB is not authorization.
- `llms.txt` lists `EventTagMismatchError` export and command pipeline guard behavior.
- Full typecheck, lint, and test gates pass.

## Suggested tests
- Documentation slice; no new test-first step. Preserve behavior coverage from earlier tasks.

## Manual verification
- not applicable — docs/LLM guide change plus full automated gates; no CLI/HTTP/manual product workflow changed.

## Verification
- `bun run typecheck`
- `bun run lint`
- `bun run test`
