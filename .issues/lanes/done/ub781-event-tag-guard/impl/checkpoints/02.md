# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-05-01

## Summary
- Added command pipeline guard after definition-backed event schema validation and before append.
- Added focused pipeline tests for matching observed tags, extra emitted tags, missing observed tags, schema-before-guard order, and empty/global observed tags.
- Updated existing transform command test data so observed boundary tags remain visible on emitted event while preserving append options.

## Files changed
- `src/core/pipeline.ts`: added `ensureObservedTagsVisibleOnEvent` / `missingObservedTags` guard and updated command execution order comments.
- `src/__tests__/pipeline-wiring.test.ts`: added definition-backed observed-tag guard tests and adjusted existing transformed-event success fixture for new invariant.
- `.issues/lanes/in-progress/ub781-event-tag-guard/impl/checkpoints/02.md`: recorded task checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: fail — test-first failure before guard; missing observed tag command returned ok.
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 36 pass, 0 fail, 161 expect calls.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json`.
- `bun run lint`: pass — ESLint and dependency-cruiser passed.

## Manual verification
- not applicable — library command pipeline behavior has no CLI/HTTP workflow here; focused automated dispatch tests cover slice behavior.

## Drift assessment
- Aligned with `impl/02.md`: guard runs after parsed event result and before `eventStore.append(...)`; returns `EventTagMismatchError` directly; matching and extra tags append; empty observed tags pass; malformed definition-backed event still returns `SchemaError` first.
- Aligned with `plan/01-implementation-plan.md`: core pipeline owns policy, adapters unchanged, append options unchanged when guard passes.
- Plan-check watch items satisfied: missing tag order follows observed tag order; set membership makes event tag order and extra tags irrelevant; no append verified by append counter/query; execution order comment updated.

## Follow-ups
- none for task 02; docs/`llms.txt` updates remain owned by later implementation task 04.
