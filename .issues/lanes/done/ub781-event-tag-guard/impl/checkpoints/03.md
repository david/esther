# 03 — Implementation checkpoint

Status: aligned
Task: impl/03.md
Date: 2026-05-01

## Summary
- Added pipeline coverage for raw command event mismatch, `castTagQuery(...)` mismatch, and no-mismatch fanout behavior.
- Kept core implementation unchanged because task 02's shared guard already covers raw and cast observation paths after event candidate parsing and before append.

## Files changed
- `src/__tests__/pipeline-wiring.test.ts`: added raw-path `EventTagMismatchError` test with append/projector/processor/effect counters and store query, plus `castTagQuery(...)` mismatch test proving no append.
- `.issues/lanes/in-progress/ub781-event-tag-guard/impl/checkpoints/03.md`: recorded task checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 38 pass, 0 fail, 175 expect calls.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json`.
- `bun run lint`: pass — ESLint and dependency-cruiser passed.

## Manual verification
- not applicable — side-effect behavior is internal library fanout; automated counters and event-store query are more reliable than manual observation.

## Drift assessment
- Aligned with `impl/03.md`: raw command event path now has explicit mismatch coverage returning `EventTagMismatchError`; `castTagQuery(...)` observations use the same guard; no append is proven by append counter and store query; no downstream fanout is proven across read-model binding/projector, processor, and effect counters.
- Aligned with `plan/01-implementation-plan.md`: guard remains in core pipeline, adapters unchanged, append boundary not reached on mismatch, and side effects run only after successful append.
- Aligned with plan-check watch items: no-append evidence does not rely only on result error; fanout coverage names current hook paths `onAfterInsert`/`onAfterCommit` through projection and processor/effect wiring; `EventTagMismatchError` bypasses `outputErr`.

## Follow-ups
- none for task 03; docs/`llms.txt` updates remain owned by task 04.
