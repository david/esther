# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-05-01

## Summary
- Updated DCB docs and domain language to describe strict observed-tag visibility guard after event schema validation and before append.
- Updated `llms.txt` with `EventTagMismatchError` export/type guidance, command pipeline order, mismatch behavior, no opt-out, core-owned policy, and default Fastify 422 note.
- Left `src/core/pipeline.ts` unchanged because task 02/03 already documented execution order in code comments.

## Files changed
- `doc/dcb.md`: replaced old emitted-tag sharp edge with strict guard behavior, extra-tags allowance, empty/global boundary behavior, and no-fanout mismatch outcome.
- `doc/domain-language.md`: documented command event emission guard order and DCB semantics without treating DCB as authorization.
- `llms.txt`: documented public export, error shape, command pipeline guard order, no opt-out, core-owned policy, Fastify default 422 behavior, and error notes.
- `.issues/lanes/in-progress/ub781-event-tag-guard/impl/checkpoints/04.md`: recorded task checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed.
- `bun run lint`: pass — ESLint and dependency-cruiser passed.
- `bun run test`: pass — 291 pass, 0 fail, 747 expect calls.

## Manual verification
- not applicable — documentation/LLM guide change only; full automated gates cover repo health and no CLI/HTTP/manual product workflow changed.

## Drift assessment
- Aligned with `impl/04.md`: `doc/dcb.md` no longer says app authors alone own emitted-tag verification; docs state missing observed tags return `EventTagMismatchError` before append; extra tags are allowed; DCB-not-authorization wording remains.
- Aligned with `plan/01-implementation-plan.md`: strict `observedBoundary.tags ⊆ emittedEvent.tags` guard is documented as no-opt-out, core-owned, after event schema validation and before append, with no adapter-owned mismatch policy.
- Aligned with plan-check watch items: command execution order is present in `llms.txt`; `src/core/pipeline.ts` comments already had the same order, so no source comment change was needed; intersection semantics and single-boundary limit were preserved.

## Follow-ups
- none
