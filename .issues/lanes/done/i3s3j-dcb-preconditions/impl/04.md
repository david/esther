# 04 — Add castTagQuery observations and non-observation guardrails

## Source
- plan/02-implementation-plan.md
- plan/checks/02-plan-sanity.md

## Goal

Extend the command observation behavior to `castTagQuery(...)` and pin that projection reads, derived/generated values, query slices, projectors, processors, and effects keep their existing contracts.

## Depends on
- 03.md

## Scope
- `src/core/slice.ts`
- `src/core/pipeline.ts`, only if task 03 leaves shared observation derivation needing a small adjustment
- `src/__tests__/pipeline.test.ts` or `src/__tests__/pipeline-wiring.test.ts`
- `src/core/slice.test.ts` or existing query/pipeline tests for read-only state behavior, if better localized
- `src/__tests__/type-check.ts`, only if `castTagQuery(...)` or public inference changes need coverage

## Contract surfaces
- command-side `castTagQuery(...)` observation recording
- projection lookup/query cast subject resolution remains unchanged
- `lookup(...)`, `derive(...)`, and `generate(...)` remain non-observing descriptors
- query-slice `state().pipe(tagQuery(...))` remains read-only and non-observing
- failed stale or multi-observation commands still produce no append/projector/processor/effect side effects

## Plan-check watch items
- Ensure multi-observation checking happens only after input resolution succeeds; domain/input descriptor errors should still route through `outputErr` as planned.
- Verify query-slice `state().pipe(tagQuery(...))` still has no append-precondition/observation behavior.
- Keep new casts within existing approved cast categories; avoid widening `ContextPatch`/descriptor types to loose `Record<string, unknown>` value shapes.
- Run the full repo gates, not only focused tests: `bun run typecheck`, `bun run lint`, `bun run test`.

## Work
- Add a focused failing command-pipeline test for successful `castTagQuery(...)` observation:
  - resolve the subject through the existing projection lookup/query cast path.
  - read event history for tags derived from the subject.
  - insert a matching event between `queryByTags(...)` and append.
  - assert dispatch returns `ConcurrencyError` and the command event is not appended.
- Add or preserve coverage that absent-subject and malformed-row behavior remains unchanged and does not become a boundary observation failure.
- Update command-side `castTagQuery(...).toStep(...)` to copy tags after successful subject resolution, call `queryByTags(...)`, and record `{ tags, maxPosition }` when the sink exists.
- Add focused guardrail tests proving non-event-history descriptors do not record append observations:
  - `lookup(...)` does not derive append options.
  - `derive(...)` does not derive append options.
  - `generate(...)` does not derive append options.
- Add or update a query-slice test proving `state().pipe(tagQuery(...))` remains read-only and does not interact with append preconditions.
- Confirm stale and multi-observation failures still skip success output, projectors, processors, and effects.
- Run full gates after focused tests pass.

## Implementation checkpoint
- write `impl/checkpoints/04.md` before finishing this task
- compare this slice against the source artifact and relevant approved plan-check
- classify the result as `aligned`, `minor-local-drift`, `high-risk-drift`, or `blocked`
- stop and report if a new high-risk behavior, contract, migration/replay, auth, side-effect, rollout, or verification issue appears

## Acceptance
- Command-side `castTagQuery(...)` records the exact queried tags and returned `maxPosition`, with tag arrays copied after successful subject resolution.
- A command with one `castTagQuery(...)` observation appends with matching `boundaryTags` and `expectedPosition`.
- A stale concurrent write on that cast boundary returns `ConcurrencyError` and does not append the command event.
- `castTagQuery(...)` absent-subject and malformed-row failures keep their prior error routing.
- `lookup(...)`, `derive(...)`, and `generate(...)` commands append as before without derived preconditions.
- Query-slice `state().pipe(tagQuery(...))` remains read-only and does not record DCB observations.
- Projection lookups, projectors, processors, effects, and successful output behavior are unchanged outside the stale/multi-observation failure cases.
- `bun run typecheck`, `bun run lint`, and `bun run test` pass.

## Suggested tests
- `src/__tests__/pipeline.test.ts` or `src/__tests__/pipeline-wiring.test.ts`: `castTagQuery(...)` stale-boundary integration test.
- Existing pipeline or slice tests: absent/malformed cast behavior remains unchanged.
- Existing pipeline or slice tests: `lookup(...)`, `derive(...)`, and `generate(...)` do not record observations.
- Existing query tests: query-slice `state().pipe(tagQuery(...))` remains non-observing/read-only.

## Verification
- `bun test src/__tests__/pipeline.test.ts`
- `bun test src/__tests__/pipeline-wiring.test.ts`
- `bun test src/core/slice.test.ts`
- `bun run typecheck`
- `bun run lint`
- `bun run test`
