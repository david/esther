# 04 — Implementation checkpoint

Status: aligned
Task: impl/04.md
Date: 2026-04-25

## Summary
- Added command-side `castTagQuery(...)` boundary observation recording after successful subject resolution, copying queried tags and recording the returned `maxPosition`.
- Added an integration stale-boundary test proving a successful `castTagQuery(...)` observation derives append preconditions and returns `ConcurrencyError` when the boundary changes before append.
- Added guardrail coverage proving `lookup(...)`, `derive(...)`, and `generate(...)` commands still append without observation-derived `AppendOptions`.
- Preserved existing absent-subject, malformed-row, query-slice read-only, and multi-observation side-effect guard coverage.

## Files changed
- `src/core/slice.ts`: records copied `castTagQuery(...)` boundary observations through the existing optional observation sink.
- `src/__tests__/pipeline-wiring.test.ts`: adds cast stale-boundary coverage, command side-effect negative assertions, and non-observing descriptor append-option guardrails.
- `.issues/lanes/in-progress/i3s3j-dcb-preconditions/impl/checkpoints/04.md`: records this checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 21 tests passed.
- `bun test src/core/slice.test.ts`: pass — 14 tests passed.
- `bun test src/__tests__/pipeline.test.ts`: pass — 26 tests passed.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint:code`: pass — ESLint completed with zero warnings.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.
- `bun run test`: pass — 209 tests passed.

## Drift assessment
- Aligned with `impl/04.md`: implemented command-side `castTagQuery(...)` observation recording and added focused stale-boundary coverage.
- Aligned with `plan/02-implementation-plan.md`: projection lookup/query cast subject resolution is unchanged; observation recording happens only after successful subject resolution; absent-subject and malformed-row failures keep their existing routing; one observed boundary appends with copied `{ boundaryTags, expectedPosition }` through task 03 pipeline behavior.
- Aligned with `plan/checks/02-plan-sanity.md` watch items: multi-observation checking remains after input resolution; query-slice `state().pipe(tagQuery(...))` remains read-only/non-appending; `lookup(...)`, `derive(...)`, and `generate(...)` do not record append observations; no loose `Record<string, unknown>` value shapes or new broad casts were introduced.
- Stale append failure coverage asserts no command event append, success output, command-event projector, processor, or effect side effects.
- No high-risk behavior, persistence/replay, auth, side-effect, rollout, or verification drift found.

## Follow-ups
- none
