# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-26

## Summary
- Added name-preserving operation type contracts for inferred command and query definitions.
- Added tuple-based operation lookup helpers for names, input, output, error, and `Result` extraction.
- Added compile-only assertions for preserved tuples, widened-array degradation, and dynamic dispatch compatibility.

## Files changed
- `src/core/slice.ts`: added generic operation names, helper types, and name-preserving `defineCommand`/`defineQuery` overloads.
- `src/index.ts`: exported the new operation helper types from the package root.
- `src/__tests__/type-check.ts`: covered literal name inference, helper extraction, negative cases, widened arrays, `App.dispatch`, and `DispatchFn`.
- `.issues/lanes/in-progress/hgqcm-typed-adapter-bindings/impl/checkpoints/01.md`: recorded this implementation checkpoint.

## Verification
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json` completed successfully.
- `bun run lint`: pass — ESLint and dependency-cruiser completed successfully.

## Manual verification
- not applicable — task only changes compile-time type contracts with no user-visible runtime behavior.

## Drift assessment
- Aligned with `impl/01.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- Dynamic runtime boundaries remain unchanged: `App.dispatch` and `DispatchFn` still accept `string` plus `unknown` and return `Promise<Result<unknown, unknown>>`.
- Literal precision is tied to preserved tuples; widened `ReadonlyArray<RegisterableOperation>` degrades to string/unknown rather than false route-level precision.
- No adapter imports, runtime behavior changes, schemas, persistence, events, processors, or side effects were added.

## Follow-ups
- none
