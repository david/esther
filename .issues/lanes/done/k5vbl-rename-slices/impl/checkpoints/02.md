# 02 — Implementation checkpoint

Status: aligned
Task: impl/02.md
Date: 2026-04-29

## Summary
- Converted canonical `createApp(...)` wiring in source tests and type-check examples from `slices` to `operations`.
- Preserved explicit deprecated `slices` runtime and type coverage from task 01.
- Left adapter route/request names and dispatch `sliceName` terminology unchanged.

## Files changed
- `src/__tests__/pipeline-wiring.test.ts`: canonical app configs now use `operations`.
- `src/__tests__/pipeline.test.ts`: canonical app configs now use `operations`.
- `src/__tests__/query-listing.test.ts`: canonical app configs now use `operations`.
- `src/__tests__/type-check.ts`: canonical app configs now use `operations`; deprecated alias and mixed-config type checks stay on `slices`.
- `src/core/app.test.ts`: non-alias app configs now use `operations`; explicit alias/mixed tests still use `slices`.
- `src/core/processor.test.ts`: canonical app configs now use `operations`.
- `src/core/read-model.test.ts`: canonical app configs now use `operations`.

## Verification
- `rg "createApp\\(|slices:" src README.md llms.txt doc`: pass for task scope — remaining `slices:` in `src` are deprecated alias coverage in `src/core/app.test.ts`, type-level alias/mixed checks in `src/__tests__/type-check.ts`, and public/deep docs scheduled for task 03; adapter route `slice` names unchanged.
- `bun run typecheck`: pass.
- `bun run lint`: pass.
- `bun run test`: pass — 281 tests.

## Drift assessment
- aligned with `impl/02.md`, `plan/01-implementation-plan.md`, and `plan/checks/01-plan-sanity.md`.
- `sliceMeansEventModelingBoundaryOutsideAppConfig`: test examples now prefer `operations` for AppConfig wiring.
- `dynamicDispatchUnchanged`: no dispatch parameter, Fastify route `slice`, CLI `sliceName`, or adapter contract rename.
- `acceptDeprecatedSlicesAlias`: preserved through task 01 runtime and type tests.

## Follow-ups
- Continue with `impl/03.md` to update README, `llms.txt`, and docs guidance.
