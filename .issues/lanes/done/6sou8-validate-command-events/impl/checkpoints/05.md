# 05 — Implementation checkpoint

Status: aligned
Task: impl/05.md
Date: 2026-04-29

## Summary
- Added runtime regression coverage for transform-schema definition-backed commands.
- Proved dispatch accepts schema-input candidate payloads, appends parsed schema-output events, and passes parsed events to `output(event, ctx)`.
- Proved malformed transform candidates return `SchemaError("Event validation failed", issues)` before append, output, projectors, processors, and effects.
- Renamed pipeline ordering comments/local variables around candidate vs parsed event to preserve the chosen contract.

## Files changed
- `src/__tests__/pipeline-wiring.test.ts`: adds transform success/failure dispatch coverage, parsed append/output assertions, append option preservation, and no-downstream regression checks.
- `src/core/pipeline.ts`: clarifies runtime ordering with `CommandEventCandidate` and parsed event naming; append/output still use the parse result.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/05.md`: records task checkpoint.

## Verification
- `bun test src/__tests__/pipeline-wiring.test.ts`: pass — 26 tests, 118 assertions.
- `bun run typecheck`: pass — `tsgo --noEmit -p tsconfig.json`.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.

## Drift assessment
- Matches task goal and source plan: definition-backed dispatch builds a schema-input candidate, validates it with `EventDefinition.schema`, then uses only the parsed output event for append and `output(event, ctx)`.
- Malformed transform candidate stays on framework-error path and bypasses `outputErr`, append, projectors, read-model bindings, processors, and effects.
- Raw command path remains unchanged and unvalidated by event definitions; existing raw compatibility coverage still passes.
- Boundary observation append options are preserved for successfully parsed transform events.
- No persistence, replay, read-model, processor, adapter, or public type contract changes were introduced beyond parsed event value preservation.
- No new casts were added.

## Follow-ups
- Continue with task 06.
