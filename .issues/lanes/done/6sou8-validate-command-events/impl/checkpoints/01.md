# 01 — Implementation checkpoint

Status: aligned
Task: impl/01.md
Date: 2026-04-29

## Summary
- Added event-definition-backed `defineCommand` overloads with `event`, `tags(ctx)`, and `payload(ctx)`.
- Normalized definition-backed commands into existing `event(ctx)` runtime shape via `EventDefinition.create(...)`.
- Added optional `eventSchema` command metadata for later pipeline validation.
- Added type-check coverage for valid inference, wrong payloads, wrong tag returns, and raw command compatibility.

## Files changed
- `src/core/slice.ts`: adds event-definition command definition shape, overloads, normalization, and optional schema metadata.
- `src/__tests__/type-check.ts`: adds public DSL type coverage for definition-backed command event emission.
- `.issues/lanes/in-progress/6sou8-validate-command-events/impl/checkpoints/01.md`: records task checkpoint.
- `.issues/lanes/in-progress/6sou8-validate-command-events/**`: issue moved from backlog to in-progress before coding.

## Verification
- `bun run typecheck`: pass — type-level command event API checks compile.
- `bun run lint`: pass — ESLint and dependency-cruiser clean.

## Drift assessment
- Matches task scope: changed only `src/core/slice.ts` and `src/__tests__/type-check.ts` plus workflow checkpoint/lane move.
- Raw `event(ctx) => EventRecordInput` form remains supported, including existing explicit generic call sites.
- `payload(ctx)` uses `z.output` via `EventPayloadOf` to match `EventDefinition.create(...)`.
- New cast is local to overload normalization and documented near the cast.
- No public helper types exported; existing public exports were enough for callers and tests.
- `llms.txt` not updated in this task because task 03 owns docs after runtime validation exists.

## Follow-ups
- Continue with task 02 to enforce `eventSchema` validation before append.
